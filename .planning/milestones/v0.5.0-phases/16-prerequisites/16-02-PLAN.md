---
phase: 16-prerequisites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/publish-skill.sh
  - tests/run.sh
autonomous: true
requirements:
  - DEBT-02

must_haves:
  truths:
    - "`conjure publish-skill my-skill myorg/myrepo` resolves TARGET_REPO from positional $2 without requiring TARGET_REPO env"
    - "Positional $2 takes priority over TARGET_REPO env when both are present"
    - "When TARGET_REPO env is set and $2 is absent, the env value is used but a WARN: deprecation line is printed to stderr"
    - "When neither positional $2 nor TARGET_REPO env is set, the script prints the updated usage line and exits 2"
    - "The hardcoded default `TARGET_REPO=mohandoz/conjure` is removed"
    - "The --to flag still works as before (backwards compatible)"
    - "Usage line reads: conjure publish-skill <name> <org/repo> [--dry-run]"
    - "All existing SKILL-01 through SKILL-04 regression tests still pass"
    - "New regression test covering positional arg, deprecation warn, and missing-both-exit-2 passes"
  artifacts:
    - path: "scripts/publish-skill.sh"
      provides: "positional $2 arg parsing for org/repo"
      contains: "DEBT-02"
    - path: "tests/run.sh"
      provides: "SKILL-05 regression block"
      contains: "SKILL-05"
  key_links:
    - from: "scripts/publish-skill.sh"
      to: "TARGET_REPO"
      via: "positional $2 or deprecated env fallback"
      pattern: "TARGET_REPO"
---

<objective>
Refactor `scripts/publish-skill.sh` to accept `org/repo` as positional `$2` per DEBT-02.
Remove the hardcoded default. Emit a deprecation warning when `TARGET_REPO` env is used.
Add SKILL-05 regression test block to `tests/run.sh`.

Purpose: Clean up the publish-skill interface so downstream callers (Phase 19 Auto-PR)
can pass `org/repo` as a plain positional argument instead of injecting an env var.

Output: Updated publish-skill.sh + SKILL-05 test block in tests/run.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-prerequisites/16-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor publish-skill.sh — positional $2 + deprecation + remove default</name>
  <files>scripts/publish-skill.sh</files>
  <read_first>scripts/publish-skill.sh — read the full file. The critical sections are: (1) line 22 `TARGET_REPO="${TARGET_REPO:-mohandoz/conjure}"` — this default must be removed; (2) lines 24-25 SKILL_NAME=$1 + shift; (3) lines 27-42 the while loop handling --to/--to=*/--dry-run/-h. The new positional $2 must be consumed before the while loop processes flags. (4) lines 46-49 the org/repo format validation — this already validates TARGET_REPO; it will catch a malformed positional arg automatically.</read_first>
  <behavior>
    - `publish-skill.sh my-skill myorg/myrepo` → TARGET_REPO=myorg/myrepo, no warning, proceeds normally
    - `publish-skill.sh my-skill myorg/myrepo --dry-run` → TARGET_REPO=myorg/myrepo, DRY_RUN=1
    - `publish-skill.sh my-skill` with TARGET_REPO=myorg/myrepo in env → uses env value, prints "WARN: TARGET_REPO env var is deprecated; use 'conjure publish-skill <name> <org/repo>' instead" to stderr, proceeds
    - `publish-skill.sh my-skill` with no env and no positional → prints usage line "Usage: conjure publish-skill <name> <org/repo> [--dry-run]" to stderr, exits 2
    - `publish-skill.sh my-skill --to myorg/myrepo` → still works (--to flag path unchanged)
    - Positional $2 takes priority: `TARGET_REPO=other/repo publish-skill.sh my-skill myorg/myrepo` → uses myorg/myrepo (no deprecation warning)
  </behavior>
  <action>Apply these changes to scripts/publish-skill.sh per DEBT-02:

1. Remove the hardcoded default on line 22. Change `TARGET_REPO="${TARGET_REPO:-mohandoz/conjure}"` to `TARGET_REPO="${TARGET_REPO:-}"` (empty default, preserving env passthrough without a hardcoded fallback).

2. After `SKILL_NAME="${1:-}"` and `shift || true`, add positional $2 consumption: check if `$#` is greater than 0 and the first remaining arg does NOT start with `-` (i.e., it looks like an org/repo positional, not a flag). If so, consume it as TARGET_REPO: `TARGET_REPO="$1"; shift || true`. This must happen before the while loop so flags after the positional still parse correctly.

3. After the while loop closes (after the done statement), add the deprecation and missing-arg guards. First: if TARGET_REPO is non-empty AND it was not set by the positional (we need a flag to track this — add `REPO_FROM_POS=0` before the positional check, set `REPO_FROM_POS=1` when consuming positional, leave 0 otherwise). Then: if `[ "$REPO_FROM_POS" = "0" ] && [ -n "$TARGET_REPO" ]`, print the deprecation warning to stderr: `echo "WARN: TARGET_REPO env var is deprecated; use 'conjure publish-skill <name> <org/repo>' instead" >&2`. Then: if `[ -z "$TARGET_REPO" ]`, print usage to stderr and `exit 2`.

4. Update the usage line in the -h/--help case and in the validation guard at the bottom to read: `Usage: conjure publish-skill <name> <org/repo> [--dry-run]`.

5. Update the Usage comment in the file header (lines 7-8) to match: `bash scripts/publish-skill.sh <skill-name> <org/repo> [--dry-run]`.

Exit code discipline: missing arg exits 2 (hard prerequisite failure, per existing convention in this file). No change to any other exit codes.</action>
  <verify>
    <automated>bash -c 'cd /tmp && mkdir -p test-conjure-skill-$$/scripts test-conjure-skill-$$/lib; cp /Users/mohandoz/u01/innovate/conjure/scripts/publish-skill.sh test-conjure-skill-$$/scripts/; cp /Users/mohandoz/u01/innovate/conjure/lib/mutate.sh test-conjure-skill-$$/lib/; RC=0; ( cd test-conjure-skill-$$ && bash scripts/publish-skill.sh myskill 2>&1 ) || RC=$?; echo "missing-both RC=$RC"; rm -rf test-conjure-skill-$$' 2>&1 | grep "RC=2" && echo PASS || echo FAIL</automated>
  </verify>
  <done>scripts/publish-skill.sh has no hardcoded `mohandoz/conjure` default. Positional $2 sets TARGET_REPO with REPO_FROM_POS=1. TARGET_REPO env without $2 triggers the WARN: deprecation line to stderr. Neither $2 nor env → exit 2. --to flag still works. Help text reads `conjure publish-skill <name> <org/repo> [--dry-run]`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SKILL-05 regression test to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>tests/run.sh — read lines 893-1075 (the SKILL publish-skill test block) to understand the sandbox setup pattern: SKILL_DIR mktemp, git init, copy scripts + lib, commit + tag, skill_run() helper. The SKILL-05 block appends to this sandbox before the cleanup `rm -rf "$SKILL_DIR"` + `trap - EXIT`. Insert SKILL-05 between SKILL-04 and the cleanup lines.</read_first>
  <behavior>
    - SKILL-05a: positional $2 works — `skill_run test-skill myorg/myrepo` exits 0 and output contains "myorg/myrepo" (PR instructions reference the correct repo)
    - SKILL-05b: deprecation warn — run with TARGET_REPO env and no $2; capture stderr; assert it contains "WARN: TARGET_REPO env var is deprecated"; command still exits 0 (not blocked)
    - SKILL-05c: missing both — run with no $2 and no TARGET_REPO env; assert exit code is 2; assert stderr contains the usage line "conjure publish-skill"
    - SKILL-05d: positional takes priority over env — run with both $2 and TARGET_REPO=other/repo set; assert output contains positional repo and NOT the env repo (no deprecation warning)
  </behavior>
  <action>After the SKILL-04 test block and before `rm -rf "$SKILL_DIR"` + `trap - EXIT`, insert a SKILL-05 block:

`echo ""` + `echo "▸ SKILL-05: positional arg + deprecation (DEBT-02)"`.

Sub-case a (positional $2 works): `P2_OUT="$(skill_run test-skill myorg/myrepo 2>&1)"`. Assert `printf '%s\n' "$P2_OUT" | grep -q 'myorg/myrepo'` → pass/fail "positional $2 sets target repo (SKILL-05a)".

Sub-case b (deprecation warn): `DEPR_ERR="$(TARGET_REPO=myorg/myrepo skill_run test-skill 2>&1 1>/dev/null)"`. Assert `printf '%s\n' "$DEPR_ERR" | grep -q 'WARN: TARGET_REPO'` → pass/fail "TARGET_REPO env emits deprecation WARN (SKILL-05b)". Also assert the command exits 0 by running `TARGET_REPO=myorg/myrepo skill_run test-skill >/dev/null 2>&1` and checking RC.

Sub-case c (missing both exits 2): `MISS_RC=0; skill_run test-skill >/dev/null 2>&1 || MISS_RC=$?`. Assert `[ "$MISS_RC" -eq 2 ]` → pass/fail "missing $2 and no TARGET_REPO env exits 2 (SKILL-05c)". Also assert stderr contains usage: `MISS_ERR="$(skill_run test-skill 2>&1 || true)"` + grep for "conjure publish-skill".

Sub-case d (positional priority): `PRIO_OUT="$(TARGET_REPO=other/repo skill_run test-skill myorg/myrepo 2>&1)"`. Assert output contains "myorg/myrepo" (pass) AND does NOT contain "WARN:" (pass "positional $2 takes priority over TARGET_REPO env (SKILL-05d)"). No deprecation warning when positional is present.

Use the existing `skill_run` helper throughout — it cd's into SKILL_DIR and runs the script.</action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E "(PASS|FAIL|SKILL-05)" | head -20</automated>
  </verify>
  <done>tests/run.sh contains a "SKILL-05" block with passing assertions for positional $2, deprecation warn, missing-both-exit-2, and positional-priority. `bash tests/run.sh` exits 0. SKILL-01 through SKILL-04 still pass (no regressions). The block is placed between SKILL-04 and the SKILL_DIR cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI caller → publish-skill.sh $2 | User-supplied org/repo string; validated by existing grep -qE org/repo format check before any git or gh invocation |
| env TARGET_REPO → publish-skill.sh | Env var; same format validation applies; deprecation path does not bypass validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-16-03 | Tampering | positional $2 (org/repo injection) | mitigate | Existing `grep -qE '^[a-zA-Z0-9_-]+/[a-zA-Z0-9_.-]+$'` validation at line 46 applies to TARGET_REPO regardless of source (positional or env); malformed values exit 1 before any git/gh call |
| T-16-04 | Information Disclosure | WARN deprecation message | accept | Message only reveals that TARGET_REPO env was used — no secrets disclosed; printed to stderr only |
| T-16-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `bash tests/run.sh` exits 0 with FAIL count = 0
2. `grep -n 'mohandoz/conjure' scripts/publish-skill.sh` returns 0 lines (hardcoded default removed)
3. `grep -c 'SKILL-05' tests/run.sh` returns >= 4
4. `grep 'REPO_FROM_POS' scripts/publish-skill.sh` returns at least 2 lines (declaration + assignment)
5. Running the script with no args: `bash scripts/publish-skill.sh 2>&1; echo RC=$?` shows usage line and RC=2 (empty SKILL_NAME check fires first — which is the existing guard at line 44; missing TARGET_REPO guard fires after skill name check, so test must supply a skill name but no repo)
6. `bash scripts/publish-skill.sh somesk 2>&1; echo RC=$?` shows usage and RC=2 (missing repo guard)
7. No `mohandoz/conjure` default anywhere in scripts/publish-skill.sh
</verification>

<success_criteria>
- Positional $2 accepted as org/repo with priority over TARGET_REPO env (per DEBT-02)
- TARGET_REPO env still works as deprecated fallback with a WARN: stderr message
- Hardcoded default `mohandoz/conjure` removed; missing both → exit 2
- --to flag unchanged and backwards compatible
- Usage line updated to `conjure publish-skill <name> <org/repo> [--dry-run]`
- All SKILL-01 through SKILL-05 tests pass; bash tests/run.sh exits 0
</success_criteria>

<output>
Create `.planning/phases/16-prerequisites/16-02-SUMMARY.md` when done
</output>
