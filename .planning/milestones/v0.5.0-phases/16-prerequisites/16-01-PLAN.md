---
phase: 16-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mutate.sh
  - tests/run.sh
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "mutate_rm <path> is defined in lib/mutate.sh and callable by sourcing the file"
    - "With DRY_RUN=1, mutate_rm prints '[dry-run] would rm <path>' and increments CONJURE_DRY_MUTATION_COUNT without touching the filesystem"
    - "With DRY_RUN=0, mutate_rm executes 'rm -f <path>' and removes the file"
    - "Regression test in tests/run.sh exercises both dry-run and live paths and passes"
    - "Existing mutate_cp and mutate_write tests still pass (no regressions)"
  artifacts:
    - path: "lib/mutate.sh"
      provides: "mutate_rm function"
      contains: "mutate_rm"
    - path: "tests/run.sh"
      provides: "mutate_rm regression test block"
      contains: "mutate_rm"
  key_links:
    - from: "lib/merge.sh (Phase 18)"
      to: "lib/mutate.sh:mutate_rm"
      via: "source lib/mutate.sh; mutate_rm <sidecar-path>"
      pattern: "mutate_rm"
---

<objective>
Add `mutate_rm` to `lib/mutate.sh` — the dry-run-safe file deletion primitive required by
Phase 18 (`conjure resolve`) per INFRA-01.

Purpose: `conjure resolve` must remove resolved conflict sidecars. Without `mutate_rm`,
Phase 18 cannot call a dry-run-safe delete through the established mutation chokepoint.

Output: `mutate_rm` function in `lib/mutate.sh` + regression test block in `tests/run.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-prerequisites/16-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mutate_rm to lib/mutate.sh</name>
  <files>lib/mutate.sh</files>
  <read_first>lib/mutate.sh — read the full file; extract the exact guard pattern used by mutate_cp (DRY_RUN check, echo format, counter increment, return 0) and the live-mode operation. mutate_rm mirrors this pattern verbatim.</read_first>
  <behavior>
    - mutate_rm called with DRY_RUN=1: prints exactly "[dry-run] would rm <path>", increments CONJURE_DRY_MUTATION_COUNT by 1, returns 0, filesystem unchanged
    - mutate_rm called with DRY_RUN=0: executes rm -f "$1", file is removed from disk
    - mutate_rm called with DRY_RUN unset: treats as DRY_RUN=0 (matches ${DRY_RUN:-0} guard used by siblings)
  </behavior>
  <action>Insert mutate_rm after the closing brace of mutate_write and before the mutate_summary function. The function body must use the identical guard pattern as mutate_cp: check `[ "${DRY_RUN:-0}" = "1" ]`; on dry branch echo "[dry-run] would rm $1" and increment CONJURE_DRY_MUTATION_COUNT; on live branch call `rm -f "$1"`. Add a comment block above the function consistent with existing sibling comment style. Do NOT add -r flag (per D decision: callers control recursive logic; Phase 18 only deletes individual sidecar files). Update the Usage comment block at the top of the file to add `mutate_rm <path>` to the listed callable functions.</action>
  <verify>
    <automated>
      DRY_RUN=1 bash -c 'source lib/mutate.sh; mutate_rm /tmp/test-conjure-rm; echo "count=$CONJURE_DRY_MUTATION_COUNT"' | grep -q "would rm" && echo PASS || echo FAIL
    </automated>
  </verify>
  <done>lib/mutate.sh contains a mutate_rm function. DRY_RUN=1 path prints "[dry-run] would rm <arg>" and increments CONJURE_DRY_MUTATION_COUNT. DRY_RUN=0 path calls rm -f. Function is placed between mutate_write and mutate_summary. Usage comment at top lists mutate_rm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mutate_rm regression test to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>tests/run.sh — locate the "3-way merge tests" section (search for "source lib/mutate.sh"). The file already sources lib/mutate.sh before MERGE-01. Add the mutate_rm block in the "Dry-run enforcement" section or immediately after it — before "Migration scripts exist" — so it sits alongside the existing dry-run assertions for the mutate library. Read lines 186-232 (the dry-run enforcement block and its cleanup) to understand where to insert without breaking the trap/EXIT pattern.</read_first>
  <behavior>
    - Test DRY_RUN path: source lib/mutate.sh; call mutate_rm with a temp path; assert output contains "[dry-run] would rm"; assert CONJURE_DRY_MUTATION_COUNT equals 1; assert file was not deleted
    - Test live path: create a real temp file; source lib/mutate.sh; set DRY_RUN=0; call mutate_rm; assert file no longer exists
    - Label assertions with "mutate_rm" prefix so grep can isolate them
  </behavior>
  <action>Add a new labeled section `echo ""` + `echo "▸ mutate_rm unit tests (INFRA-01)"` after the existing dry-run enforcement block (after the `trap - EXIT` on line 222 that closes that block). Write two test sub-cases using the same pass/fail helpers already in scope. Sub-case 1 (dry-run): use a mktemp path but do NOT create the file (rm -f on a nonexistent path is valid; the test only cares about counter and output); invoke via subshell that sources lib/mutate.sh and calls `DRY_RUN=1 mutate_rm "$TMPPATH"`, capture output with $(), then assert output contains "would rm" (pass/fail) and assert no filesystem change (path still absent — same condition before and after). Sub-case 2 (live): `TMPFILE="$(mktemp)"`, source lib/mutate.sh, set `DRY_RUN=0`, call `mutate_rm "$TMPFILE"`, assert `[ ! -f "$TMPFILE" ]` (pass/fail). Do not register an EXIT trap for these temp files — mktemp files left after live rm are already gone; dry-run path never created a file. Keep existing trap/EXIT patterns for all other blocks intact.</action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E "(PASS|FAIL|mutate_rm)" | head -20</automated>
  </verify>
  <done>tests/run.sh contains a "mutate_rm unit tests (INFRA-01)" section with passing dry-run and live assertions. `bash tests/run.sh` exits 0 (FAIL count is 0). Output includes "✓" lines for both mutate_rm sub-cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → mutate_rm | Path argument supplied by caller script; in Phase 18 it will be a `.conjure-conflict-*` sidecar path constructed from known prefixes — no user-supplied raw paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-16-01 | Tampering | mutate_rm $1 (path arg) | accept | Callers (Phase 18) construct paths from controlled prefixes; `rm -f` on a wrong path at worst removes one file; no escalation possible |
| T-16-02 | Denial of Service | DRY_RUN counter overflow | accept | bash arithmetic; counter reaches shell integer max only after billions of calls — not a realistic threat in CLI context |
| T-16-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `bash tests/run.sh` exits 0 with FAIL count = 0
2. `grep -n 'mutate_rm' lib/mutate.sh` returns at least 4 lines (comment, function def, dry-run echo, counter, rm -f)
3. `grep -c 'mutate_rm' tests/run.sh` returns >= 4 (section header + assertions)
4. `DRY_RUN=1 bash -c 'source lib/mutate.sh; F=/tmp/should-not-exist-conjure-$$; mutate_rm "$F"; [ ! -f "$F" ] && echo OK'` prints OK
5. `bash -c 'source lib/mutate.sh; F=$(mktemp); DRY_RUN=0 mutate_rm "$F"; [ ! -f "$F" ] && echo OK'` prints OK
</verification>

<success_criteria>
- mutate_rm is present in lib/mutate.sh following the exact guard pattern of mutate_cp
- DRY_RUN=1 path: "[dry-run] would rm <path>" printed, counter incremented, no filesystem mutation
- DRY_RUN=0 path: file removed via rm -f
- tests/run.sh passes with zero FAILs including the new mutate_rm assertions
- No regressions to mutate_cp, mutate_write, or any other existing test
</success_criteria>

<output>
Create `.planning/phases/16-prerequisites/16-01-SUMMARY.md` when done
</output>
