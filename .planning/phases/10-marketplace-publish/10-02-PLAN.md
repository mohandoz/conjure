---
phase: 10-marketplace-publish
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - scripts/publish-plugin.sh
  - cli/conjure
  - .claude-plugin/submit-entry.json
autonomous: true
requirements:
  - MKTPL-01
  - MKTPL-04

must_haves:
  truths:
    - "conjure publish updates .plugins[0].source.sha in marketplace.json to HEAD SHA"
    - "conjure publish updates .plugins[0].version in marketplace.json to VERSION file value"
    - "conjure publish updates .version in plugin.json to VERSION file value"
    - "conjure publish aborts with exit code 2 when working tree is dirty"
    - "conjure publish --dry-run prints mutations without writing files"
    - "conjure publish --submit writes .claude-plugin/submit-entry.json"
    - "conjure publish --submit prints a checklist to stdout"
    - "All writes go through mutate_write (DRY_RUN honored)"
  artifacts:
    - path: "scripts/publish-plugin.sh"
      provides: "Worker script for manifest update and submit-entry generation"
      contains: "mutate_write"
    - path: ".claude-plugin/submit-entry.json"
      provides: "Catalog PR snippet written by --submit flag"
  key_links:
    - from: "cli/conjure cmd_publish"
      to: "scripts/publish-plugin.sh"
      via: "CONJURE_HOME DRY_RUN env-prefixed bash invocation"
      pattern: "bash.*publish-plugin\\.sh"
    - from: "scripts/publish-plugin.sh"
      to: "lib/mutate.sh"
      via: "source mutate.sh"
      pattern: "source.*mutate\\.sh"
    - from: "scripts/publish-plugin.sh"
      to: ".claude-plugin/marketplace.json"
      via: "jq --arg sha --arg ver + mutate_write"
      pattern: "mutate_write.*marketplace\\.json"
---

<objective>
Create `scripts/publish-plugin.sh` and wire `cmd_publish` in `cli/conjure` so that `conjure publish` updates both manifest files with the current HEAD SHA and version, and `conjure publish --submit` additionally writes `.claude-plugin/submit-entry.json` with the catalog submission snippet.

Purpose: Delivers MKTPL-01 (SHA + version update with local validation) and MKTPL-04 (submit entry + checklist). The script follows the existing worker pattern (sources lib/mutate.sh, respects DRY_RUN, calls mutate_summary at end).

Output: scripts/publish-plugin.sh (new), cli/conjure updated with cmd_publish and dispatch entry, .claude-plugin/submit-entry.json (created by --submit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-marketplace-publish/10-RESEARCH.md
@.planning/phases/10-marketplace-publish/10-PATTERNS.md
@.planning/phases/10-marketplace-publish/10-CONTEXT.md
@.planning/phases/10-marketplace-publish/10-01-SUMMARY.md
@lib/mutate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/publish-plugin.sh (per D-05, D-06, D-07, D-08, D-09, D-10, D-11)</name>
  <files>scripts/publish-plugin.sh</files>
  <action>Create scripts/publish-plugin.sh as a new executable worker script following the init-project.sh pattern exactly.

File structure and behavior requirements:

HEADER: shebang `#!/usr/bin/env bash`, comment block describing purpose + usage + exit codes (0=success, 1=validation error, 2=dirty tree / missing dep), `set -euo pipefail`.

CONJURE_HOME RESOLUTION: `CONJURE_HOME="$(cd "$(dirname "$0")/.." && pwd)"` — self-resolving, not relying on caller.

SOURCE mutate.sh: `source "$CONJURE_HOME/lib/mutate.sh"` immediately after CONJURE_HOME resolution.

ENV DEFAULTS: `DRY_RUN="${DRY_RUN:-0}"` and `CONJURE_SUBMIT="${CONJURE_SUBMIT:-0}"` at top before arg parsing so both env var and flag paths work.

ARG PARSING: while loop consuming $@; cases: --submit sets CONJURE_SUBMIT=1, --dry-run sets DRY_RUN=1, -h|--help prints usage and exits 0, * prints "Unknown argument: $1" >&2 and exits 1.

PREREQUISITE CHECKS (exit 2 on failure):
1. jq availability: `if ! command -v jq >/dev/null 2>&1; then echo "✗ jq not installed" >&2; exit 2; fi`
2. git availability: same pattern for git
3. PLUGIN_DIR sanity: `PLUGIN_DIR="$CONJURE_HOME/.claude-plugin"` — hardcoded, never user-supplied (prevents path traversal per RESEARCH.md security domain). Check `[ -f "$PLUGIN_DIR/marketplace.json" ]` or exit 2.

DIRTY-TREE ABORT (per D-06 — exit 2 to signal hard prerequisite failure, not validation error):
```
if ! git -C "$CONJURE_HOME" diff --quiet || ! git -C "$CONJURE_HOME" diff --cached --quiet; then
  echo "✗ Working tree has uncommitted changes — commit or stash before publishing." >&2
  exit 2
fi
```

VERSION + SHA READS:
```
CURRENT_VERSION="$(cat "$CONJURE_HOME/VERSION" 2>/dev/null || echo unknown)"
[ "$CURRENT_VERSION" = "unknown" ] && { echo "✗ VERSION file missing" >&2; exit 2; }
CURRENT_SHA="$(git -C "$CONJURE_HOME" rev-parse HEAD)"
```

PRE-WRITE jq VALIDATION (per D-07 — validate existing files before mutating):
Run `jq empty "$PLUGIN_DIR/marketplace.json"` and `jq empty "$PLUGIN_DIR/plugin.json"` — exit 1 on parse error with descriptive message.

JQ MUTATIONS (per D-05 — SHA to .plugins[0].source.sha, version to both .plugins[0].version and .version):
Build updated JSON in variables using `jq --arg sha "$CURRENT_SHA" --arg ver "$CURRENT_VERSION"`. For marketplace.json: update `.plugins[0].source.sha = $sha | .plugins[0].version = $ver`. For plugin.json: update `.version = $ver`. After each jq call, validate the output with `printf '%s' "$NEW_X" | jq empty 2>/dev/null || { echo "✗ jq produced invalid JSON" >&2; exit 1; }`.

WRITE via mutate_write (per D-08):
```
echo "▸ conjure publish: updating marketplace.json (version=$CURRENT_VERSION sha=${CURRENT_SHA:0:12}...)"
mutate_write "$PLUGIN_DIR/marketplace.json" "$NEW_MKT"
echo "✓ marketplace.json updated"
mutate_write "$PLUGIN_DIR/plugin.json" "$NEW_PLG"
echo "✓ plugin.json updated"
```

SUBMIT PATH (per D-09, D-10, D-11 — only when CONJURE_SUBMIT=1):
Build submit-entry.json using jq with the structure from RESEARCH.md: name, description, source (same source/repo/ref/sha fields as plugins[0].source), homepage, category. Use `jq -n --arg sha "$CURRENT_SHA" ...` to build from scratch.

Print stdout checklist with all items per D-11:
```
echo ""
echo "▸ conjure publish --submit checklist:"
echo "  [ ] Run: claude plugin validate . && claude plugin validate .claude-plugin/plugin.json"
echo "  [ ] Commit marketplace.json, plugin.json, and submit-entry.json"
echo "  [ ] Push branch and create a release tag"
echo "  [ ] Re-run 'conjure publish' after tagging to update SHA to tag commit"
echo "  [ ] Visit: https://claude.ai/settings/plugins/submit"
echo "  [ ] Paste the contents of .claude-plugin/submit-entry.json into the submission form"
echo "  NOTE: Direct PRs to anthropics/claude-plugins-community are auto-closed."
echo "        Use the web form at the URL above."
```

Write submit-entry.json via mutate_write:
```
mutate_write "$PLUGIN_DIR/submit-entry.json" "$SUBMIT_JSON"
echo "✓ submit-entry.json written"
```

FINAL: `mutate_summary` always last, then `exit 0`.

Make the file executable: `chmod +x scripts/publish-plugin.sh`.
Use `jq --arg` for all variable interpolation — never string concatenation into JSON.
Do NOT use temp files for jq transformations; build content in variables and pass to mutate_write.</action>
  <verify>
    <automated>shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/publish-plugin.sh && echo "shellcheck pass"</automated>
  </verify>
  <done>scripts/publish-plugin.sh exists, is executable, passes shellcheck with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Add cmd_publish to cli/conjure dispatch (per D-05, D-08)</name>
  <files>cli/conjure</files>
  <action>Add `cmd_publish` function and dispatch table entry to cli/conjure following the cmd_audit/cmd_init pattern established in the codebase.

Two changes required in cli/conjure:

CHANGE 1 — Add `conjure publish` to the usage() heredoc. The heredoc is at the top of the file. Add this line after `conjure preflight` (alphabetical order):
```
  conjure publish [--submit] [--dry-run]
```

CHANGE 2 — Add cmd_publish function. Place it after cmd_preflight (around line 260) and before the dispatch table. The function must follow cmd_audit pattern exactly:

```bash
cmd_publish() {
  local do_submit=0
  local dryrun=0
  while [ $# -gt 0 ]; do
    case "$1" in
      --submit)    do_submit=1 ;;
      --dry-run)   dryrun=1 ;;
      --help|-h)   echo "Usage: conjure publish [--submit] [--dry-run]"; return 0 ;;
      *)           echo "Unknown option: $1"; return 1 ;;
    esac
    shift
  done
  CONJURE_HOME="$CONJURE_HOME" DRY_RUN="$dryrun" CONJURE_SUBMIT="$do_submit" \
    bash "$CONJURE_HOME/scripts/publish-plugin.sh"
}
```

Note: cmd_publish does NOT source lib/mutate.sh itself — the script does. It sets DRY_RUN and CONJURE_SUBMIT as env vars passed to the script, following the cmd_audit precedent (not cmd_init which sources mutate.sh directly).

CHANGE 3 — Add dispatch table entry. In the case block starting around line 272, insert before `version|-v|--version`:
```
  publish)         shift; cmd_publish "$@"         ;;
```

After changes, verify the dispatch table still handles all existing commands correctly: init, migrate, audit, update, refresh-graph, install-mcp, preflight, version, help, and the new publish.</action>
  <verify>
    <automated>shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 cli/conjure && grep -q 'cmd_publish' cli/conjure && grep -q 'publish)' cli/conjure</automated>
  </verify>
  <done>cli/conjure passes shellcheck; cmd_publish function present; dispatch table contains `publish)` case; `conjure publish --help` prints usage without error</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli/conjure → scripts/publish-plugin.sh | CLI passes DRY_RUN + CONJURE_SUBMIT as env; script validates all inputs |
| scripts/publish-plugin.sh → .claude-plugin/ | All writes through mutate_write; PLUGIN_DIR is hardcoded |
| git rev-parse HEAD → sha field | SHA is a 40-char hex string; passed via jq --arg (not interpolated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | Shell injection via SHA/version strings | mitigate | Use `jq --arg sha` and `jq --arg ver` — jq escapes values; never string-interpolate into JSON |
| T-10-04 | Tampering | Write outside .claude-plugin/ | mitigate | PLUGIN_DIR hardcoded as `"$CONJURE_HOME/.claude-plugin"` — user cannot supply path via CLI |
| T-10-05 | Information Disclosure | submit-entry.json contents | accept | Contains only public fields: name, description, source (repo+SHA), homepage, category. No credentials or private data. |
| T-10-06 | Denial of Service | Dirty tree abort bypassed | accept | Guard uses both `git diff --quiet` and `git diff --cached --quiet` — catches both staged and unstaged changes. exit 2 is hard stop. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. |
</threat_model>

<verification>
```bash
# Dry-run smoke test — must print mutations without writing
DRY_RUN=1 bash scripts/publish-plugin.sh 2>&1 | grep -q 'dry-run'

# CLI dispatch smoke test
cli/conjure publish --help

# shellcheck clean
shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/publish-plugin.sh cli/conjure

# Dispatch table integrity
grep -E '^\s+(init|migrate|audit|update|refresh-graph|install-mcp|preflight|publish|version|help)' cli/conjure
```
</verification>

<success_criteria>
- scripts/publish-plugin.sh exists, is executable, passes shellcheck with 0 errors
- `DRY_RUN=1 bash scripts/publish-plugin.sh` prints "[dry-run] would write" lines without modifying files
- `cli/conjure publish --help` exits 0 and prints usage
- Dispatch table in cli/conjure contains `publish)` before the wildcard `*)` case
- `CONJURE_HOME` and `DRY_RUN` are passed as env vars (not exported globally) to the script invocation
- All jq mutations use `--arg` for variable interpolation (no string concatenation into JSON)
- mutate_summary is called as the final statement in publish-plugin.sh
</success_criteria>

<output>
Create `.planning/phases/10-marketplace-publish/10-02-SUMMARY.md` when done.
</output>
