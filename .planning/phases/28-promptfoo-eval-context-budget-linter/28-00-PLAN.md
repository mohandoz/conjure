---
phase: 28-promptfoo-eval-context-budget-linter
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/_eval/harness/CLAUDE.md
  - tests/fixtures/_eval/harness/.claude/skills/audit-helper/SKILL.md
  - tests/fixtures/_eval/harness/.claude/skills/code-review/SKILL.md
  - tests/fixtures/_eval/expected-promptfooconfig.yaml
  - tests/fixtures/_eval-coverage-gap/harness/CLAUDE.md
  - tests/fixtures/_eval-coverage-gap/harness/.claude/skills/audit-helper/SKILL.md
  - tests/fixtures/_eval-coverage-gap/harness/.claude/skills/code-review/SKILL.md
  - tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml
  - tests/fixtures/_eval-overbudget/harness/CLAUDE.md
  - tests/fixtures/_eval-overbudget/harness/.claude/skills/bigskill/SKILL.md
  - tests/fixtures/_eval-probe/README.md
  - tests/run.sh
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05

must_haves:
  truths:
    - "tests/fixtures/_eval/harness/ exists with CLAUDE.md (containing rule lines) and 2 installed skills (audit-helper, code-review)"
    - "tests/fixtures/_eval/expected-promptfooconfig.yaml is a valid golden config: provider id anthropic:claude-agent-sdk with working_dir:. and setting_sources:[project], evaluateOptions.repeat:3, one skill-used assertion per skill, one llm-rubric per CLAUDE.md rule line"
    - "tests/fixtures/_eval-coverage-gap/ contains a harness with 2 installed skills but a promptfooconfig.yaml asserting only 1 skill — leaving code-review without a skill-used assertion (EVAL-05 gap fixture)"
    - "tests/fixtures/_eval-overbudget/ contains a CLAUDE.md + SKILL.md totaling >=25000 tokens equivalent (>=100000 chars) to trigger the err() threshold (EVAL-04 over-budget fixture)"
    - "tests/fixtures/_eval-probe/README.md documents the A1/A2/A3 resolution: probe gated on node/promptfoo availability; A2/A3 verified via YAML structure only; A1 behavioral result deferred to manual verification"
    - "tests/run.sh contains a Phase 28 graceful-red EVAL block (▸ Phase 28 — promptfoo Eval + Context-Budget Linter (EVAL-01..05)) with all EVAL-* test stubs failing because scripts/eval.sh is absent"
  artifacts:
    - path: "tests/fixtures/_eval/expected-promptfooconfig.yaml"
      provides: "Golden promptfooconfig.yaml for EVAL-01 output comparison"
      contains: "anthropic:claude-agent-sdk"
    - path: "tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml"
      provides: "Pre-seeded config missing code-review skill-used assertion (EVAL-05 negative fixture)"
      contains: "skill-used"
    - path: "tests/fixtures/_eval-overbudget/harness/CLAUDE.md"
      provides: "CLAUDE.md exceeding 25k-token equivalent to trigger err() in --budget"
    - path: "tests/run.sh"
      provides: "Phase 28 graceful-red EVAL test block"
      contains: "Phase 28 — promptfoo Eval"
  key_links:
    - from: "tests/run.sh Phase 28 block"
      to: "scripts/eval.sh"
      via: "P28_EVAL_OK presence guard"
      pattern: "P28_EVAL_OK=.*\\[ -f.*eval.sh"
    - from: "tests/run.sh EVAL-04 test"
      to: "scripts/audit-setup.sh"
      via: "P28_AUDIT_OK presence guard"
      pattern: "P28_AUDIT_OK=.*\\[ -f.*audit-setup.sh"
    - from: "tests/fixtures/_eval/expected-promptfooconfig.yaml"
      to: "anthropic:claude-agent-sdk provider"
      via: "golden comparison in EVAL-01 test"
      pattern: "anthropic:claude-agent-sdk"
---

<objective>
Wave 0: Build the test infrastructure for Phase 28 — eval fixtures, golden config, and graceful-red EVAL test block — BEFORE any feature code lands.

Purpose: Nyquist Wave 0 invariant. All fixtures, the golden promptfooconfig.yaml, and the
EVAL test block in tests/run.sh must exist before scripts/eval.sh or audit-setup.sh EVAL
extensions are written. When Wave 0 completes, `bash tests/run.sh` exits non-zero with FAIL
on all EVAL-01..05 test cases (graceful-red). EVAL-PROBE (A1/A2/A3 integration probe) is the
exception — it passes as a documentation test (no live promptfoo invocation required).

SC reconciliation note (baked into golden fixture): ROADMAP SC-28-1 says "repeat:3, minPassCount:2
for all llm-rubric assertions". RESEARCH confirms these are NOT per-assertion YAML keys — they
cause parse errors if placed on individual assertions. Correct realization:
- `evaluateOptions.repeat: 3` at the TOP LEVEL of promptfooconfig.yaml (config-level flakiness guard)
- `repeat: 3` and `repeat-min-pass: 2` as inputs to promptfoo/promptfoo-action in the emitted workflow

The golden fixture (expected-promptfooconfig.yaml) encodes this correct realization. Verification
must accept `evaluateOptions.repeat: 3` (top-level) as satisfying the SC intent — NOT require
per-assertion repeat keys which do not exist in the promptfoo spec.

Provider note (D-28-CONTEXT override): CONTEXT.md preferred exec/claude -p but delegated provider
to research. RESEARCH determined `anthropic:claude-agent-sdk` is the ONLY provider that can supply
`response.metadata.skillCalls` for the `skill-used` assertion type. The golden fixture encodes
`id: anthropic:claude-agent-sdk` per RESEARCH. This is not a contradiction — CONTEXT explicitly
deferred the provider choice to research.

Output: 7 fixture directories under tests/fixtures/, golden expected-promptfooconfig.yaml,
A1/A2/A3 probe README, and Phase 28 graceful-red EVAL block in tests/run.sh.
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-promptfoo-eval-context-budget-linter/28-CONTEXT.md
@.planning/phases/28-promptfoo-eval-context-budget-linter/28-RESEARCH.md
@.planning/phases/28-promptfoo-eval-context-budget-linter/28-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create eval fixture harnesses and golden promptfooconfig.yaml</name>
  <files>
    tests/fixtures/_eval/harness/CLAUDE.md
    tests/fixtures/_eval/harness/.claude/skills/audit-helper/SKILL.md
    tests/fixtures/_eval/harness/.claude/skills/code-review/SKILL.md
    tests/fixtures/_eval/expected-promptfooconfig.yaml
    tests/fixtures/_eval-coverage-gap/harness/CLAUDE.md
    tests/fixtures/_eval-coverage-gap/harness/.claude/skills/audit-helper/SKILL.md
    tests/fixtures/_eval-coverage-gap/harness/.claude/skills/code-review/SKILL.md
    tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml
    tests/fixtures/_eval-overbudget/harness/CLAUDE.md
    tests/fixtures/_eval-overbudget/harness/.claude/skills/bigskill/SKILL.md
    tests/fixtures/_eval-probe/README.md
  </files>
  <read_first>
    - .planning/phases/28-promptfoo-eval-context-budget-linter/28-RESEARCH.md (Pattern 1: full provider block with working_dir/setting_sources/skills/permission_mode/append_allowed_tools; Pattern 2: skill-used assertion shape; Pattern 3: llm-rubric assertion shape + evaluateOptions.repeat location; Pattern 4: Full promptfooconfig.yaml template; Section "Open Questions (RESOLVED)" — A1/A2/A3 resolutions)
    - tests/fixtures/_schema-audit/valid/harness/.claude/skills/ok-skill/SKILL.md (SKILL.md frontmatter structure to mirror)
    - tests/fixtures/_schema-audit/valid/harness/CLAUDE.md (CLAUDE.md header structure to mirror)
  </read_first>
  <action>
Create the three fixture trees and the golden config. All fixture directories are _-prefixed so generic audit/golden loops exclude them.

TREE 1 — tests/fixtures/_eval/harness/ (eval-init happy-path: 2 installed skills + CLAUDE.md with rule lines):

harness/CLAUDE.md — a CLAUDE.md with at least 3 extractable rule lines (non-heading, non-blank, non-table, non-code-fence). Use imperative list items starting with `-`. Include a ## Project section header followed by rule lines such as:
  - Always run shellcheck before committing bash scripts.
  - Never use exit 1 in hooks; use exit 2.
  - Backup before mutate: all file mutations route through lib/mutate.sh.

harness/.claude/skills/audit-helper/SKILL.md — minimal valid SKILL.md frontmatter (name: audit-helper, description: Helps run conjure audit checks, allowed-tools: ["Bash"]) with a one-line body after the closing ---.

harness/.claude/skills/code-review/SKILL.md — minimal valid SKILL.md frontmatter (name: code-review, description: Reviews code for style and correctness, allowed-tools: ["Read", "Bash"]) with a one-line body.

tests/fixtures/_eval/expected-promptfooconfig.yaml — the golden promptfooconfig.yaml that `conjure eval init` on this fixture MUST produce (EVAL-01 output reference). Shape per RESEARCH Pattern 4:
  - Header comment: "# Generated by conjure eval init — do not edit manually"
  - description: 'Conjure harness prompt-adherence eval'
  - providers block with `id: anthropic:claude-agent-sdk` and config: {working_dir: ., setting_sources: ['project'], skills: all, permission_mode: dontAsk, append_allowed_tools: ['Read', 'Bash']}
  - evaluateOptions: {repeat: 3}  (TOP-LEVEL — NOT on assertions; this is the SC reconciliation: repeat:3 at evaluateOptions, not per assertion)
  - prompts: ['Summarize the purpose of this project in one sentence.']
  - defaultTest: {options: {disableVarExpansion: true}}
  - tests: section containing TWO skill-used blocks (one for audit-helper, one for code-review) and THREE llm-rubric blocks (one per rule line extracted from harness/CLAUDE.md). Each llm-rubric has type: llm-rubric, value: "<exact rule text>", threshold: 0.8. No repeat key on individual assertions.
  Note: the exact rule text in llm-rubric values must match the exact lines from harness/CLAUDE.md that pass the rule-line extraction filter (non-heading, non-blank, non-table, non-code-fence lines).

TREE 2 — tests/fixtures/_eval-coverage-gap/harness/ (EVAL-05 negative fixture: skill added after init):

harness/CLAUDE.md — same structure as _eval/harness/CLAUDE.md (2-3 rule lines).
harness/.claude/skills/audit-helper/SKILL.md — same as above.
harness/.claude/skills/code-review/SKILL.md — same as above.
harness/.conjure/eval/promptfooconfig.yaml — a PRE-SEEDED config that only has a skill-used assertion for audit-helper (NOT for code-review). This simulates the state where code-review was added AFTER eval init. The config is otherwise valid and contains the anthropic:claude-agent-sdk provider block and evaluateOptions.repeat:3. The EVAL-05 check must detect that code-review is an installed skill without a skill-used assertion.

TREE 3 — tests/fixtures/_eval-overbudget/harness/ (EVAL-04 over-budget: >=25000 tokens via chars/4):

harness/CLAUDE.md — a large CLAUDE.md containing enough text to push the total (CLAUDE.md + SKILL.md) chars above 100000 (100000 chars / 4 = 25000 tokens). Achieve this by writing a CLAUDE.md with a very long body (approximately 80000 chars of repeated rule text padded with `#` comment lines that pass the non-rule filter — use lines starting with `#` which ARE filtered, but then add enough actual rule lines with `-` prefix to accumulate char count). Alternative: use repeated `## Section N` headings (filtered) plus a padding section. The goal is total chars across CLAUDE.md + bigskill/SKILL.md >= 100001 chars. Verify: `wc -c harness/CLAUDE.md harness/.claude/skills/bigskill/SKILL.md | tail -1` shows total >= 100001.
harness/.claude/skills/bigskill/SKILL.md — minimal SKILL.md (name: bigskill, description: Large skill for budget test, allowed-tools: ["Read"]).

tests/fixtures/_eval-probe/README.md — documentation of the A1/A2/A3 Open Question resolutions:
  - A1 (auto-install of @anthropic-ai/claude-agent-sdk): treated as [ASSUMED]; gated integration probe would need a live network + node + promptfoo environment (ANTHROPIC_API_KEY required for SDK interaction); the Wave 0 probe fixture is documentation-only; if auto-install fails at runtime, eval.sh must surface a human-readable error message and exit 2. The file notes the probe is SKIPPED in automated CI (no live promptfoo invocation in tests/run.sh).
  - A2 (working_dir: .): verified by YAML structure only — the golden config encodes working_dir: . as expected; live correctness is Manual-Only (see VALIDATION.md).
  - A3 (setting_sources: ['project']): same as A2 — YAML structure verified by golden fixture; live skill discovery behavior is Manual-Only.

Important YAML generation notes for the golden file:
- Write YAML with printf, not heredoc with variable expansion — avoid YAML injection (Pitfall 3).
- Single-quote the YAML scalars for skill names and rule values to avoid colon/special-char issues.
- The `setting_sources` value in the provider config MUST be a YAML sequence: `['project']` (inline sequence).
  </action>
  <verify>
    <automated>
      test -f tests/fixtures/_eval/harness/CLAUDE.md &amp;&amp;
      test -f tests/fixtures/_eval/harness/.claude/skills/audit-helper/SKILL.md &amp;&amp;
      test -f tests/fixtures/_eval/harness/.claude/skills/code-review/SKILL.md &amp;&amp;
      test -f tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -q "anthropic:claude-agent-sdk" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -q "evaluateOptions" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -q "repeat: 3" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -q "skill-used" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -q "llm-rubric" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -q "working_dir" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -q "setting_sources" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      grep -qv "minPassCount" tests/fixtures/_eval/expected-promptfooconfig.yaml &amp;&amp;
      test -f tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml &amp;&amp;
      grep -q "audit-helper" tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml &amp;&amp;
      grep -qv "code-review" tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml &amp;&amp;
      test -f tests/fixtures/_eval-overbudget/harness/CLAUDE.md &amp;&amp;
      test -f tests/fixtures/_eval-overbudget/harness/.claude/skills/bigskill/SKILL.md &amp;&amp;
      TOTAL_CHARS=$(( $(wc -c &lt; tests/fixtures/_eval-overbudget/harness/CLAUDE.md | tr -d ' ') + $(wc -c &lt; tests/fixtures/_eval-overbudget/harness/.claude/skills/bigskill/SKILL.md | tr -d ' ') )) &amp;&amp; [ "$TOTAL_CHARS" -gt 100000 ] &amp;&amp;
      test -f tests/fixtures/_eval-probe/README.md &amp;&amp;
      echo "Wave 0 fixtures: all present, golden config valid, overbudget fixture sized correctly"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/_eval/expected-promptfooconfig.yaml contains `id: anthropic:claude-agent-sdk` (RESEARCH-determined provider — NOT exec/claude -p)
    - `grep -q "evaluateOptions" tests/fixtures/_eval/expected-promptfooconfig.yaml` passes — repeat:3 is at the TOP LEVEL under evaluateOptions, NOT on individual assertions (SC reconciliation: ROADMAP intent satisfied at the correct YAML location)
    - `grep -qv "minPassCount" tests/fixtures/_eval/expected-promptfooconfig.yaml` passes — minPassCount does NOT appear in promptfooconfig.yaml (it is a GitHub Action input, not a YAML key)
    - `grep -c "type: skill-used" tests/fixtures/_eval/expected-promptfooconfig.yaml` returns 2 (one per installed skill: audit-helper, code-review)
    - `grep -c "type: llm-rubric" tests/fixtures/_eval/expected-promptfooconfig.yaml` returns at least 3 (one per rule line in _eval/harness/CLAUDE.md)
    - tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml contains audit-helper skill-used assertion but NOT code-review (simulates post-init skill addition)
    - CLAUDE.md + bigskill/SKILL.md total chars >= 100001: `awk 'BEGIN{s=0} {s+=length($0)+1} END{print s+0}' tests/fixtures/_eval-overbudget/harness/CLAUDE.md tests/fixtures/_eval-overbudget/harness/.claude/skills/bigskill/SKILL.md` is >= 100001 or `wc -c` sum > 100000
    - tests/fixtures/_eval-probe/README.md documents A1 (auto-install assumed/manual), A2 (yaml-structure only), A3 (yaml-structure only)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Phase 28 graceful-red EVAL test block to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh (read lines 5195-5209: the end section with GH_HIDE_STUBS cleanup + summary block; insertion point is IMMEDIATELY BEFORE "# ─────────────────────────── Clean up any gh-hiding stub dirs" comment at line ~5199)
    - tests/run.sh (read lines ~5000-5050: Phase 27 block header style and presence-guard pattern to mirror exactly — P27_AUDIT_OK, P27_CHECK_OK patterns; use P28_EVAL_OK and P28_AUDIT_OK)
    - .planning/phases/28-promptfoo-eval-context-budget-linter/28-VALIDATION.md (Per-Task Verification Map: full list of EVAL-* test IDs: EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05)
    - .planning/phases/28-promptfoo-eval-context-budget-linter/28-RESEARCH.md (Pattern 7: --budget linter; Pattern 8: EVAL-05 coverage diff using awk two-line lookahead)
  </read_first>
  <action>
Append a Phase 28 test block to tests/run.sh IMMEDIATELY BEFORE the "# Clean up any gh-hiding stub dirs" / GH_HIDE_STUBS cleanup loop. Do NOT alter the summary block or the final `[ "$FAIL" -eq 0 ]` gate.

SECTION HEADER (match Phase 27 width exactly):
  # ──────────────────────────────────────────────────────────────────────────────
  # Phase 28 — promptfoo Eval + Context-Budget Linter (EVAL-01..05)
  # ──────────────────────────────────────────────────────────────────────────────

PRESENCE GUARDS:
  P28_EVAL_SH="$CONJURE_HOME/scripts/eval.sh"
  P28_AUDIT_SH="$CONJURE_HOME/scripts/audit-setup.sh"
  P28_EVAL_OK=0
  P28_AUDIT_OK=0
  [ -f "$P28_EVAL_SH" ] && P28_EVAL_OK=1
  [ -f "$P28_AUDIT_SH" ] && P28_AUDIT_OK=1

Echo header:
  echo
  echo "▸ Phase 28 — promptfoo Eval + Context-Budget Linter (EVAL-01..05)"

Implement these test cases (each using its own mktemp -d sandbox with trap 'rm -rf "$P28_xxx_DIR"' EXIT, and trap - EXIT after cleanup, per Phase 25/26/27 pattern). Guard each eval.sh invocation behind P28_EVAL_OK; audit invocations behind P28_AUDIT_OK.

EVAL-01-init: `conjure eval init` on the _eval/harness fixture produces a promptfooconfig.yaml matching the golden:
  - mktemp -d sandbox; cp -R tests/fixtures/_eval/harness/. into sandbox; git init + commit (so harness is git-tracked); run CONJURE_HOME="$CONJURE_HOME" bash "$P28_EVAL_SH" init "$P28_INIT_DIR"; check that .conjure/eval/promptfooconfig.yaml exists; check it contains "anthropic:claude-agent-sdk"; check it contains "evaluateOptions" (repeat:3 at top level); check it contains "skill-used" twice; check it does NOT contain "minPassCount" (per SC reconciliation: minPassCount is a GitHub Action input not a YAML key); guard with P28_EVAL_OK.
  - pass/fail "eval init creates promptfooconfig.yaml with correct provider and assertions (EVAL-01-init)"

EVAL-01-skills: Generated config has one skill-used assertion per installed skill:
  - Reuse the init sandbox output from EVAL-01-init (or create fresh); grep -c "type: skill-used" .conjure/eval/promptfooconfig.yaml; assert count == 2 (audit-helper + code-review); guard with P28_EVAL_OK.
  - pass/fail "eval init: one skill-used assertion per installed skill (EVAL-01-skills)"

EVAL-01-rubrics: Generated config has at least one llm-rubric assertion:
  - Reuse the init sandbox output; grep -c "type: llm-rubric"; assert count >= 1; guard with P28_EVAL_OK.
  - pass/fail "eval init: llm-rubric assertions present for CLAUDE.md rule lines (EVAL-01-rubrics)"

EVAL-01-noskills: `conjure eval init` on a harness with NO installed skills produces a config with 0 skill-used assertions (no crash):
  - mktemp -d sandbox; write only CLAUDE.md (no .claude/skills); git init + commit; run eval init; assert exit 0 and .conjure/eval/promptfooconfig.yaml exists; assert grep -c "type: skill-used" == 0; guard with P28_EVAL_OK.
  - pass/fail "eval init: no skill-used assertions when no skills installed (EVAL-01-noskills)"

EVAL-02-node-absent: When node is NOT on PATH, `conjure eval run` exits 2 with human-readable message:
  - Use PATH_WITHOUT_NODE (derived by stripping node from PATH using the same pattern as mk_path_without_gh); PATH="$PATH_WITHOUT_NODE" CONJURE_HOME="$CONJURE_HOME" bash "$P28_EVAL_SH" run /dev/null; assert exit code 2; assert stderr contains "Node" or "node"; guard with P28_EVAL_OK.
  - Alternative: create a temp dir stub with a node script that prints "v19.0.0" (old version) to test version-too-old path; assert exit 2 and stderr contains "20.20".
  - pass/fail "eval run: exits 2 with human-readable message when node is absent or too old (EVAL-02-node-absent)"

EVAL-02-audit-decoupled: `conjure audit` with promptfoo absent exits 0 (no EVAL coupling):
  - Run CONJURE_HOME="$CONJURE_HOME" bash "$P28_AUDIT_SH" /tmp with no promptfoo installed; assert exit code 0 or 1 (never 2 due to promptfoo absence); guard with P28_AUDIT_OK. Note: audit may exit 1 (warnings) or 0 (pass) but MUST exit 0 NOT exit 2 when promptfoo is simply absent from PATH — there is no promptfoo invocation in the audit path.
  - pass/fail "audit with promptfoo absent exits 0 (not exit 2) — promptfoo fully decoupled from audit (EVAL-02-audit-decoupled)"

EVAL-03-emit: `conjure eval --emit-workflow` creates .github/workflows/conjure-eval.yml:
  - mktemp -d sandbox; git init + commit; run CONJURE_HOME="$CONJURE_HOME" bash "$P28_EVAL_SH" --emit-workflow "$P28_WF_DIR"; assert .github/workflows/conjure-eval.yml exists; assert it contains "pull_request"; assert it contains "promptfoo/promptfoo-action"; assert it contains "fail-on-threshold: 80" (integer 80, NOT 0.8 — per Pitfall 9 reconciliation); assert it contains "repeat: 3"; assert it contains "repeat-min-pass: 2"; assert it contains "0.121.14" (pinned version); assert it contains ".claude/**" (path filter); guard with P28_EVAL_OK.
  - pass/fail "eval --emit-workflow creates conjure-eval.yml with correct shape (EVAL-03-emit)"

EVAL-04-budget-ok: `conjure audit --budget` on a normal harness exits 0 and prints token count:
  - mktemp -d sandbox with the _eval/harness fixture (small CLAUDE.md + 2 small skills = well under 15k tokens); run CONJURE_HOME="$CONJURE_HOME" CONJURE_BUDGET=1 bash "$P28_AUDIT_SH" "$P28_BUDOK_DIR"; assert exit code 0 or 1 (not 2); assert output contains "tokens"; guard with P28_AUDIT_OK.
  - pass/fail "audit --budget on normal harness exits 0 or 1 (under threshold) (EVAL-04-budget-ok)"

EVAL-04-budget-err: `conjure audit --budget` on the _eval-overbudget fixture exits 2 (>=25k tokens):
  - mktemp -d sandbox copying _eval-overbudget/harness/.; git init + commit; run CONJURE_HOME="$CONJURE_HOME" CONJURE_BUDGET=1 bash "$P28_AUDIT_SH" "$P28_BUDERR_DIR"; assert exit code 2; guard with P28_AUDIT_OK.
  - pass/fail "audit --budget exits 2 when estimated tokens >=25000 (EVAL-04-budget-err)"

EVAL-04-porcelain: `conjure audit --budget --porcelain` emits JSON with correct shape:
  - mktemp -d sandbox with _eval/harness fixture; run CONJURE_HOME="$CONJURE_HOME" CONJURE_BUDGET=1 CONJURE_PORCELAIN=1 bash "$P28_AUDIT_SH" "$P28_PORCDIR" 2>/dev/null; capture stdout; assert stdout passes `jq -e '.total_tokens'`; assert `jq -e '(.contributors | type) == "array"'`; assert `jq -e 'has("over")'`; guard with P28_AUDIT_OK.
  - pass/fail "audit --budget --porcelain emits valid JSON with total_tokens, threshold, over, contributors[] (EVAL-04-porcelain)"

EVAL-05-gap: `conjure audit` on the _eval-coverage-gap fixture reports code-review as uncovered:
  - mktemp -d sandbox copying _eval-coverage-gap/harness/.; git init + commit; run CONJURE_HOME="$CONJURE_HOME" bash "$P28_AUDIT_SH" "$P28_GAPDIR"; assert exit code 0 or 1 (note() is advisory, never exit 2); assert output contains "code-review" (the gap skill); guard with P28_AUDIT_OK.
  - pass/fail "audit reports skill with no skill-used assertion as coverage gap (EVAL-05-gap)"

EVAL-05-noconfig: `conjure audit` on a harness with no eval config emits note() but exits 0:
  - mktemp -d sandbox with CLAUDE.md + 1 skill but NO .conjure/eval/ dir; git init + commit; run CONJURE_HOME="$CONJURE_HOME" bash "$P28_AUDIT_SH" "$P28_NOCFGDIR"; assert exit code 0 or 1 (never exit 2 from the note); assert output contains "eval config" or "conjure eval init"; guard with P28_AUDIT_OK.
  - pass/fail "audit with no eval config emits note() advisory and exits 0 (EVAL-05-noconfig)"

All test cases:
  - Guard eval.sh invocations behind P28_EVAL_OK (else fail "scripts/eval.sh not implemented — Wave 1 must create it (EVAL-XX-tag)").
  - Guard audit invocations for EVAL-04/05 behind P28_AUDIT_OK (else fail "audit-setup.sh EVAL section not implemented — Wave 3 must add it (EVAL-XX-tag)").
  - Use `pass`/`fail` helpers already in tests/run.sh (do NOT redefine).
  - POSIX 3.2+: no associative arrays, no mapfile, no local -n.
  - Use mktemp -d per test with trap 'rm -rf "$P28_xxx_DIR"' EXIT and trap - EXIT after cleanup.
  - Insert IMMEDIATELY BEFORE the "for _s in $GH_HIDE_STUBS" cleanup loop.
  - Note: EVAL-02-audit-decoupled may use a real /tmp sandbox (audit-setup.sh does not need promptfoo); the test simply confirms audit exits 0 and not 2 when promptfoo is absent from PATH (which it normally is in the test environment unless installed globally).
  </action>
  <verify>
    <automated>
      grep -c "Phase 28" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "P28_EVAL_OK" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "EVAL-01-init" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "EVAL-03-emit" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "EVAL-04-porcelain" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "EVAL-05-gap" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep -E "FAIL.*(EVAL-0)" | grep -c "FAIL" | grep -qE "^[1-9]" &amp;&amp;
      echo "Phase 28 block present, shellcheck clean, graceful-red on EVAL-01..05"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/run.sh contains "Phase 28 — promptfoo Eval + Context-Budget Linter (EVAL-01..05)" section header
    - All 12 EVAL-* test tags are present: EVAL-01-init, EVAL-01-skills, EVAL-01-rubrics, EVAL-01-noskills, EVAL-02-node-absent, EVAL-02-audit-decoupled, EVAL-03-emit, EVAL-04-budget-ok, EVAL-04-budget-err, EVAL-04-porcelain, EVAL-05-gap, EVAL-05-noconfig
    - `bash tests/run.sh 2>&1 | grep -E "FAIL.*(EVAL)" | grep -c FAIL` exits 0 with count >= 10 (graceful-red — eval.sh is absent so all EVAL-01/02/03 tests fail, audit EVAL-04/05 extensions absent so those fail)
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` passes (no new errors)
    - The existing summary block and `[ "$FAIL" -eq 0 ]` gate are unmodified
    - All Phase 27 SCHM-* tests are unmodified (no regression)
    - EVAL-02-audit-decoupled test correctly exits without calling eval.sh (it tests audit-setup.sh only)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/fixtures/_eval/harness/CLAUDE.md → eval init | Rule-line extraction reads user-owned CLAUDE.md; fixture content is team-authored (no user input at test time) |
| tests/fixtures/_eval/expected-promptfooconfig.yaml → EVAL-01 test | Golden file compared against generated output; mismatch = test failure |
| tests/fixtures/_eval-overbudget → --budget fixture | Deliberately large file; contained under _-prefixed dir excluded from generic audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-W0-01 | Tampering | Golden expected-promptfooconfig.yaml (must encode correct provider + evaluateOptions location) | mitigate | Task 1 acceptance_criteria asserts: `grep -q "anthropic:claude-agent-sdk"` AND `grep -q "evaluateOptions"` AND `grep -qv "minPassCount"` — any wrong encoding fails the Wave 0 verify step before any Wave 1 implementation runs |
| T-28-W0-02 | Denial of Service | Graceful-red EVAL block silently passing when eval.sh is absent | mitigate | Each EVAL-01/02/03 test is gated on P28_EVAL_OK; when absent, calls `fail "... not implemented"` — never silently passes |
| T-28-W0-03 | Tampering | _eval-coverage-gap config accidentally asserting code-review (defeats the fixture intent) | mitigate | Task 1 acceptance_criteria asserts `grep -qv "code-review"` on the gap fixture config |
| T-28-W0-04 | Information Disclosure | _eval-probe/README.md documenting A1 (API key requirement for live probe) | accept | README documents that live probe requires ANTHROPIC_API_KEY; no credential values are stored in the fixture; it is documentation only |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — bash + fixtures only; promptfoo is never invoked in Wave 0 |
</threat_model>

<verification>
After both tasks complete:
- `grep -q "anthropic:claude-agent-sdk" tests/fixtures/_eval/expected-promptfooconfig.yaml` exits 0
- `grep -q "evaluateOptions" tests/fixtures/_eval/expected-promptfooconfig.yaml` exits 0 (repeat at top level, per SC reconciliation)
- `grep -qv "minPassCount" tests/fixtures/_eval/expected-promptfooconfig.yaml` exits 0 (not a YAML key)
- `grep -qv "code-review" tests/fixtures/_eval-coverage-gap/harness/.conjure/eval/promptfooconfig.yaml` exits 0
- `bash tests/run.sh 2>&1 | grep -E "FAIL.*EVAL" | grep -c FAIL` shows >= 10 FAIL (graceful-red)
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` exits 0
</verification>

<success_criteria>
- Wave 0 complete: all fixture trees + golden + _eval-probe README + Phase 28 graceful-red EVAL block exist
- `bash tests/run.sh` exits non-zero (FAIL count > 0) because scripts/eval.sh and audit-setup.sh EVAL extensions are absent
- Wave 1 (28-01) can now implement scripts/eval.sh + cmd_eval to turn EVAL-01/02 tests green
</success_criteria>

<output>
Create .planning/phases/28-promptfoo-eval-context-budget-linter/28-00-SUMMARY.md when done.
</output>
