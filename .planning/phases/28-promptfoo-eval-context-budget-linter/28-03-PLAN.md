---
phase: 28-promptfoo-eval-context-budget-linter
plan: "03"
type: execute
wave: 3
depends_on:
  - 28-00
files_modified:
  - scripts/audit-setup.sh
  - cli/conjure
autonomous: true
requirements:
  - EVAL-04
  - EVAL-05

must_haves:
  truths:
    - "`conjure audit --budget` measures estimated tokens for CLAUDE.md + always-loaded SKILL.md indexes using chars/4 heuristic; flags >=15k (warn) and >=25k (err, exit 2); lists top-5 contributors by token count (EVAL-04)"
    - "`conjure audit --budget --porcelain` emits JSON: {total_tokens, threshold, over, contributors:[{path, tokens}]} — parsed successfully by `jq .` (EVAL-04)"
    - "BUDGET_TMP tempfile joins the EXISTING _audit_cleanup function (no second EXIT trap registered — Phase 27 WR-03 lesson)"
    - "`conjure audit` reports installed skills with no skill-used assertion in .conjure/eval/promptfooconfig.yaml as note() advisory (exit 0); a skill added after eval init appears in the gap report (EVAL-05)"
    - "When .conjure/eval/promptfooconfig.yaml is absent, audit emits note() 'no eval config — run conjure eval init' and exits 0 (EVAL-05)"
    - "`conjure audit` (without --budget) is NOT affected — existing checks pass as before; promptfoo is NEVER invoked from the audit path"
    - "cli/conjure cmd_audit gains --budget and --porcelain flags forwarded to audit-setup.sh via CONJURE_BUDGET and CONJURE_PORCELAIN env vars"
  artifacts:
    - path: "scripts/audit-setup.sh"
      provides: "--budget linter block (EVAL-04) + EVAL-05 coverage diff block"
      contains: "CONJURE_BUDGET"
    - path: "cli/conjure"
      provides: "--budget and --porcelain flags in cmd_audit"
      contains: "CONJURE_BUDGET"
  key_links:
    - from: "scripts/audit-setup.sh _audit_cleanup"
      to: "BUDGET_TMP tempfile"
      via: "_audit_cleanup rm -f extension"
      pattern: "_audit_cleanup.*BUDGET_TMP"
    - from: "scripts/audit-setup.sh EVAL-05 block"
      to: "_eval_extract_skill_used awk two-line lookahead"
      via: "parse .conjure/eval/promptfooconfig.yaml"
      pattern: "type: skill-used"
    - from: "cli/conjure cmd_audit --budget"
      to: "scripts/audit-setup.sh CONJURE_BUDGET=1"
      via: "env var forwarding"
      pattern: "CONJURE_BUDGET"
---

<objective>
Wave 3: Implement `conjure audit --budget` (EVAL-04) and EVAL-05 coverage gap report in
scripts/audit-setup.sh; wire --budget and --porcelain flags in cli/conjure.

Purpose: EVAL-04 — static context budget linter measuring always-loaded file token load via the
existing chars/4 heuristic; extends the existing token estimate check (audit-setup.sh ~233-239)
with per-file breakdown, threshold flags, top-5 contributors, and --porcelain JSON output.
EVAL-05 — diff installed skills vs skill-used assertions in the eval config; report uncovered
skills as advisory note() with exit 0.

Critical constraints:
- BUDGET_TMP MUST join `_audit_cleanup` (line ~29) — NOT register a new EXIT trap.
  Phase 27 WR-03 lesson: bash has one EXIT trap slot; a second trap call clobbers the first.
- --porcelain is a separate flag from --json (existing SCHM-05); CONJURE_PORCELAIN is a new env var.
  Check whether CONJURE_PORCELAIN already exists from Phase 27 before adding — if it already exists,
  reuse it; if not, add it. The --budget --porcelain combination emits budget JSON to stdout.
- EVAL-05 uses grep/awk to parse the generated promptfooconfig.yaml (dependency-free — no yq).
  The awk two-line lookahead pattern from RESEARCH Pattern 8 handles the "type: skill-used" →
  "value: <skill-name>" extraction from Conjure's controlled YAML format.
- `conjure audit` (base) and `conjure audit --json` (SCHM-05) are NOT affected. The --budget
  block is gated on CONJURE_BUDGET=1; EVAL-05 always runs (advisory, exit 0 only).

Wave 3 is independent of Wave 2 (28-02) — both can be executed while 28-02 is in-flight, but
Wave 3 MUST depend on Wave 0 (28-00) fixtures for the test cases to exist.

Output: scripts/audit-setup.sh extended with --budget block (EVAL-04) + EVAL-05 coverage block;
cli/conjure cmd_audit updated with --budget and --porcelain flags.
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-promptfoo-eval-context-budget-linter/28-CONTEXT.md
@.planning/phases/28-promptfoo-eval-context-budget-linter/28-RESEARCH.md
@.planning/phases/28-promptfoo-eval-context-budget-linter/28-VALIDATION.md
@.planning/phases/28-promptfoo-eval-context-budget-linter/28-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --budget linter (EVAL-04) + EVAL-05 coverage gap to scripts/audit-setup.sh</name>
  <files>scripts/audit-setup.sh</files>
  <read_first>
    - scripts/audit-setup.sh (read lines 1-35: _audit_cleanup function definition at ~line 29 — MUST extend this function to include BUDGET_TMP; do NOT register a second trap; read lines 233-241: existing token estimate block with 15k/25k tiers — budget block extends this, does NOT replace it; read lines 430-515: _cost block + COST_TMP pattern — mirror this for BUDGET_TMP; read the final lines with WARN/ERR counters + `[ "$WARN" -gt 0 ] && exit 1` gate)
    - .planning/phases/28-promptfoo-eval-context-budget-linter/28-RESEARCH.md (Pattern 7: full --budget linter bash code block — implement verbatim; Pattern 8: _eval_extract_skill_used awk function + _eval_coverage_diff bash code — implement verbatim; Pitfall 5: BUDGET_TMP must join _audit_cleanup, not its own trap)
    - .planning/phases/27-schema-version-aware-audit/27-03-SUMMARY.md (SCHM-05 --json pattern: JSON_MODE, json_check function, JSONL→jq output structure — EVAL-05 note() advisory uses json_check "EVAL-05-*" IDs in the check accumulator)
  </read_first>
  <action>
Make two additions to scripts/audit-setup.sh:

ADDITION 1 — extend _audit_cleanup (line ~29, already defined):
  Locate the existing line: `_audit_cleanup() { rm -f "${CHECKS_JSONL:-}" "${COST_TMP:-}"; }`
  Replace with: `_audit_cleanup() { rm -f "${CHECKS_JSONL:-}" "${COST_TMP:-}" "${BUDGET_TMP:-}"; }`
  This is a SINGLE-LINE change. Do NOT add a new trap line — the existing `trap _audit_cleanup EXIT` at line ~30 already covers the function; extending the function body is sufficient (WR-03 lesson).

ADDITION 2 — --budget linter block (EVAL-04):
  Add after the existing `# Total token estimate` block (lines ~233-241) and before the `# Conflict markers` section. The block is gated on `[ "${CONJURE_BUDGET:-0}" = "1" ]`. Implement from RESEARCH Pattern 7 verbatim:

  BUDGET_TMP declaration: `BUDGET_TMP=""` at top-level (before the if gate) so _audit_cleanup's `${BUDGET_TMP:-}` expansion is safe even when --budget was not passed.

  Inside the block:
  a. BUDGET_TMP="$(mktemp)" — no EXIT trap needed (joins _audit_cleanup already registered).
  b. If CLAUDE.md exists: compute chars via wc -c, tokens = chars/4, printf '%s %s\n' "$_tokens" "CLAUDE.md" >> "$BUDGET_TMP".
  c. While loop over find .claude/skills -name SKILL.md 2>/dev/null (same pattern as the existing --cost block, lines ~478-483): compute chars, tokens, derive relative path via ${_skill_md#./}, printf to BUDGET_TMP.
  d. TOTAL_BUDGET_TOKENS via awk '{s+=$1} END{print s+0}' "$BUDGET_TMP".
  e. Threshold constants: BUDGET_THRESHOLD_WARN=15000, BUDGET_THRESHOLD_ERR=25000.
  f. If CONJURE_PORCELAIN=1 (porcelain JSON mode):
     - sort -rn "$BUDGET_TMP" | head -5 to a temp file (_top5_tmp).
     - Build _contrib_jsonl tempfile with jq -cn per contributor line.
     - Emit JSON via jq -cn --argjson total --argjson threshold --argjson over --slurpfile contributors per RESEARCH Pattern 7 (use `$contributors | flatten` — not just $contributors — to unwrap the slurpfile array-of-arrays).
     - rm -f _top5_tmp _contrib_jsonl.
     - Do NOT emit any human() lines in this mode.
  g. Else (human mode):
     - human() header line "── Context Budget ─────────────────────────────────────"
     - human() "  Always-loaded: CLAUDE.md + skill SKILL.md indexes"
     - human() "  Estimated tokens: ~$TOTAL_BUDGET_TOKENS (chars/4 heuristic)"
     - sort -rn "$BUDGET_TMP" | head -5 | while IFS=' ' read -r _tok _path: human() per contributor with padded path and ~N tokens.
     - Threshold gates using err()/warn()/ok() per RESEARCH Pattern 7:
       >=25k: err "context budget: ~$TOTAL_BUDGET_TOKENS tokens (>=25000 — prune CLAUDE.md or skills)"
       >=15k: warn "context budget: ~$TOTAL_BUDGET_TOKENS tokens (>=15000 — watch for growth)"
       else:  ok "context budget: ~$TOTAL_BUDGET_TOKENS tokens (well-tuned)"
     Note: err() increments ERR counter and results in exit 2; warn() increments WARN and results in exit 1. These are the EXISTING audit exit code semantics (do not change).
  h. rm -f "$BUDGET_TMP" — NOT needed in the block body since _audit_cleanup handles it, but include for defense-in-depth since the trap only fires on EXIT.
     WAIT: actually, do rm -f "$BUDGET_TMP" at the end of the block. _audit_cleanup covers the EXIT case; inline cleanup is defense-in-depth for the normal code path.

ADDITION 3 — EVAL-05 coverage diff block:
  Add near the END of audit-setup.sh, AFTER the --budget block, BEFORE the final WARN/ERR gate.
  This block always runs (no gate condition — EVAL-05 is always advisory).

  Implement _eval_extract_skill_used and _eval_coverage_diff from RESEARCH Pattern 8 verbatim.
  The _eval_coverage_diff function:
  a. If config ($TARGET/.conjure/eval/promptfooconfig.yaml) does not exist:
     note "no eval config — run \`conjure eval init\` (EVAL-05)"
     json_check "EVAL-05-no-config" "note" "no eval config — run conjure eval init"
     return 0 (or just continue — this is in the main script body).
  b. Extract asserted skill names into a sorted temp file via _eval_extract_skill_used.
  c. Extract installed skill names via find .claude/skills -name SKILL.md | sed | sort into another temp file.
  d. comm -23 to find installed skills NOT in asserted skills.
  e. For each gap skill: note "⚠ [eval] skill 'SKILL' has no skill-used assertion — run \`conjure eval init\` to update"; json_check "EVAL-05-gap" "note" "skill 'SKILL' has no skill-used assertion in eval config"
  f. If no gaps: ok "eval coverage: all installed skills have skill-used assertions"
  g. rm -f temp files used in the diff.

  Inline the function body directly (no sub-function call needed — keep the script flat like the rest of audit-setup.sh). Alternatively, define _eval_extract_skill_used as a function at the top of the EVAL-05 section and call it inline.

IMPORTANT POSIX 3.2+ compliance:
  - No associative arrays, no mapfile, no local -n.
  - Use while IFS= read -r / while IFS=' ' read -r for line iteration.
  - Use `${var%%.*}` / `${var#*}` pattern substitution (no arrays).
  - The awk two-line lookahead for skill-used extraction is POSIX awk (no gawk extensions).

IMPORTANT: CONJURE_PORCELAIN is a NEW env var for the --budget --porcelain combination. Check
if CONJURE_PORCELAIN is already used in audit-setup.sh from Phase 27 (it is a check.sh variable
in the check path — audit-setup.sh likely does NOT have it). Add `CONJURE_PORCELAIN="${CONJURE_PORCELAIN:-0}"` near the top alongside the existing CONJURE_BUDGET variable. The --json flag (CONJURE_JSON) remains separate.
  </action>
  <verify>
    <automated>
      grep -q "BUDGET_TMP" scripts/audit-setup.sh &amp;&amp;
      grep -q "_audit_cleanup.*BUDGET_TMP" scripts/audit-setup.sh &amp;&amp;
      grep -qv "trap.*BUDGET" scripts/audit-setup.sh &amp;&amp;
      grep -q "CONJURE_BUDGET" scripts/audit-setup.sh &amp;&amp;
      grep -q "BUDGET_THRESHOLD_ERR=25000" scripts/audit-setup.sh &amp;&amp;
      grep -q "EVAL-05" scripts/audit-setup.sh &amp;&amp;
      grep -q "_eval_extract_skill_used" scripts/audit-setup.sh &amp;&amp;
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/audit-setup.sh &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep -E "(PASS|FAIL).*EVAL-04-budget-err" | grep -c "PASS" | grep -qE "^[1-9]" &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep -E "(PASS|FAIL).*EVAL-04-porcelain" | grep -c "PASS" | grep -qE "^[1-9]" &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep -E "(PASS|FAIL).*EVAL-05-gap" | grep -c "PASS" | grep -qE "^[1-9]" &amp;&amp;
      echo "audit-setup.sh: --budget block + EVAL-05 coverage diff implemented, tests GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "_audit_cleanup.*BUDGET_TMP" scripts/audit-setup.sh` exits 0 — BUDGET_TMP is cleaned up via the EXISTING _audit_cleanup function (NOT a new trap)
    - `grep -qv "trap.*BUDGET" scripts/audit-setup.sh` exits 0 — NO new EXIT trap is registered for BUDGET_TMP (WR-03 lesson: bash has one EXIT slot)
    - Running `CONJURE_HOME=. CONJURE_BUDGET=1 bash scripts/audit-setup.sh tests/fixtures/_eval-overbudget/harness` exits 2 (>=25k tokens triggers err())
    - Running `CONJURE_HOME=. CONJURE_BUDGET=1 bash scripts/audit-setup.sh tests/fixtures/_eval/harness` exits 0 or 1 (small harness is under err threshold)
    - Running `CONJURE_HOME=. CONJURE_BUDGET=1 CONJURE_PORCELAIN=1 bash scripts/audit-setup.sh tests/fixtures/_eval/harness` emits JSON stdout that passes `jq -e '.total_tokens'` and `jq -e '(.contributors | type) == "array"'`
    - Running `CONJURE_HOME=. bash scripts/audit-setup.sh tests/fixtures/_eval-coverage-gap/harness` (without --budget) outputs text containing "code-review" (the gap skill) and exits 0 or 1 (note() is advisory — NEVER exit 2)
    - Running `CONJURE_HOME=. bash scripts/audit-setup.sh tests/fixtures/_eval/harness` on a harness with no .conjure/eval/ dir outputs "no eval config" or "conjure eval init" and exits 0 or 1
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/audit-setup.sh` exits 0
    - `bash tests/run.sh 2>&1 | grep "PASS.*EVAL-04-budget-err"` shows at least 1 PASS
    - `bash tests/run.sh 2>&1 | grep "PASS.*EVAL-04-porcelain"` shows at least 1 PASS
    - `bash tests/run.sh 2>&1 | grep "PASS.*EVAL-05-gap"` shows at least 1 PASS
    - `bash tests/run.sh 2>&1 | grep "PASS.*EVAL-05-noconfig"` shows at least 1 PASS
    - All existing Phase 27 SCHM-* tests remain GREEN (no regression in audit-setup.sh)
    - Running `CONJURE_HOME=. bash scripts/audit-setup.sh <any-fixture>` NEVER invokes npx or promptfoo (eval is decoupled from audit path)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire --budget and --porcelain flags in cli/conjure cmd_audit</name>
  <files>cli/conjure</files>
  <read_first>
    - cli/conjure (read lines 147-170: cmd_audit function — add --budget and --porcelain to the flag-parsing while loop; add CONJURE_BUDGET and CONJURE_PORCELAIN to the env-var forwarding line; update the usage line for conjure audit; read lines 35-40: usage() function conjure audit line)
  </read_first>
  <action>
Modify cli/conjure cmd_audit to add --budget and --porcelain flag support.

1. In cmd_audit, add two new local variables after the existing ones:
     local do_budget=0 do_porcelain=0

2. In the flag-parsing while loop, add two new cases:
     --budget)       do_budget=1 ;;
     --porcelain)    do_porcelain=1 ;;

3. Update the CONJURE_HOME= forwarding line:
   From: `CONJURE_HOME="$CONJURE_HOME" CONJURE_COST="$do_cost" CONJURE_EXACT="$do_exact" \`
         `  CONJURE_RETIRE="$do_retire" CONJURE_JSON="$do_json" \`
         `  bash "$CONJURE_HOME/scripts/audit-setup.sh" "$target"`
   To: Add `CONJURE_BUDGET="$do_budget" CONJURE_PORCELAIN="$do_porcelain"` to the env var list.

4. Update the usage() function conjure audit line:
   From: `conjure audit [--json] [--cost] [--exact] [--retire-list] [target]`
   To:   `conjure audit [--json] [--cost] [--exact] [--retire-list] [--budget] [--porcelain] [target]`

5. Update the --help case in cmd_audit:
   From: `echo "Usage: conjure audit [--json] [--cost] [--exact] [--retire-list] [target]"`
   To:   `echo "Usage: conjure audit [--json] [--cost] [--exact] [--retire-list] [--budget] [--porcelain] [target]"`

Do not alter any other cmd_audit logic, the --json flow (which routes preflight to stderr), or the dispatch case statement.
  </action>
  <verify>
    <automated>
      grep -q "CONJURE_BUDGET" cli/conjure &amp;&amp;
      grep -q "CONJURE_PORCELAIN" cli/conjure &amp;&amp;
      grep -q "\-\-budget" cli/conjure &amp;&amp;
      grep -q "\-\-porcelain" cli/conjure &amp;&amp;
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 cli/conjure &amp;&amp;
      bash cli/conjure audit --help 2>&amp;1 | grep -q "budget" &amp;&amp;
      bash cli/conjure help 2>&amp;1 | grep "audit" | grep -q "budget" &amp;&amp;
      echo "cli/conjure: --budget and --porcelain wired in cmd_audit, shellcheck clean"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "CONJURE_BUDGET" cli/conjure` exits 0
    - `grep -q "CONJURE_PORCELAIN" cli/conjure` exits 0
    - `bash cli/conjure audit --help` exits 0 and output contains "--budget"
    - `bash cli/conjure help | grep audit` shows "--budget" in the usage line
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 cli/conjure` exits 0
    - `bash cli/conjure audit --budget --porcelain tests/fixtures/_eval/harness 2>/dev/null | jq -e '.total_tokens'` exits 0 (JSON shape correct from end-to-end CLI invocation)
    - `bash cli/conjure audit tests/fixtures/_eval-coverage-gap/harness 2>&1` exits 0 or 1 (eval coverage gap note is advisory — no exit 2 from cli) and output contains "code-review"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .conjure/eval/promptfooconfig.yaml → EVAL-05 grep/awk parser | File is user-repo content; read-only parse via awk; no user input at read time |
| BUDGET_TMP → _audit_cleanup EXIT trap | Tempfile; must be cleaned up; cannot use a second trap without clobbering CHECKS_JSONL cleanup |
| audit-setup.sh → promptfoo | Zero boundary — audit NEVER invokes promptfoo; the EVAL-05 block only reads the config file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04a | Denial of Service | Context budget under-counting always-loaded context — false "within budget" while session blows context window | mitigate | Budget linter counts CLAUDE.md (always loaded) + ALL SKILL.md files (conservative: counts entire file, not just frontmatter; overcount is safe — warns earlier than a false negative would); threshold constants match existing audit tiers (15k/25k) |
| T-28-04b | Tampering | BUDGET_TMP tempfile not cleaned up (leaks under /tmp) | mitigate | _audit_cleanup extended to include ${BUDGET_TMP:-}; rm -f inline at end of block for defense-in-depth; no new trap registered (WR-03 lesson preserved) |
| T-28-04c | Tampering | --porcelain JSON injection via BUDGET_TMP path or tokens value | mitigate | jq -cn --argjson for all numeric values; --arg for path strings; slurpfile for contributors array — all values are injection-safe (jq handles escaping) |
| T-28-05a | Denial of Service | EVAL-05 gap report masking a missing eval config as "all covered" | mitigate | _eval_coverage_diff checks for config file existence first; absent config emits note() not ok(); no false "all covered" when config is absent |
| T-28-05b | Tampering | EVAL-05 awk parser producing false negatives (gaps missed) | mitigate | awk two-line lookahead from RESEARCH Pattern 8 is verbatim from research; assertion A5 in Assumptions Log flags this as [ASSUMED]; EVAL-05-gap fixture test in tests/run.sh catches false-negative regressions |
| T-28-eval-coupling | Tampering | eval coupling leaking into audit: promptfoo invoked from audit path | mitigate | acceptance_criteria explicitly asserts "NEVER invokes npx or promptfoo from audit path"; EVAL-05 reads config file only (grep/awk, no exec); --budget uses only bash arithmetic + wc; zero npx calls in audit-setup.sh |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — bash + jq + awk only; promptfoo is invoked ONLY from scripts/eval.sh cmd_eval_run |
</threat_model>

<verification>
After both tasks complete:
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/audit-setup.sh cli/conjure` exits 0
- `grep -q "_audit_cleanup.*BUDGET_TMP" scripts/audit-setup.sh` exits 0 (no second trap)
- `bash cli/conjure audit --budget --porcelain tests/fixtures/_eval/harness 2>/dev/null | jq -e '.total_tokens'` exits 0
- `bash cli/conjure audit --budget tests/fixtures/_eval-overbudget/harness` exits 2 (>=25k tokens)
- `bash cli/conjure audit tests/fixtures/_eval-coverage-gap/harness 2>&1` exits 0 or 1 and contains "code-review"
- `bash tests/run.sh 2>&1 | grep "PASS.*EVAL-04"` shows PASSes
- `bash tests/run.sh 2>&1 | grep "PASS.*EVAL-05"` shows PASSes
- All existing SCHM-* tests remain GREEN
- `bash tests/run.sh` exits 0 (all tests PASS — full green suite)
</verification>

<success_criteria>
- EVAL-04: `conjure audit --budget` measures context load, flags thresholds, lists top-5 contributors; --porcelain emits parseable JSON; BUDGET_TMP joins _audit_cleanup (no second trap)
- EVAL-05: `conjure audit` reports skills with no skill-used coverage as note() advisory (exit 0); config absent → note() advisory; promptfoo never invoked from audit path
- All EVAL-01..05 tests in tests/run.sh are GREEN
- `bash tests/run.sh` exits 0 — Phase 28 complete
</success_criteria>

<output>
Create .planning/phases/28-promptfoo-eval-context-budget-linter/28-03-SUMMARY.md when done.
</output>
