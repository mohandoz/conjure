---
phase: 29-workspace-orchestration-read-only
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/_workspace/repos/alpha/.claude/settings.json
  - tests/fixtures/_workspace/repos/alpha/CLAUDE.md
  - tests/fixtures/_workspace/repos/beta/.claude/settings.json
  - tests/fixtures/_workspace/repos/beta/CLAUDE.md
  - tests/fixtures/_workspace/repos/gamma/.claude/settings.json
  - tests/fixtures/_workspace/repos/gamma/CLAUDE.md
  - tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json
  - tests/fixtures/_workspace/repos/gamma-bad/CLAUDE.md
  - tests/fixtures/_workspace/.conjure-workspace.json
  - tests/fixtures/_workspace-badpath/.conjure-workspace.json
  - tests/run.sh
autonomous: true
requirements:
  - WS-01
  - WS-02
  - WS-03
  - WS-04

must_haves:
  truths:
    - "tests/fixtures/_workspace/ contains a parent dir with 3 mini repos (alpha, beta, gamma), each with .claude/ + minimal settings.json + CLAUDE.md, plus a fourth repo variant (gamma-bad) whose settings.json triggers audit failure (boolean disableBypassPermissionsMode)"
    - "tests/fixtures/_workspace/.conjure-workspace.json is a pre-built valid manifest referencing all three good repos by relative path"
    - "tests/fixtures/_workspace-badpath/.conjure-workspace.json references 3 repos, one of which has an invalid path (nonexistent-repo), exercising the bad-path skip behavior"
    - "tests/run.sh contains a Phase 29 graceful-red WS block with all WS-01..04 test stubs that fail because lib/workspace.sh and scripts/workspace.sh are absent"
    - "The WS block includes a non-TTY no-yes reject test via pipe (</dev/null conjure workspace init → exit 2, no file written)"
  artifacts:
    - path: "tests/fixtures/_workspace/.conjure-workspace.json"
      provides: "Pre-built valid workspace manifest for WS-03/04 aggregation tests"
      contains: "schema_version"
    - path: "tests/fixtures/_workspace-badpath/.conjure-workspace.json"
      provides: "Bad-path manifest for WS-04 skip-and-warn test"
      contains: "nonexistent-repo"
    - path: "tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json"
      provides: "Repo variant whose audit --json output has status:fail (triggers aggregate exit 2)"
      contains: "disableBypassPermissionsMode"
    - path: "tests/run.sh"
      provides: "Phase 29 graceful-red WS test block"
      contains: "Phase 29 — Workspace Orchestration"
  key_links:
    - from: "tests/run.sh Phase 29 block"
      to: "lib/workspace.sh"
      via: "P29_WS_LIB_OK presence guard"
      pattern: "P29_WS_LIB_OK=.*\\[ -f.*lib/workspace.sh"
    - from: "tests/run.sh Phase 29 block"
      to: "scripts/workspace.sh"
      via: "P29_WS_SH_OK presence guard"
      pattern: "P29_WS_SH_OK=.*\\[ -f.*scripts/workspace.sh"
    - from: "tests/fixtures/_workspace/.conjure-workspace.json"
      to: "repos/alpha, repos/beta, repos/gamma"
      via: "relative path from manifest dir"
      pattern: "repos/alpha"
---

<objective>
Wave 0: Build all test infrastructure for Phase 29 BEFORE any feature code — workspace
fixtures (multi-repo tree), pre-built manifests, and the graceful-red WS test block in
tests/run.sh.

Purpose: Nyquist Wave 0 invariant. When Wave 0 completes, `bash tests/run.sh` exits
non-zero with FAIL on all WS-01..04 test cases because lib/workspace.sh and
scripts/workspace.sh do not yet exist. This proves every test case is wired BEFORE its
implementation.

Output: tests/fixtures/_workspace/ tree (3 valid repos + 1 audit-fail variant),
tests/fixtures/_workspace-badpath/ manifest, and Phase 29 graceful-red WS block in
tests/run.sh.
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-workspace-orchestration-read-only/29-CONTEXT.md
@.planning/phases/29-workspace-orchestration-read-only/29-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace fixture trees</name>
  <files>
    tests/fixtures/_workspace/repos/alpha/.claude/settings.json
    tests/fixtures/_workspace/repos/alpha/CLAUDE.md
    tests/fixtures/_workspace/repos/beta/.claude/settings.json
    tests/fixtures/_workspace/repos/beta/CLAUDE.md
    tests/fixtures/_workspace/repos/gamma/.claude/settings.json
    tests/fixtures/_workspace/repos/gamma/CLAUDE.md
    tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json
    tests/fixtures/_workspace/repos/gamma-bad/CLAUDE.md
    tests/fixtures/_workspace/.conjure-workspace.json
    tests/fixtures/_workspace-badpath/.conjure-workspace.json
  </files>
  <read_first>
    - tests/fixtures/_schema-audit/valid/harness/.claude/settings.json (minimal settings.json shape that passes conjure audit -- must have a "hooks" key if hooks referenced, else bare {"hooks":{}} is fine; the critical contract: settings.json must be valid JSON that jq can parse)
    - tests/fixtures/_schema-audit/disablebypass/harness/.claude/settings.json (how boolean disableBypassPermissionsMode is written -- must be {"disableBypassPermissionsMode": true} so conjure audit --json emits status:"fail")
    - scripts/audit-setup.sh lines 1-30 (CONJURE_JSON env var + JSON_MODE handling so CONJURE_JSON=1 audit-setup.sh emits {"schema_version","status","checks":[],"summary":{}} to stdout)
    - .planning/phases/29-workspace-orchestration-read-only/29-CONTEXT.md (manifest schema: {schema_version, generated, repos:[{name,path,tags:[]}]}; relative paths from manifest dir; parent-dir discovery pattern)
  </read_first>
  <action>
Create all fixture files. All _-prefixed directories are excluded from the generic audit/golden loops in tests/run.sh.

TREE 1 — tests/fixtures/_workspace/repos/  (the three good repos):

repos/alpha/CLAUDE.md — minimal CLAUDE.md (single line: "# alpha repo") — enough for audit to parse.
repos/alpha/.claude/settings.json — {"hooks":{}} — minimal valid JSON settings; passes conjure audit --json with status:"pass" or "warn" (warnings are acceptable; only status:"fail" triggers aggregate exit 2).

repos/beta/CLAUDE.md — "# beta repo"
repos/beta/.claude/settings.json — {"hooks":{}} — same minimal shape.

repos/gamma/CLAUDE.md — "# gamma repo"
repos/gamma/.claude/settings.json — {"hooks":{}} — same minimal shape.

AUDIT-FAIL VARIANT:
repos/gamma-bad/CLAUDE.md — "# gamma-bad repo"
repos/gamma-bad/.claude/settings.json — write exactly {"disableBypassPermissionsMode": true} (boolean, not string) so that conjure audit --json on this repo produces status:"fail" (SCHM-02 fires). This is the fixture that triggers aggregate exit 2 in workspace audit tests.

MANIFEST — tests/fixtures/_workspace/.conjure-workspace.json:
Write the JSON object: {"schema_version":1,"generated":"2026-06-03T00:00:00Z","repos":[{"name":"alpha","path":"repos/alpha","tags":[]},{"name":"beta","path":"repos/beta","tags":[]},{"name":"gamma","path":"repos/gamma","tags":[]}]}
Note: paths are RELATIVE to the manifest directory (tests/fixtures/_workspace/). The manifest references only the three GOOD repos (alpha, beta, gamma); gamma-bad is NOT in this manifest — it is used separately by bad-path and audit-fail variants. The manifest must be valid JSON (jq-parseable).

BAD-PATH MANIFEST — tests/fixtures/_workspace-badpath/.conjure-workspace.json:
Write the JSON object: {"schema_version":1,"generated":"2026-06-03T00:00:00Z","repos":[{"name":"alpha","path":"../workspace/repos/alpha","tags":[]},{"name":"beta","path":"../workspace/repos/beta","tags":[]},{"name":"nonexistent-repo","path":"repos/nonexistent-repo","tags":[]}]}
The path "repos/nonexistent-repo" does not exist — this is the bad-path entry. alpha and beta point to the good repos via relative traversal from the _workspace-badpath/ dir.

Verification that fixtures are well-formed:
  jq empty tests/fixtures/_workspace/.conjure-workspace.json
  jq empty tests/fixtures/_workspace-badpath/.conjure-workspace.json
  jq -r '.repos[].path' tests/fixtures/_workspace/.conjure-workspace.json  # must show 3 paths
  jq -r '.repos[].path' tests/fixtures/_workspace-badpath/.conjure-workspace.json  # must show 3 paths, 1 bad
  </action>
  <verify>
    <automated>
      test -f tests/fixtures/_workspace/repos/alpha/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_workspace/repos/beta/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_workspace/repos/gamma/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json &amp;&amp;
      jq -e '.disableBypassPermissionsMode == true' tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json &amp;&amp;
      jq empty tests/fixtures/_workspace/.conjure-workspace.json &amp;&amp;
      test "$(jq '.repos | length' tests/fixtures/_workspace/.conjure-workspace.json)" = "3" &amp;&amp;
      jq empty tests/fixtures/_workspace-badpath/.conjure-workspace.json &amp;&amp;
      grep -q "nonexistent-repo" tests/fixtures/_workspace-badpath/.conjure-workspace.json &amp;&amp;
      echo "Wave 0 fixtures: all present and well-formed"
    </automated>
  </verify>
  <acceptance_criteria>
    - jq empty on both manifests exits 0 (valid JSON)
    - tests/fixtures/_workspace/.conjure-workspace.json has schema_version field and repos array with 3 entries (alpha, beta, gamma) — gamma-bad is absent from this manifest
    - tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json contains `"disableBypassPermissionsMode": true` (boolean, not string) so that conjure audit --json on it emits status:"fail"
    - tests/fixtures/_workspace-badpath/.conjure-workspace.json references "nonexistent-repo" as one of the 3 repos — this path does not exist on disk
    - All .claude/settings.json files in alpha/beta/gamma are valid JSON (jq empty exits 0)
    - All fixture dirs are _-prefixed (excluded from generic audit loop in tests/run.sh)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Phase 29 graceful-red WS test block to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh lines 5731-5742 (the GH_HIDE_STUBS cleanup + summary block at the end — insertion point is IMMEDIATELY BEFORE the "# Clean up any gh-hiding stub dirs" comment)
    - tests/run.sh lines 5199-5215 (Phase 28 block header style: separator width, presence guard pattern, echo header — mirror exactly for Phase 29)
    - .planning/phases/29-workspace-orchestration-read-only/29-VALIDATION.md (Per-Task Verification Map: all WS-* test IDs and their correct-behavior descriptions)
    - scripts/adopt.sh lines 56-61 (the /dev/tty prompt + non-TTY exit 2 pattern to reference in the test description)
  </read_first>
  <action>
Append a Phase 29 test block to tests/run.sh IMMEDIATELY BEFORE the "# Clean up any gh-hiding stub dirs" / "for _s in $GH_HIDE_STUBS" loop. Do NOT alter the summary block or the final `[ "$FAIL" -eq 0 ]` gate. Do NOT redefine the pass/fail helpers.

SECTION HEADER (match Phase 28 width exactly — 80 dashes):
  # ──────────────────────────────────────────────────────────────────────────────
  # Phase 29 — Workspace Orchestration — Read-Only (WS-01..04)
  # Mirrors Phase 28 block style: mktemp sandboxes with EXIT-trap discipline.
  # All workspace.sh invocations guarded behind P29_WS_SH_OK so the suite reports
  # graceful RED when scripts/workspace.sh is absent (Wave 1 not yet complete).
  # lib/workspace.sh helper invocations guarded behind P29_WS_LIB_OK.
  # ──────────────────────────────────────────────────────────────────────────────

PRESENCE GUARDS (declare before the echo header):
  P29_WS_LIB="$CONJURE_HOME/lib/workspace.sh"
  P29_WS_SH="$CONJURE_HOME/scripts/workspace.sh"
  P29_WS_LIB_OK=0
  P29_WS_SH_OK=0
  [ -f "$P29_WS_LIB" ] && P29_WS_LIB_OK=1
  [ -f "$P29_WS_SH" ] && P29_WS_SH_OK=1

Echo header:
  echo
  echo "▸ Phase 29 — Workspace Orchestration — Read-Only (WS-01..04)"

Implement the following test cases. Each uses its own mktemp -d sandbox with trap 'rm -rf "$P29_xxx_DIR"' EXIT, then trap - EXIT after cleanup. Use POSIX bash 3.2+: no associative arrays, no mapfile, no local -n.

WS-01-manifest-valid: workspace_manifest_validate in lib/workspace.sh validates a well-formed manifest:
  - Define P29_MV_DIR=$(mktemp -d); trap 'rm -rf "$P29_MV_DIR"' EXIT
  - Copy tests/fixtures/_workspace/.conjure-workspace.json to "$P29_MV_DIR/.conjure-workspace.json"
  - If P29_WS_LIB_OK=1: source "$P29_WS_LIB"; call workspace_manifest_validate "$P29_MV_DIR/.conjure-workspace.json"; assert exit 0
  - Else: fail "lib/workspace.sh not implemented — Wave 1 must create it (WS-01-manifest-valid)"
  - pass/fail "workspace_manifest_validate accepts a well-formed manifest (WS-01-manifest-valid)"
  - trap - EXIT; rm -rf "$P29_MV_DIR"

WS-01-manifest-invalid: workspace_manifest_validate rejects malformed JSON → exit 2:
  - mktemp -d P29_MV2_DIR; write "not-json" to "$P29_MV2_DIR/.conjure-workspace.json"
  - If P29_WS_LIB_OK=1: source (idempotent); P29_MV2_RC=0; workspace_manifest_validate "$P29_MV2_DIR/.conjure-workspace.json" || P29_MV2_RC=$?; assert P29_MV2_RC -eq 2
  - Else: fail "lib/workspace.sh not implemented — Wave 1 must create it (WS-01-manifest-invalid)"
  - pass/fail "workspace_manifest_validate rejects malformed manifest with exit 2 (WS-01-manifest-invalid)"

WS-02-init-writes: workspace init writes .conjure-workspace.json in a TTY-simulated (--yes) scenario:
  - mktemp -d P29_INIT_DIR; create 2 sibling subdirs each with .claude/ inside P29_INIT_DIR
  - If P29_WS_SH_OK=1: P29_INIT_RC=0; CONJURE_HOME="$CONJURE_HOME" bash "$P29_WS_SH" init --yes "$P29_INIT_DIR" >/dev/null 2>&1 || P29_INIT_RC=$?; assert P29_INIT_RC -eq 0; assert -f "$P29_INIT_DIR/.conjure-workspace.json"; assert jq empty "$P29_INIT_DIR/.conjure-workspace.json" >/dev/null 2>&1
  - Else: fail "scripts/workspace.sh not implemented — Wave 1 must create it (WS-02-init-writes)"
  - pass/fail "workspace init --yes writes valid .conjure-workspace.json (WS-02-init-writes)"

WS-02-init-no-tty: workspace init without --yes in non-TTY context exits 2 and writes NO file:
  - mktemp -d P29_NOTTY_DIR; create a sibling subdir with .claude/ inside P29_NOTTY_DIR
  - If P29_WS_SH_OK=1: P29_NOTTY_RC=0; CONJURE_HOME="$CONJURE_HOME" bash "$P29_WS_SH" init "$P29_NOTTY_DIR" </dev/null >/dev/null 2>&1 || P29_NOTTY_RC=$?; assert P29_NOTTY_RC -eq 2; assert ! -f "$P29_NOTTY_DIR/.conjure-workspace.json"
  - Else: fail "scripts/workspace.sh not implemented — Wave 1 must create it (WS-02-init-no-tty)"
  - pass/fail "workspace init without --yes in non-TTY exits 2 and writes no file (WS-02-init-no-tty)"

WS-03-check-table: workspace check on valid manifest emits a per-repo table:
  - mktemp -d P29_CHK_DIR; copy tests/fixtures/_workspace/ tree to P29_CHK_DIR (cp -r "$CONJURE_HOME/tests/fixtures/_workspace/." "$P29_CHK_DIR/")
  - If P29_WS_SH_OK=1: P29_CHK_RC=0; P29_CHK_OUT="$(CONJURE_HOME="$CONJURE_HOME" bash "$P29_WS_SH" check "$P29_CHK_DIR/.conjure-workspace.json" 2>&1)" || P29_CHK_RC=$?; assert P29_CHK_RC -le 1 (0 = all clean, 1 = partial); assert printf '%s' "$P29_CHK_OUT" contains "alpha"; assert printf '%s' "$P29_CHK_OUT" contains "beta"
  - Else: fail "scripts/workspace.sh not implemented — Wave 1 must create it (WS-03-check-table)"
  - pass/fail "workspace check emits per-repo status table (WS-03-check-table)"

WS-03-check-fail-tolerant: workspace check with a permissions-error repo exits 1 (not 2) and processes remaining repos:
  - Use the same P29_CHK_DIR if still in scope, or recreate it; note this test is conceptually validated when the workspace check block in scripts/workspace.sh is implemented — test stubs fail when absent
  - If P29_WS_SH_OK=1: create a scratch manifest with 3 repos where 1 is gamma-bad (will error on check if audit exits non-zero); assert overall workspace check exit is 0 or 1 but NOT 2
  - Else: fail "scripts/workspace.sh not implemented — Wave 1 must create it (WS-03-check-fail-tolerant)"
  - pass/fail "workspace check exits 1 partial-success (not 2) when one repo errors (WS-03-check-fail-tolerant)"

WS-03-check-badpath: workspace check skips bad-path repo with a warning and processes the rest:
  - mktemp -d P29_BPCHK_DIR; copy tests/fixtures/_workspace-badpath/.conjure-workspace.json; copy actual repo dirs (alpha, beta from _workspace/repos/) to the expected relative paths; run workspace check on the manifest; assert exit ≤ 1; assert output contains "alpha" or "beta" (other repos processed); assert output contains warning/skip text about the bad path
  - Else: fail "scripts/workspace.sh not implemented — Wave 1 must create it (WS-03-check-badpath)"
  - pass/fail "workspace check skips bad-path repo with warning and processes remaining repos (WS-03-check-badpath)"

WS-04-audit-pass: workspace audit on all-good manifest exits 0:
  - mktemp -d P29_AUD_DIR; copy tests/fixtures/_workspace/ (the 3 good repos manifest); run CONJURE_HOME="$CONJURE_HOME" bash "$P29_WS_SH" audit "$P29_AUD_DIR/.conjure-workspace.json"; assert exit 0 or 1 (all repos pass/warn — never exit 2 without a fail)
  - Else: fail "scripts/workspace.sh not implemented — Wave 2 must create it (WS-04-audit-pass)"
  - pass/fail "workspace audit on all-good repos exits 0 or 1 (WS-04-audit-pass)"

WS-04-audit-fail: workspace audit exits 2 when any repo audit status is fail:
  - mktemp -d P29_AUDF_DIR; build a manifest referencing alpha, beta, and gamma-bad (the boolean-disableBypassPermissionsMode variant); run workspace audit; assert exit 2
  - Else: fail "scripts/workspace.sh not implemented — Wave 2 must create it (WS-04-audit-fail)"
  - pass/fail "workspace audit exits 2 when any repo status is fail (WS-04-audit-fail)"

WS-04-audit-failfast: workspace audit --fail-fast stops at first failure:
  - Same setup as WS-04-audit-fail; add --fail-fast flag; assert exit 2; assert output does NOT contain output from repos listed after gamma-bad in the manifest (i.e., processing stopped)
  - Else: fail "scripts/workspace.sh not implemented — Wave 2 must create it (WS-04-audit-failfast)"
  - pass/fail "workspace audit --fail-fast stops at first failure (WS-04-audit-failfast)"

WS-04-audit-badpath: workspace audit skips bad-path repo with warning and processes rest:
  - Same structure as WS-03-check-badpath but calling workspace audit; assert skip warning present; assert other repos processed
  - Else: fail "scripts/workspace.sh not implemented — Wave 2 must create it (WS-04-audit-badpath)"
  - pass/fail "workspace audit skips bad-path repo with warning and processes remaining repos (WS-04-audit-badpath)"

All test cases:
  - Guard workspace.sh invocations behind P29_WS_SH_OK (else fail "scripts/workspace.sh not implemented — Wave 1/2 must create it (WS-XX-tag)").
  - Guard lib/workspace.sh invocations behind P29_WS_LIB_OK (else fail "lib/workspace.sh not implemented — Wave 1 must create it (WS-XX-tag)").
  - Use pass/fail helpers already in tests/run.sh — do NOT redefine.
  - POSIX 3.2+: no associative arrays, no mapfile, no local -n.
  - Each test case gets its own mktemp -d with trap 'rm -rf "$P29_xxx_DIR"' EXIT and trap - EXIT after.
  - Insert IMMEDIATELY BEFORE the "for _s in $GH_HIDE_STUBS" cleanup loop.
  </action>
  <verify>
    <automated>
      grep -q "Phase 29" tests/run.sh &amp;&amp;
      grep -q "P29_WS_LIB_OK" tests/run.sh &amp;&amp;
      grep -q "P29_WS_SH_OK" tests/run.sh &amp;&amp;
      grep -q "WS-01-manifest-valid" tests/run.sh &amp;&amp;
      grep -q "WS-02-init-no-tty" tests/run.sh &amp;&amp;
      grep -q "WS-03-check-table" tests/run.sh &amp;&amp;
      grep -q "WS-04-audit-fail" tests/run.sh &amp;&amp;
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep -E "FAIL.*(WS-0)" | grep -c "FAIL" | grep -qE "^[1-9]" &amp;&amp;
      echo "Phase 29 block present, shellcheck clean, graceful-red on WS-01..04"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/run.sh contains "Phase 29 — Workspace Orchestration — Read-Only (WS-01..04)" section header
    - All 11 WS-* test tags present: WS-01-manifest-valid, WS-01-manifest-invalid, WS-02-init-writes, WS-02-init-no-tty, WS-03-check-table, WS-03-check-fail-tolerant, WS-03-check-badpath, WS-04-audit-pass, WS-04-audit-fail, WS-04-audit-failfast, WS-04-audit-badpath
    - WS-02-init-no-tty test uses `</dev/null` redirection (not PTY/expect) to simulate non-TTY and asserts both exit 2 AND no file written
    - `bash tests/run.sh 2>&1 | grep -E "FAIL.*(WS-)" | grep -c FAIL` exits 0 with count >= 9 (graceful-red — lib/workspace.sh and scripts/workspace.sh are absent)
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` passes (no new errors)
    - The existing summary block and `[ "$FAIL" -eq 0 ]` gate are unmodified
    - All Phase 28 EVAL-* tests are unmodified (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json → audit | Deliberately malformed settings (boolean disableBypassPermissionsMode) to trigger audit failure |
| tests/fixtures/_workspace-badpath/.conjure-workspace.json → workspace check/audit | Manifest with one non-existent path to test skip-with-warning behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-W0-01 | Tampering | gamma-bad settings.json must encode boolean true not string — wrong encoding fails the audit-fail fixture | mitigate | Task 1 acceptance_criteria asserts `jq -e '.disableBypassPermissionsMode == true'` on gamma-bad/settings.json — wrong encoding fails Wave 0 verify |
| T-29-W0-02 | Denial of Service | Graceful-red WS block silently passing when lib/workspace.sh or scripts/workspace.sh is absent | mitigate | Each WS-* test case is gated on P29_WS_LIB_OK / P29_WS_SH_OK; when absent, calls `fail "... not implemented"` — never silently passes |
| T-29-W0-03 | Tampering | Bad-path manifest accidentally resolving to a real directory on the test machine | mitigate | Use path "repos/nonexistent-repo" (no real dir at that relative location from _workspace-badpath/); Task 1 acceptance_criteria asserts path does not exist on disk |
| T-29-W0-04 | Information Disclosure | Fixture repos exposing real user data | accept | All fixture repos contain only synthetic CLAUDE.md ("# alpha repo") and minimal {"hooks":{}} settings; no real credentials or config |
| T-29-W0-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — bash + fixtures only; zero network egress |
</threat_model>

<verification>
After both tasks complete:
- `jq empty tests/fixtures/_workspace/.conjure-workspace.json` exits 0
- `jq empty tests/fixtures/_workspace-badpath/.conjure-workspace.json` exits 0
- `jq -e '.disableBypassPermissionsMode == true' tests/fixtures/_workspace/repos/gamma-bad/.claude/settings.json` exits 0
- `grep -q "nonexistent-repo" tests/fixtures/_workspace-badpath/.conjure-workspace.json` exits 0
- `bash tests/run.sh 2>&1 | grep -E "FAIL.*WS-" | grep -c FAIL` shows >= 9 FAIL (graceful-red)
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` exits 0
</verification>

<success_criteria>
- Wave 0 complete: all fixture trees, both manifests, and Phase 29 graceful-red WS block exist
- `bash tests/run.sh` exits non-zero (FAIL count > 0) because lib/workspace.sh and scripts/workspace.sh are absent
- Wave 1 (29-01) can now implement lib/workspace.sh + scripts/workspace.sh init + cmd_workspace to turn WS-01/02 tests green
</success_criteria>

<output>
Create .planning/phases/29-workspace-orchestration-read-only/29-00-SUMMARY.md when done.
</output>
