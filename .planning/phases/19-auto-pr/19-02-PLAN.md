---
phase: 19-auto-pr
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - tests/run.sh
autonomous: true
requirements:
  - AUTPR-01
  - AUTPR-02

must_haves:
  truths:
    - "AUTPR-01 zero-drift test: stub conjure check exit 0 → command prints 'Harness is current' and exits 0"
    - "AUTPR-01 missing-gh test: PATH without gh → command exits 2"
    - "AUTPR-01 idempotency test: stub gh pr list returns a URL → command prints URL and exits 0"
    - "AUTPR-02 cron test: conjure update --cron in a temp dir writes .github/workflows/conjure-update.yml with cron: '0 9 * * 1'"
    - "AUTPR-02 idempotency test: running --cron twice exits 0 both times with no crash"
    - "bash tests/run.sh exits 0 (PASS: N FAIL: 0)"
  artifacts:
    - path: "tests/run.sh"
      provides: "AUTPR regression test section"
      contains: "AUTPR-01"
  key_links:
    - from: "tests/run.sh AUTPR section"
      to: "cli/conjure"
      via: "CONJURE_HOME=$(pwd) cli/conjure update --pr / --cron"
      pattern: "conjure update --pr"
---

<objective>
Add the AUTPR regression test section to tests/run.sh. All tests use stub binaries for git and gh so no real network operations or PRs are created during the test run. Tests cover the four observable behaviours: zero-drift guard, missing-gh guard, idempotency, and cron template write.

Purpose: Prove the --pr and --cron implementations from Plan 01 behave correctly under controlled inputs without requiring network access or a real GitHub repo.

Output: tests/run.sh extended with an AUTPR section (≥ 8 new pass/fail assertions); `bash tests/run.sh` still exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/run.sh
@.planning/phases/19-auto-pr/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AUTPR regression tests to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    Read tests/run.sh lines 1460–1543 (the RESOLVE section + summary block) to understand the exact insertion point and the stub-bin pattern.
    Stub-bin pattern (established in SKILL-02 test at lines 1028–1042):
      `STUB_BIN="$(mktemp -d)"`
      `printf '#!/bin/sh\nexit 0\n' > "$STUB_BIN/gh"`
      `chmod +x "$STUB_BIN/gh"`
      `PATH="$STUB_BIN:$PATH"` — restore with `PATH="$SAVED_PATH"` and `rm -rf "$STUB_BIN"` after.
    Sandbox cleanup pattern: `mktemp -d` + `trap 'rm -rf "$DIR"' EXIT` + manual `rm -rf "$DIR"; trap - EXIT` at end of each sub-test.
    The AUTPR section must be inserted BEFORE the summary block (lines "echo" / "echo PASS: $PASS ..." / "[ "$FAIL" -eq 0 ]") so it is included in the pass/fail totals.
    Read .planning/phases/19-auto-pr/19-01-SUMMARY.md to confirm exact function names, echo strings, and exit codes produced by the --pr and --cron implementations.
  </read_first>
  <action>
    Insert the following AUTPR section into tests/run.sh immediately before the summary block (the final `echo` / `echo "PASS: $PASS ..."` / `[ "$FAIL" -eq 0 ]` lines).

    Section header:
      `echo`
      `echo "▸ Auto-PR tests (AUTPR-01, AUTPR-02)"`

    Sub-test AUTPR-01a — zero-drift guard (no gh needed):
      Create a temp dir. Run `CONJURE_HOME="$CONJURE_HOME" cli/conjure init "$AUTPR_DIR" >/dev/null 2>&1` to get a fully-current harness (check exits 0). Then:
      `AUTPR_OUT="$(CONJURE_HOME="$CONJURE_HOME" cli/conjure update --pr "$AUTPR_DIR" 2>&1 || true)"`
      Assert: output contains "Harness is current" → `pass "update --pr exits 0 on zero-drift harness (AUTPR-01)"`.
      Assert: `AUTPR_RC` captured separately; must be 0 → `pass/fail "update --pr exit code 0 on zero-drift (AUTPR-01)"`.
      Note: this test does NOT need a stub for gh because the zero-drift guard returns before ever calling gh.
      Clean up temp dir.

    Sub-test AUTPR-01b — missing-gh guard:
      Strip gh from PATH using the same FILTERED_PATH pattern as SKILL-02 (lines 1044–1054 of tests/run.sh).
      Create a temp dir with a deliberate drift: `conjure init` then append a byte to `.claude/settings.json`.
      Run `PATH="$FILTERED_PATH" CONJURE_HOME="$CONJURE_HOME" cli/conjure update --pr "$AUTPR_DIR" 2>&1`; capture output and exit code.
      Assert: exit code is 2 → `pass/fail "update --pr exits 2 when gh is absent (AUTPR-01)"`.
      Assert: output contains "gh CLI required" → `pass/fail "update --pr prints gh-required message (AUTPR-01)"`.
      Clean up temp dir.

    Sub-test AUTPR-01c — idempotency guard:
      Build a stub gh that, when called as `gh pr list ...`, prints a fake PR URL to stdout:
        `printf '#!/bin/sh\nif [ "$1" = "pr" ] && [ "$2" = "list" ]; then printf "https://github.com/owner/repo/pull/42\n"; fi\nexit 0\n' > "$STUB_BIN/gh"`
      Create a temp dir with drift (init + append to settings.json).
      Run `PATH="$STUB_BIN:$PATH" CONJURE_HOME="$CONJURE_HOME" cli/conjure update --pr "$AUTPR_DIR" 2>&1`; capture output.
      Assert: exit code is 0 → `pass/fail "update --pr exits 0 when PR already exists (AUTPR-01)"`.
      Assert: output contains "https://github.com" → `pass/fail "update --pr prints existing PR URL (AUTPR-01)"`.
      Clean up stub and temp dir.

    Sub-test AUTPR-02a — cron template write:
      Create a fresh temp dir (no conjure init needed).
      Run `CONJURE_HOME="$CONJURE_HOME" cli/conjure update --cron "$AUTPR_DIR" 2>&1`; capture RC.
      Assert: RC is 0 → `pass/fail "update --cron exits 0 (AUTPR-02)"`.
      Assert: file exists at `$AUTPR_DIR/.github/workflows/conjure-update.yml` → `pass/fail "conjure-update.yml written (AUTPR-02)"`.
      Assert: file contains `0 9 * * 1` → `pass/fail "cron schedule is Monday 09:00 UTC (AUTPR-02)"`.
      Assert: file contains `conjure update --pr` → `pass/fail "cron template invokes conjure update --pr (AUTPR-02)"`.
      Clean up temp dir.

    Sub-test AUTPR-02b — cron template idempotency:
      Create a fresh temp dir.
      Run `cli/conjure update --cron` twice. Both runs must exit 0.
      Assert: second run exits 0 → `pass/fail "update --cron is idempotent (AUTPR-02)"`.
      Clean up temp dir.

    Each sub-test must follow the established sandbox pattern:
      `AUTPR_DIR="$(mktemp -d)"`
      `trap 'rm -rf "$AUTPR_DIR"' EXIT`
      ... test body ...
      `rm -rf "$AUTPR_DIR"`
      `trap - EXIT`

    After inserting the section, run `bash -n tests/run.sh` to confirm no syntax errors, then `bash tests/run.sh` to confirm all tests pass.
  </action>
  <verify>
    <automated>bash -n /Users/mohandoz/u01/innovate/conjure/tests/run.sh && grep -q 'AUTPR-01' /Users/mohandoz/u01/innovate/conjure/tests/run.sh && grep -q 'AUTPR-02' /Users/mohandoz/u01/innovate/conjure/tests/run.sh && CONJURE_HOME=/Users/mohandoz/u01/innovate/conjure bash /Users/mohandoz/u01/innovate/conjure/tests/run.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run.sh` exits 0
    - `grep -q '▸ Auto-PR tests' tests/run.sh` succeeds
    - `grep -q 'AUTPR-01' tests/run.sh` succeeds
    - `grep -q 'AUTPR-02' tests/run.sh` succeeds
    - `grep -q 'Harness is current' tests/run.sh` succeeds (zero-drift assertion)
    - `grep -q 'gh CLI required' tests/run.sh` or `grep -q 'gh.* absent' tests/run.sh` succeeds (missing-gh assertion)
    - `grep -q 'conjure-update.yml' tests/run.sh` succeeds (cron file assertion)
    - `bash tests/run.sh` exits 0 with FAIL: 0
    - Count of new AUTPR-tagged pass/fail assertions: ≥ 8
  </acceptance_criteria>
  <done>tests/run.sh has AUTPR section; bash tests/run.sh exits 0 with FAIL: 0; all AUTPR acceptance criteria pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/run.sh→stub gh | Stub binary placed on PATH head; cleaned up after each sub-test |
| tests/run.sh→cli/conjure | Same CONJURE_HOME-scoped invocation pattern as all other regression tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering | stub gh binary in tests | mitigate | stub is a mktemp dir; PATH restored immediately after each sub-test; rm -rf on cleanup |
| T-19-07 | Information Disclosure | gh pr list output in tests | accept | test uses synthetic URLs; no real tokens or credentials |
| T-19-SC | Tampering | no new package installs | mitigate | zero new dependencies; test framework is hand-rolled bash |
</threat_model>

<verification>
- `bash -n tests/run.sh` exits 0
- `grep -q 'AUTPR-01' tests/run.sh` succeeds
- `grep -q 'AUTPR-02' tests/run.sh` succeeds
- `bash tests/run.sh` exits 0 (FAIL: 0)
</verification>

<success_criteria>
tests/run.sh has ≥ 8 new AUTPR-tagged assertions; all tests pass; no real git or gh network operations during test run; bash tests/run.sh exits 0.
</success_criteria>

<output>
Create `.planning/phases/19-auto-pr/19-02-SUMMARY.md` when done.
</output>
