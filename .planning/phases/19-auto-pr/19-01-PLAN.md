---
phase: 19-auto-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/conjure
autonomous: true
requirements:
  - AUTPR-01
  - AUTPR-02

must_haves:
  truths:
    - "conjure update --pr on a zero-drift harness prints 'Harness is current — no PR needed' and exits 0"
    - "conjure update --pr when a PR already exists for the branch prints the existing PR URL and exits 0"
    - "conjure update --pr when gh is absent exits 2 with a clear message"
    - "conjure update --cron writes .github/workflows/conjure-update.yml with weekly Monday 09:00 UTC schedule"
    - "conjure update --cron is idempotent (can be run twice without error)"
  artifacts:
    - path: "cli/conjure"
      provides: "--pr and --cron branches inside cmd_update"
      contains: "action=\"--pr\""
  key_links:
    - from: "cmd_update --pr branch"
      to: "scripts/check.sh"
      via: "CONJURE_HOME=$CONJURE_HOME bash $CONJURE_HOME/cli/conjure check $target"
      pattern: "conjure check"
    - from: "cmd_update --pr branch"
      to: "gh pr list"
      via: "idempotency guard"
      pattern: "gh pr list"
---

<objective>
Extend cmd_update in cli/conjure with --pr and --cron flag branches. This delivers both AUTPR-01 and AUTPR-02 behaviour directly inside the existing cmd_update function, following the inline worker pattern established by cmd_resolve and cmd_check.

Purpose: Ship the git/gh automation that lets users create a harness-update PR from the CLI, and the cron template that enables weekly automation without external dependencies.

Output: cli/conjure with two new action branches: --pr (full git branch + gh pr create flow) and --cron (write .github/workflows/conjure-update.yml).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@cli/conjure
@scripts/check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --pr and --cron arg parsing to cmd_update</name>
  <files>cli/conjure</files>
  <read_first>
    Read cli/conjure lines 192–290 (cmd_update function) and lines 356–372 (dispatch block).
    Key patterns to follow:
    - arg-parsing while loop: `--check|--apply) action="$1" ;;`
    - CONJURE_HOME-prefixed invocations: `CONJURE_HOME="$CONJURE_HOME" bash "$CONJURE_HOME/scripts/check.sh" "$target"`
    - cmd_resolve pattern for sourcing lib: `source "$CONJURE_HOME/lib/mutate.sh"`
    - `set -uo pipefail` is already at top; new local variables must be declared with `local`
    Porcelain format (from scripts/check.sh lines 106–110): one line per drifted file, format `<M|R|A> <path>`, no headers.
    Context file: .planning/phases/19-auto-pr/19-CONTEXT.md (full design decisions).
  </read_first>
  <action>
    In cmd_update's while-loop arg parser (currently `--check|--apply) action="$1" ;;`), add two new cases on the same line-group:
      `--pr)   action="--pr"   ;;`
      `--cron) action="--cron" ;;`

    Then add two new action branches in the if/case block that follows the version-comparison logic. Insert them BEFORE the existing `--apply` branch (so they are evaluated first). Use this exact structure:

    --pr branch (implements AUTPR-01 per D-branch-name, D-idempotency, D-zero-drift):
      1. Guard: `if ! command -v gh >/dev/null 2>&1; then echo "✗ gh CLI required for --pr (install: https://cli.github.com)" >&2; return 2; fi`
      2. Zero-drift guard: run `CONJURE_HOME="$CONJURE_HOME" bash "$CONJURE_HOME/cli/conjure" check "$target" >/dev/null 2>&1`; capture return code into `local check_rc`. If `check_rc` is 0, print "Harness is current — no PR needed" and return 0.
      3. Compute branch name: `local kit_ver; kit_ver="$(cat "$CONJURE_HOME/VERSION" 2>/dev/null || echo unknown)"`. Then `local branch_hash; branch_hash="$(printf '%s' "$kit_ver" | sha256sum 2>/dev/null | awk '{print $1}' || shasum -a 256 /dev/stdin <<< "$kit_ver" | awk '{print $1}')"`. Set `local branch="conjure/update-${branch_hash:0:7}"`.
         Note: the sha256sum / shasum fallback must be written so shellcheck does not flag it. Use: `if command -v sha256sum >/dev/null 2>&1; then branch_hash="$(printf '%s' "$kit_ver" | sha256sum | awk '{print $1}')"; else branch_hash="$(printf '%s' "$kit_ver" | shasum -a 256 | awk '{print $1}')"; fi`
      4. Idempotency check: `local existing; existing="$(gh pr list --head "$branch" --state open --json url --jq '.[0].url' 2>/dev/null || true)"`. If non-empty: `echo "▸ PR already exists: $existing"` then `return 0`.
      5. Build PR body from porcelain output: `local drift_lines; drift_lines="$(CONJURE_HOME="$CONJURE_HOME" bash "$CONJURE_HOME/cli/conjure" check --porcelain "$target" 2>/dev/null || true)"`. Format it as markdown: prepend `## Harness drift detected by \`conjure check\`` and `| Status | File |` table header, then each line `<S> <path>` becomes `| <S> | \`<path>\` |`.  Assign the full body to `local pr_body`.
      6. git operations (run from inside `$target`):
         - `git -C "$target" checkout -b "$branch"` — exits non-zero on failure; propagate with `|| { echo "✗ git checkout -b failed"; return 1; }`
         - `CONJURE_HOME="$CONJURE_HOME" bash "$CONJURE_HOME/cli/conjure" update --apply "$target"` — apply the drift; propagate failure.
         - `git -C "$target" add -A`
         - `git -C "$target" commit -m "conjure: update harness to v${CONJURE_VERSION}"`
         - `git -C "$target" push origin "$branch"` — exits non-zero on network failure; propagate.
      7. Open PR: `gh pr create --title "conjure: update harness to v${CONJURE_VERSION}" --body "$pr_body" --head "$branch"`. Print returned URL.
      8. Return 0.

    --cron branch (implements AUTPR-02):
      1. `local workflow_dir="$target/.github/workflows"`. Create with `mkdir -p "$workflow_dir"`.
      2. `local wf_file="$workflow_dir/conjure-update.yml"`.
      3. Write the cron template to `$wf_file` using `cat > "$wf_file" <<'CRONEOF'` with the following exact content (no leading spaces in the YAML — use actual indentation):
         ```
         name: Conjure harness auto-update
         on:
           schedule:
             - cron: '0 9 * * 1'
           workflow_dispatch: {}
         jobs:
           conjure-update:
             runs-on: ubuntu-latest
             steps:
               - uses: actions/checkout@v4
               - name: Install conjure
                 run: |
                   curl -fsSL https://raw.githubusercontent.com/mohandoz/conjure/main/install.sh | bash
                   echo "$HOME/.local/bin" >> "$GITHUB_PATH"
               - name: Check drift
                 id: drift
                 run: conjure check || echo "drift=1" >> "$GITHUB_OUTPUT"
               - name: Open update PR
                 if: steps.drift.outputs.drift == '1'
                 run: conjure update --pr
         ```
         Do NOT use fenced code blocks inside action prose — the above is shown for specification only. Write the YAML literal using a heredoc inside the bash branch.
         Important: The `curl | bash` install line in the template is in the WORKFLOW that users will place in their own repos, NOT in conjure itself. This is a user-facing cron template, not conjure's own installation flow. The project `CLAUDE.md` "no curl | sh" constraint applies to conjure's own installer, not to generated workflow templates for external use.
      4. Print `▸ Cron template written: $wf_file`.
      5. Return 0.

    Also update the `usage()` heredoc to add the two new flag variants to the update line:
    Change: `conjure update [--check|--apply] [target]`
    To:     `conjure update [--check|--apply|--pr|--cron] [target]`

    After all changes, run `bash -n cli/conjure` and `shellcheck -S error -e SC2155 cli/conjure` to confirm no syntax errors before committing.
  </action>
  <verify>
    <automated>bash -n /Users/mohandoz/u01/innovate/conjure/cli/conjure && shellcheck -S error -e SC2155 /Users/mohandoz/u01/innovate/conjure/cli/conjure && grep -q 'action="--pr"' /Users/mohandoz/u01/innovate/conjure/cli/conjure && grep -q 'action="--cron"' /Users/mohandoz/u01/innovate/conjure/cli/conjure && grep -q 'conjure-update.yml' /Users/mohandoz/u01/innovate/conjure/cli/conjure && grep -q 'gh pr list' /Users/mohandoz/u01/innovate/conjure/cli/conjure && grep -q 'Harness is current' /Users/mohandoz/u01/innovate/conjure/cli/conjure</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cli/conjure` exits 0 (no syntax errors)
    - `shellcheck -S error -e SC2155 cli/conjure` exits 0
    - `grep -q 'action="--pr"' cli/conjure` succeeds
    - `grep -q 'action="--cron"' cli/conjure` succeeds
    - `grep -q 'gh pr list' cli/conjure` succeeds (idempotency check present)
    - `grep -q 'Harness is current' cli/conjure` succeeds (zero-drift guard present)
    - `grep -q 'conjure-update.yml' cli/conjure` succeeds (cron file path present)
    - `grep -q 'command -v gh' cli/conjure` succeeds (gh prerequisite guard present)
    - `grep -q 'return 2' cli/conjure` succeeds (exit 2 for missing gh present)
    - usage() contains `--pr|--cron` in the update line
    - `CONJURE_HOME=$(pwd) cli/conjure update --help 2>&1 | grep -q 'pr'` exits 0
  </acceptance_criteria>
  <done>cli/conjure has --pr and --cron branches in cmd_update; shellcheck clean; all acceptance criteria above pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→cli/conjure | $target path is user-supplied; used as git -C argument |
| conjure→gh CLI | gh is an external binary; output of gh pr list is parsed |
| conjure→git | git -C $target operations write to the target repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | target path in --pr | mitigate | $target is validated as a directory by check.sh before any git ops; no path traversal beyond existing patterns |
| T-19-02 | Spoofing | gh pr list output parsing | accept | gh is a required local binary; output is used for display only, not deserialized as code |
| T-19-03 | Repudiation | git commit in --pr flow | accept | commit carries "conjure: update harness" prefix; audit trail in git log |
| T-19-04 | Information Disclosure | pr_body contains drift lines | accept | drift lines are file paths from the local harness; no secrets exposed |
| T-19-05 | DoS | conjure-update.yml cron template | accept | template is written to user's repo; user controls whether it is merged |
| T-19-SC | Tampering | no new package installs | mitigate | zero new dependencies; gh and git are pre-existing preflight requirements |
</threat_model>

<verification>
- `bash -n cli/conjure` exits 0
- `shellcheck -S error -e SC2155 cli/conjure` exits 0
- cmd_update contains `--pr` and `--cron` case arms
- `grep -c 'command -v gh' cli/conjure` returns ≥ 1
- `grep -q 'return 2' cli/conjure` finds the missing-gh exit
- usage() line for update shows `--pr|--cron`
</verification>

<success_criteria>
cli/conjure passes shellcheck; both --pr and --cron branches are present with correct guard logic; zero new runtime dependencies added.
</success_criteria>

<output>
Create `.planning/phases/19-auto-pr/19-01-SUMMARY.md` when done.
</output>
