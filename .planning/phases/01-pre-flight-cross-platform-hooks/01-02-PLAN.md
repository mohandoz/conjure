---
phase: 01-pre-flight-cross-platform-hooks
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - templates/settings.json.tmpl
  - scripts/init-project.sh
  - scripts/audit-setup.sh
  - tests/run.sh
autonomous: true
requirements:
  - SAFE-03

must_haves:
  truths:
    - "A harness scaffolded by conjure init wires hooks via `node .claude/hooks/*.mjs` — hooks fire on native Windows"
    - "templates/settings.json.tmpl contains no `bash .claude/hooks/` command strings"
    - "templates/settings.json.tmpl contains all five node hook commands with correct relative paths"
    - "scripts/init-project.sh copies .mjs hooks from templates/hooks-nodejs/, not .sh hooks from templates/hooks/"
    - "scripts/audit-setup.sh checks for .mjs hook file existence, not .sh executable bit"
    - "tests/run.sh template lint assertions catch any future regression back to bash hooks"
  artifacts:
    - path: "templates/settings.json.tmpl"
      provides: "Hook wiring for 5 node .mjs hooks; no bash hook commands"
      contains: "node .claude/hooks/"
    - path: "scripts/init-project.sh"
      provides: "Hook copy loop sourcing templates/hooks-nodejs/*.mjs with no chmod +x"
      contains: "hooks-nodejs"
    - path: "scripts/audit-setup.sh"
      provides: "Hook check for .mjs file existence (not .sh executable bit)"
      contains: ".mjs"
    - path: "tests/run.sh"
      provides: "Template lint section: no-bash-hook and node-hook-present assertions"
      contains: "bash .claude/hooks"
  key_links:
    - from: "scripts/init-project.sh hook copy loop"
      to: "templates/hooks-nodejs/*.mjs"
      via: "for hook in \"$KIT\"/templates/hooks-nodejs/*.mjs"
      pattern: "hooks-nodejs.*\\.mjs"
    - from: "templates/settings.json.tmpl"
      to: ".claude/hooks/*.mjs"
      via: "\"command\": \"node .claude/hooks/foo.mjs\""
      pattern: "node .claude/hooks/"
    - from: "scripts/audit-setup.sh hook check"
      to: ".claude/hooks/*.mjs"
      via: "find .claude/hooks -maxdepth 1 -name '*.mjs'"
      pattern: "name '\\*.mjs'"
---

<objective>
Fix the live Windows hook bug (SAFE-03): replace all `bash .claude/hooks/*.sh` command strings in
templates/settings.json.tmpl with `node .claude/hooks/*.mjs`, update scripts/init-project.sh to
copy .mjs hooks (not .sh), update scripts/audit-setup.sh to check .mjs file existence, and add
template lint assertions to tests/run.sh.

Purpose: `bash .claude/hooks/*.sh` is dead on native Windows where bash is not in PATH. Replacing
with `node .claude/hooks/*.mjs` uses the Node.js runtime that Claude Code itself requires on all
platforms (D-01). No OS branching needed — single template, all platforms (D-02).

Output:
- templates/settings.json.tmpl: 4 bash commands replaced with 5 node commands (adds pre-commit-quality-gate.mjs)
- scripts/init-project.sh: hook copy loop updated from templates/hooks/*.sh to templates/hooks-nodejs/*.mjs
- scripts/audit-setup.sh: hook presence check updated from *.sh executable-bit to *.mjs file-existence
- tests/run.sh: template lint section with no-bash-hook and node-hook-present assertions
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mohandoz/u01/innovate/conjure/.planning/ROADMAP.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/01-pre-flight-cross-platform-hooks/01-CONTEXT.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/01-pre-flight-cross-platform-hooks/01-RESEARCH.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/01-pre-flight-cross-platform-hooks/01-PATTERNS.md
@/Users/mohandoz/u01/innovate/conjure/.planning/phases/01-pre-flight-cross-platform-hooks/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update templates/settings.json.tmpl and scripts/init-project.sh — node hook wiring (D-01, D-02)</name>
  <files>templates/settings.json.tmpl, scripts/init-project.sh</files>
  <read_first>
    - templates/settings.json.tmpl lines 41-88: entire current hooks block — read before any edit
    - templates/hooks-nodejs/README.md: canonical node command format (no args; hooks read process.env directly)
    - scripts/init-project.sh lines 45-53: current .sh hook copy loop — read before editing
    - templates/hooks-nodejs/ directory listing: confirm all 5 .mjs files exist before writing
  </read_first>
  <behavior>
    - templates/settings.json.tmpl: grep for 'bash .claude/hooks/' returns no matches
    - templates/settings.json.tmpl: contains exactly 5 node hook command strings (node .claude/hooks/*.mjs)
    - templates/settings.json.tmpl: PostToolUse[Edit|Write|MultiEdit] wires post-edit-format.mjs
    - templates/settings.json.tmpl: PreToolUse[Bash] wires pre-bash-block-destructive.mjs AND pre-commit-quality-gate.mjs
    - templates/settings.json.tmpl: Stop wires stop-compound-engineering.mjs
    - templates/settings.json.tmpl: SessionStart wires session-start-context.mjs
    - templates/settings.json.tmpl: no arg strings like "$CLAUDE_FILE_PATH" in node hook commands
    - templates/settings.json.tmpl: valid JSON (jq empty returns exit 0)
    - scripts/init-project.sh: hook copy loop sources "$KIT"/templates/hooks-nodejs/*.mjs (not templates/hooks/*.sh)
    - scripts/init-project.sh: no chmod +x in hook copy loop
    - scripts/init-project.sh: idempotency guard [ ! -f ".claude/hooks/$name" ] preserved
  </behavior>
  <action>
    In templates/settings.json.tmpl:

    Back up current file before editing (cp templates/settings.json.tmpl templates/settings.json.tmpl.bak
    — per backup-before-mutate constraint from CLAUDE.md). Delete the backup after confirming JSON validity.

    Replace the entire "hooks" object (lines 41-84) with the following structure.
    Preserve all indentation (2-space) and surrounding keys (_comment_hooks, env) unchanged.

    The new hooks object has four event types:
    - "PostToolUse": array containing one object with matcher "Edit|Write|MultiEdit" and one hook:
        { "type": "command", "command": "node .claude/hooks/post-edit-format.mjs" }
    - "PreToolUse": array containing one object with matcher "Bash" and TWO hooks:
        { "type": "command", "command": "node .claude/hooks/pre-bash-block-destructive.mjs" }
        { "type": "command", "command": "node .claude/hooks/pre-commit-quality-gate.mjs" }
      Note: pre-commit-quality-gate.mjs is NEW — absent from the current template. The .mjs file
      guards itself internally with `if (!/^git\s+commit/.test(cmd)) process.exit(0)` so it is safe
      to wire unconditionally (confirmed in RESEARCH.md §Open Questions item 1).
    - "Stop": array containing one object with no matcher and one hook:
        { "type": "command", "command": "node .claude/hooks/stop-compound-engineering.mjs" }
    - "SessionStart": array containing one object with no matcher and one hook:
        { "type": "command", "command": "node .claude/hooks/session-start-context.mjs" }

    Do NOT include arg strings like "$CLAUDE_FILE_PATH" or "$CLAUDE_COMMAND" — node hooks read
    process.env.CLAUDE_FILE_PATH and process.env.CLAUDE_COMMAND directly. Passing them as args
    is a pitfall documented in RESEARCH.md §Pitfall 3 and §Anti-Patterns.

    After editing, verify JSON validity: jq empty templates/settings.json.tmpl (must exit 0).
    Remove the .bak file after successful validation.

    In scripts/init-project.sh:

    Read lines 45-53 (the hook copy loop). Replace the loop with a new block that:
    - Iterates over "$KIT"/templates/hooks-nodejs/*.mjs (not templates/hooks/*.sh)
    - Uses name=$(basename "$hook") — unchanged
    - Preserves the idempotency guard: if [ ! -f ".claude/hooks/$name" ]; then
    - Copies with: cp "$hook" ".claude/hooks/$name"
    - Prints: echo "  ✓ created .claude/hooks/$name"
    - Does NOT include any chmod +x line — .mjs files are invoked via `node file.mjs`, not as executables
    - Section comment should read: "# 4. Copy hooks (node .mjs — works on all platforms including Windows)"

    Also: check lines 14-17 for the [[ ]] bash test — the MODE check `[[ "$MODE" != "new" && "$MODE" != "existing" ]]` uses bash 4-ish syntax but is fine on bash 3.2 (double brackets are a bash extension, not a bash 4 feature). Leave it unchanged.

    Do not modify any other sections of init-project.sh.
  </action>
  <verify>
    <automated>jq empty templates/settings.json.tmpl && bash tests/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'bash .claude/hooks/' templates/settings.json.tmpl returns 0
    - grep -c 'node .claude/hooks/' templates/settings.json.tmpl returns 5
    - jq '.hooks.PreToolUse[0].hooks | length' templates/settings.json.tmpl returns 2 (two PreToolUse[Bash] hooks)
    - jq empty templates/settings.json.tmpl exits 0 (valid JSON)
    - grep -q 'pre-commit-quality-gate.mjs' templates/settings.json.tmpl is true
    - grep -q 'hooks-nodejs' scripts/init-project.sh is true
    - grep -q 'chmod' scripts/init-project.sh returns false (no chmod in hook copy block)
    - grep -q 'templates/hooks/' scripts/init-project.sh is false (no .sh hook copy remaining)
    - bash tests/run.sh exits 0 (JSON validity test in test suite still passes)
    - templates/settings.json.tmpl.bak does not exist (cleaned up after validation)
  </acceptance_criteria>
  <done>templates/settings.json.tmpl wires all 5 node .mjs hooks with no bash commands; scripts/init-project.sh copies .mjs hooks from hooks-nodejs/ without chmod; both files pass tests</done>
</task>

<task type="auto">
  <name>Task 2: Update scripts/audit-setup.sh hook check + add template lint assertions to tests/run.sh (SAFE-03)</name>
  <files>scripts/audit-setup.sh, tests/run.sh</files>
  <read_first>
    - scripts/audit-setup.sh lines 84-106: full hooks section including the current *.sh hook check at lines 96-103
    - scripts/audit-setup.sh lines 14-17: ok()/warn()/err() helpers — reuse these, do not redefine
    - tests/run.sh lines 76-81: grep-based lint assertion pattern (no @imports check) — copy this pattern
    - tests/run.sh lines 93-101: audit self-test section — the preflight section was inserted after this; the template lint section goes after the preflight section (added in Plan 01-01)
    - tests/run.sh current end of file: confirm where to insert without disrupting existing structure
  </read_first>
  <action>
    In scripts/audit-setup.sh:

    Replace the hook check block (lines 96-103). The current block:
      while IFS= read -r hook; do
        if [ -x "$hook" ]; then ok "Hook executable: $(basename "$hook")"
        else err "Hook NOT executable: $(basename "$hook") — run chmod +x"
        fi
      done < <(find .claude/hooks -maxdepth 1 -name '*.sh')

    Replace with the .mjs equivalent:
      while IFS= read -r hook; do
        if [ -f "$hook" ]; then ok "Hook present: $(basename "$hook")"
        else err "Hook MISSING: $(basename "$hook") — re-run conjure init"
        fi
      done < <(find .claude/hooks -maxdepth 1 -name '*.mjs')

    Key changes:
    - name '*.sh' → name '*.mjs'
    - -x "$hook" (executable bit check) → -f "$hook" (file existence check)
    - "Hook executable:" → "Hook present:"
    - error message updated from "chmod +x" to "re-run conjure init"
    Do not modify any other line in audit-setup.sh.

    In tests/run.sh:

    Add a new "Template lint" section. Insert it after the preflight section that Plan 01-01 added
    and before the "Migration coverage" section. If the preflight section is at the end of the
    existing "Audit script self-test" block, find the "Migration coverage" echo line and insert before it.

    Follow the exact section pattern from tests/run.sh (echo section header, assertions with pass/fail):

    echo
    echo "▸ Template lint"

    Assertion 1 — no bash hooks in settings.json.tmpl:
    if grep -q 'bash .claude/hooks/' templates/settings.json.tmpl 2>/dev/null; then
      fail "settings.json.tmpl: bash hook commands present (SAFE-03 regression)"
    else pass "settings.json.tmpl: no bash hook commands"
    fi

    Assertion 2 — node hooks present in settings.json.tmpl:
    if grep -q 'node .claude/hooks/' templates/settings.json.tmpl 2>/dev/null; then
      pass "settings.json.tmpl: node hook commands present"
    else fail "settings.json.tmpl: node hook commands MISSING"
    fi

    Assertion 3 — init-project.sh sources .mjs hooks, not .sh:
    if grep -q 'hooks-nodejs' scripts/init-project.sh 2>/dev/null; then
      pass "init-project.sh: sources hooks-nodejs (.mjs)"
    else fail "init-project.sh: does not source hooks-nodejs (SAFE-03 regression)"
    fi

    Assertion 4 — init-project.sh has no chmod in hook copy block:
    if grep -v '^#' scripts/init-project.sh 2>/dev/null | grep -q 'chmod.*hooks'; then
      fail "init-project.sh: chmod found in hook block (should not chmod .mjs files)"
    else pass "init-project.sh: no chmod on hook files"
    fi

    Note for assertion 4: use `grep -v '^#'` to filter comment lines before checking for chmod,
    so that a comment mentioning chmod does not trigger a false positive.
  </action>
  <verify>
    <automated>bash tests/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/audit-setup.sh: grep -q "name '*.mjs'" scripts/audit-setup.sh is true
    - scripts/audit-setup.sh: grep -q "name '*.sh'" scripts/audit-setup.sh is false (old .sh check removed)
    - scripts/audit-setup.sh: grep -q "\-f \"\$hook\"" scripts/audit-setup.sh is true (file-existence check)
    - scripts/audit-setup.sh: grep -q "chmod" scripts/audit-setup.sh is false (no chmod reference)
    - tests/run.sh: grep -q "Template lint" tests/run.sh is true
    - tests/run.sh: grep -q "bash hook commands present" tests/run.sh is true
    - tests/run.sh: grep -q "node hook commands present" tests/run.sh is true
    - tests/run.sh: grep -q "sources hooks-nodejs" tests/run.sh is true
    - bash tests/run.sh exits 0 (all tests pass including 4 new template lint assertions)
    - bash scripts/audit-setup.sh "$CONJURE_HOME" exits with rc 0, 1, or 2 (does not crash)
  </acceptance_criteria>
  <done>audit-setup.sh checks .mjs file existence instead of .sh executable bit; tests/run.sh template lint section catches any SAFE-03 regression; full test suite exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates/settings.json.tmpl → Claude Code runtime | JSON is read by Claude Code on startup; malformed JSON silently disables all hooks |
| scripts/init-project.sh → target repo .claude/hooks/ | Copies .mjs files from kit; target is user-controlled directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | settings.json.tmpl edited to invalid JSON | mitigate | Task 1 acceptance criteria require jq empty validates the file before marking done; test suite runs JSON validity check |
| T-02-02 | Denial of Service | hooks silently no-op on Windows (original bug) | mitigate | D-01: replace bash with node; test lint assertions in tests/run.sh catch regression |
| T-02-03 | Tampering | init-project.sh copies .sh hooks while settings.json expects .mjs | mitigate | Task 1 acceptance criteria assert grep -q 'hooks-nodejs' and grep absence of 'templates/hooks/' in copy loop; lint assertion in tests/run.sh enforces this |
| T-02-04 | Elevation of Privilege | .mjs hook granted execute permission via chmod +x | accept | .mjs files must NOT be executable; invoked via `node file.mjs` only; no chmod in updated init-project.sh; lint assertion enforces absence of chmod in hook block |
| T-02-05 | Information Disclosure | arg-passing $CLAUDE_FILE_PATH leaks shell expansion into hook command | mitigate | node commands have no args in template; RESEARCH.md Pitfall 3 documents this; acceptance criteria verify no arg strings in commands |
  T-02-SC | Tampering | npm/pip/cargo installs during implementation | mitigate | Phase 1 installs zero new packages; RESEARCH.md §Package Legitimacy Audit: "not applicable" |
</threat_model>

<verification>
Full: bash tests/run.sh (exits 0 — all existing + template lint assertions pass)
JSON: jq empty templates/settings.json.tmpl (exits 0)
Audit: bash scripts/audit-setup.sh "$CONJURE_HOME" (exits 0/1/2, not crash)
Regression: bash scripts/init-project.sh existing /tmp/test-conjure-init && ls /tmp/test-conjure-init/.claude/hooks/*.mjs
</verification>

<success_criteria>
- templates/settings.json.tmpl has zero `bash .claude/hooks/` strings and five `node .claude/hooks/` strings
- pre-commit-quality-gate.mjs is wired as a second PreToolUse[Bash] hook
- scripts/init-project.sh copies from templates/hooks-nodejs/*.mjs, no chmod, idempotency guard preserved
- scripts/audit-setup.sh checks .mjs existence, not .sh executable bit
- tests/run.sh template lint section prevents SAFE-03 regression in future phases
- bash tests/run.sh exits 0 with all assertions green
</success_criteria>

<output>
Create `.planning/phases/01-pre-flight-cross-platform-hooks/01-02-SUMMARY.md` when done.
</output>
