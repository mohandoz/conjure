---
phase: 30-workspace-orchestration-mutating-rollback-saga
plan: "01"
type: execute
wave: 1
depends_on:
  - "30-00"
files_modified:
  - lib/workspace.sh
autonomous: true
requirements:
  - WS-06
  - WS-07

must_haves:
  truths:
    - "workspace_state_write creates .conjure-workspace-state.json atomically (jq > tmp.$$ && mv) on first write and updates it on subsequent writes"
    - "workspace_state_read reads a jq expression from the state file, returning empty string on missing file (never exits 2 on read)"
    - "State file survives kill -9: atomic tmp+mv means no partial JSON on disk"
    - "workspace_state_validate exits 2 with message when state file is absent, malformed JSON, or missing required fields"
  artifacts:
    - path: "lib/workspace.sh"
      provides: "workspace_state_write, workspace_state_read, workspace_state_validate helpers"
      contains: "workspace_state_write"
  key_links:
    - from: "lib/workspace.sh workspace_state_write"
      to: ".conjure-workspace-state.json"
      via: "jq > tmp.$$ && mv (same-dir atomic rename)"
      pattern: "workspace-state.json.tmp"
    - from: "scripts/workspace.sh ws_do_adopt"
      to: "lib/workspace.sh workspace_state_write"
      via: "sourced at script startup"
      pattern: "workspace_state_write"
---

<objective>
Wave 1: lib/workspace.sh state helpers — atomic read/write for the workspace saga state machine.

Extend lib/workspace.sh with workspace_state_write (atomic jq>tmp+mv, create-vs-update branches), workspace_state_read (safe jq query with // "" default), and workspace_state_validate (schema presence check). These are the building blocks for every per-repo state transition in waves 3 and 4.

Purpose: Establish the SIGKILL-durable state primitives before the orchestrator uses them. Every saga invariant depends on atomic state writes — this plan delivers nothing else.

Output: lib/workspace.sh extended with 3 new functions; existing functions (workspace_manifest_validate, workspace_manifest_load, workspace_discover_siblings) are unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-workspace-orchestration-mutating-rollback-saga/30-CONTEXT.md
@.planning/phases/30-workspace-orchestration-mutating-rollback-saga/30-PATTERNS.md
@lib/workspace.sh
@scripts/adopt.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workspace_state_write / workspace_state_read / workspace_state_validate to lib/workspace.sh</name>
  <files>lib/workspace.sh</files>
  <read_first>
    1. lib/workspace.sh full file — current 160 lines; append after workspace_discover_siblings; do NOT modify any existing function
    2. scripts/adopt.sh lines 76–101 (state_record) — the atomic jq>tmp+mv pattern to mirror exactly
    3. scripts/adopt.sh lines 63–73 (sha_of) — note this lives in adopt.sh not workspace.sh; do not copy it here (it belongs in ws_do_adopt inline or a separate ws_sha_of helper added in Wave 3)
    4. 30-CONTEXT.md "Saga State Machine (WS-06)" — the JSON schema: { run_id, started, phase: "snapshot"|"apply"|"done", repos: [ { name, snapshot_ref, sha256_pre, status: "pending"|"snapshotted"|"applied"|"failed"|"rolled_back" } ] }
    5. 30-PATTERNS.md "lib/workspace.sh — workspace_state_write" section — exact diff notes: single JSON file (not directory), tmp path must be same-dir (.conjure-workspace-state.json.tmp.$$), jq -n on first write, jq filter on existing file on update
  </read_first>
  <behavior>
    - workspace_state_write <state_path> <jq_filter> [jq_args...]: if state_path does NOT exist → jq -n + filter → tmp.$$ → mv; if state_path EXISTS → jq filter state_path → tmp.$$ → mv; on jq failure → rm -f tmp; return 2 with error to stderr
    - workspace_state_read <state_path> <jq_expr>: jq -r "$jq_expr // empty" state_path 2>/dev/null || true — prints to stdout; never exits non-zero
    - workspace_state_validate <state_path>: exits 0 if file exists + is valid JSON + has run_id + has phase + has repos array; exits 2 with message to stderr otherwise
    - All three functions use POSIX bash 3.2+ (no local -n, no associative arrays, no mapfile)
    - workspace_state_write tmp path: "${state_path}.tmp.$$" (same directory as state file — guaranteed atomic mv on POSIX)
    - workspace_state_validate must not exit 2 for an absent-but-expected file during rollback; that check lives in ws_do_rollback. workspace_state_validate returns 2 with "no state file" message when file is absent — callers decide how to handle
  </behavior>
  <action>
    Append three new functions to lib/workspace.sh after the final workspace_discover_siblings function. Add a comment block separator "# ── workspace saga state helpers (Phase 30) ───────────────────────────────────".

    workspace_state_write: Signature workspace_state_write <state_path> <jq_filter> [jq_args...]. Local var tmp="${state_path}.tmp.$$". Two branches: if -f "$state_path" exists → jq with remaining args + filter on state_path → tmp; else → jq -n with remaining args + filter → tmp. On success: mv tmp to state_path. On failure: rm -f tmp; echo error to stderr; return 2. Mirror adopt.sh state_record lines 80–101 exactly except: no STATE_DIR mkdir (workspace state is a flat file); state_path is a parameter not a module-level variable.

    workspace_state_read: Signature workspace_state_read <state_path> <jq_expr>. One-liner: jq -r "${jq_expr} // empty" "$state_path" 2>/dev/null || true. Callers use this to safely extract .phase, .repos[].status, etc. Never exits non-zero.

    workspace_state_validate: Signature workspace_state_validate <state_path>. Check: -f state_path (else return 2 "no workspace state file"); jq empty (else return 2 "invalid JSON"); jq -e '.run_id' (else return 2 "missing run_id"); jq -e '.phase' (else return 2 "missing phase"); jq -e '.repos | type == "array"' (else return 2 "missing repos array"). Return 0 on all checks pass. All error messages to stderr.

    POSIX 3.2+ constraint: no local -n, no {1..N} brace expansion in functions, no mapfile, no associative arrays. Use local for all function-scoped variables.

    Do NOT touch the existing workspace_manifest_validate, workspace_manifest_load, or workspace_discover_siblings functions.
  </action>
  <verify>
    <automated>
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 lib/workspace.sh &&
      bash tests/run.sh 2>&1 | grep -E "WS-06|WS-07|STATE" | head -20
    </automated>
  </verify>
  <done>
    lib/workspace.sh has workspace_state_write, workspace_state_read, workspace_state_validate appended after existing functions. shellcheck passes. bash tests/run.sh: Phase 29 tests still green; Phase 30 state-helper tests advance (no longer MISSING — note: full green requires Waves 3+4 feature code).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jq filter input → state file | jq filters are written by the orchestrator (ws_do_adopt/ws_do_rollback), not from user input; low injection risk |
| state_path → filesystem | state_path is constructed from WORKSPACE_ROOT (validated by workspace_manifest_validate); traversal boundary already enforced before state is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Tampering | .conjure-workspace-state.json (partial write on crash) | mitigate | jq > tmp.$$ + mv atomic rename; SIGKILL between jq and mv leaves tmp.$$ orphan (not the live state file); _ws_cleanup removes tmp on EXIT |
| T-30-01-02 | Information Disclosure | state file readable by any local user | accept | Low-value target (repo paths + status); no secrets or credentials in state schema |
| T-30-01-03 | Tampering | jq filter injection via state_path argument | accept | state_path is workspace-root-relative, validated before this call; no user-controlled filter strings |
| T-30-01-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
After task completes:

1. `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 lib/workspace.sh` — exits 0
2. `grep -c 'workspace_state_write\|workspace_state_read\|workspace_state_validate' lib/workspace.sh` — count ≥ 3 function definitions
3. Atomic write smoke test (inline): `bash -c 'source lib/workspace.sh; f=$(mktemp); workspace_state_write "$f" '"'"'{run_id:"r1",started:"t",phase:"snapshot",repos:[]}'"'"' && jq -r .phase "$f"'` → prints "snapshot"
4. `bash tests/run.sh` — Phase 29 tests remain green; Phase 30 block shows partial progress (state helper tests may pass if wired in the test block)
</verification>

<success_criteria>
- lib/workspace.sh has 3 new functions: workspace_state_write (atomic jq>tmp+mv, create/update branches), workspace_state_read (safe jq query), workspace_state_validate (schema check)
- shellcheck exits 0 on lib/workspace.sh
- Existing Phase 29 tests (manifest validate/load/discover) remain green
- State write is atomic: tmp path is same-dir, mv is the only way state_path is replaced
</success_criteria>

<output>
Create `.planning/phases/30-workspace-orchestration-mutating-rollback-saga/30-01-SUMMARY.md` when done.
</output>
