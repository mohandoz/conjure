---
phase: 30-workspace-orchestration-mutating-rollback-saga
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/_workspace-trio/.conjure-workspace.json
  - tests/fixtures/_workspace-trio/repos/alpha/CLAUDE.md
  - tests/fixtures/_workspace-trio/repos/alpha/.claude/skills/git/SKILL.md
  - tests/fixtures/_workspace-trio/repos/beta/CLAUDE.md
  - tests/fixtures/_workspace-trio/repos/beta/.claude/skills/docs/SKILL.md
  - tests/fixtures/_workspace-trio/repos/gamma/CLAUDE.md
  - tests/fixtures/_workspace-trio/repos/gamma/.claude/hooks/post-tool.mjs
  - tests/run.sh
autonomous: true
requirements:
  - WS-05
  - WS-06
  - WS-07

must_haves:
  truths:
    - "_workspace-trio fixture exists with 3 adoptable repos (alpha/beta/gamma) each containing .claude/ and content files"
    - "Phase 30 graceful-red block in tests/run.sh: WS-05, WS-06, WS-07, and SAGA sections are all present and fail with MISSING messages"
    - "SIGKILL bounded-poll test block is written (mirrors Phase 24 pattern), polls .conjure-workspace-state.json .phase field, fails gracefully when workspace.sh absent"
    - "bash tests/run.sh returns non-zero before any feature code is written"
  artifacts:
    - path: "tests/fixtures/_workspace-trio/.conjure-workspace.json"
      provides: "3-repo workspace manifest (alpha/beta/gamma, tags included for --tag filter tests)"
    - path: "tests/fixtures/_workspace-trio/repos/alpha/.claude/skills/git/SKILL.md"
      provides: "adoptable harness content — gives adopt real mutations to apply"
    - path: "tests/fixtures/_workspace-trio/repos/gamma/.claude/hooks/post-tool.mjs"
      provides: "adoptable hook file — ensures snapshot_create has real .claude/ content to capture"
    - path: "tests/run.sh"
      provides: "Phase 30 graceful-red block appended"
  key_links:
    - from: "tests/run.sh Phase 30 block"
      to: "tests/fixtures/_workspace-trio"
      via: "P30_WS_TRIO fixture path variable"
      pattern: "_workspace-trio"
    - from: "SIGKILL poll loop"
      to: ".conjure-workspace-state.json .phase field"
      via: "jq -r '.phase // \"\"'"
      pattern: "conjure-workspace-state"
---

<objective>
Wave 0: Nyquist fixture + graceful-red tests before any saga feature code.

Create the `_workspace-trio` fixture (3 small adoptable repos with real .claude/ content) and append the Phase 30 graceful-red test block to tests/run.sh. All WS-05, WS-06, WS-07, and SAGA test sections must be present and fail (MISSING / not implemented messages) before any saga feature code exists. This is the Nyquist invariant for Phase 30.

Purpose: Prove that the test infrastructure exists and gates correctly on the feature code it will verify, preventing silent misses where a test never runs because a fixture or block is absent.

Output: `tests/fixtures/_workspace-trio/` (3-repo adoptable fixture) + Phase 30 block appended to `tests/run.sh` (graceful-red).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-workspace-orchestration-mutating-rollback-saga/30-CONTEXT.md
@.planning/phases/30-workspace-orchestration-mutating-rollback-saga/30-PATTERNS.md
@.planning/phases/30-workspace-orchestration-mutating-rollback-saga/30-VALIDATION.md

Existing fixture to extend from (structure reference):
@tests/fixtures/_workspace/.conjure-workspace.json

Phase 24 SIGKILL pattern to mirror (lines 3470-3564):
@tests/run.sh

Phase 22 rollback + per-file sha256 pattern (lines 2575-2633):
@tests/run.sh

Phase 29 test block pattern (line 5732+):
@tests/run.sh

Adoptable harness content reference:
@tests/fixtures/_brownfield-simple

Phase 29 guard pattern to mirror for WS-05/06/07 graceful-red:
@scripts/workspace.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _workspace-trio fixture (3 adoptable repos + manifest)</name>
  <files>
    tests/fixtures/_workspace-trio/.conjure-workspace.json
    tests/fixtures/_workspace-trio/repos/alpha/CLAUDE.md
    tests/fixtures/_workspace-trio/repos/alpha/.claude/settings.json
    tests/fixtures/_workspace-trio/repos/alpha/.claude/skills/git/SKILL.md
    tests/fixtures/_workspace-trio/repos/beta/CLAUDE.md
    tests/fixtures/_workspace-trio/repos/beta/.claude/settings.json
    tests/fixtures/_workspace-trio/repos/beta/.claude/skills/docs/SKILL.md
    tests/fixtures/_workspace-trio/repos/gamma/CLAUDE.md
    tests/fixtures/_workspace-trio/repos/gamma/.claude/settings.json
    tests/fixtures/_workspace-trio/repos/gamma/.claude/hooks/post-tool.mjs
  </files>
  <read_first>
    1. tests/fixtures/_workspace/.conjure-workspace.json — existing 3-repo manifest schema (alpha/beta/gamma, schema_version:1, tags:[])
    2. tests/fixtures/_workspace/repos/alpha/CLAUDE.md — minimal CLAUDE.md content that passes conjure check
    3. 30-PATTERNS.md section "_workspace-trio fixture" — what must be added over the existing _workspace fixture (adoptable content, no pre-existing .conjure-adopt-state, small enough for fast sha256)
    4. 30-CONTEXT.md decisions section — --tag filter uses manifest tags[]; alpha/beta get tags:["team-a"], gamma gets tags:["team-b"] so --tag tests have material to filter
  </read_first>
  <action>
    Create tests/fixtures/_workspace-trio/ directory tree. The manifest at .conjure-workspace.json must follow the schema_version:1 format from the existing _workspace fixture. repos[] must contain alpha (tags:["team-a"]), beta (tags:["team-a"]), gamma (tags:["team-b"]) with paths repos/alpha, repos/beta, repos/gamma.

    Each repo must be ADOPTABLE — it needs enough .claude/ content that snapshot_create has real work to do and adopt has real mutations to apply:

    - repos/alpha: CLAUDE.md (10–15 lines covering project context + a rule), .claude/settings.json (minimal {"version":1}), .claude/skills/git/SKILL.md (valid frontmatter: id: git, description: Git workflow skill, tools: [Bash], at least 10 lines of skill content). Alpha gets tags:["team-a"] in the manifest.

    - repos/beta: CLAUDE.md, .claude/settings.json, .claude/skills/docs/SKILL.md (id: docs, description: Documentation skill, tools: [Read,Write]). Beta gets tags:["team-a"].

    - repos/gamma: CLAUDE.md, .claude/settings.json, .claude/hooks/post-tool.mjs (a minimal ESM hook: export default function(event){} — must NOT be empty so snapshot_create captures it). Gamma gets tags:["team-b"].

    IMPORTANT: No repo should contain a pre-existing .conjure-adopt-state/ directory — adopt must run the full pipeline on each test. No node_modules, no .git objects (fixture dirs are not git repos — tests create mktemp copies). Keep each repo to 5–10 files maximum so sha256 hashing completes in milliseconds.

    The CLAUDE.md files must be ≤100 lines (project convention). The SKILL.md files must have valid YAML frontmatter with id, description, and tools fields. The hook must be valid ESM (.mjs).
  </action>
  <verify>
    <automated>
      find tests/fixtures/_workspace-trio -type f | sort | wc -l | grep -E '^[0-9]+$' &&
      jq '.repos | length' tests/fixtures/_workspace-trio/.conjure-workspace.json | grep -q '^3$' &&
      jq -r '.repos[].tags[]' tests/fixtures/_workspace-trio/.conjure-workspace.json | sort | uniq | grep -E 'team-a|team-b' &&
      jq empty tests/fixtures/_workspace-trio/.conjure-workspace.json
    </automated>
  </verify>
  <done>
    _workspace-trio/ exists with .conjure-workspace.json (3 repos, tags:team-a/team-b), each repo has .claude/ directory with content files (SKILL.md or hook), no .conjure-adopt-state present, no node_modules or .git objects, all files are small (sha256 in milliseconds).
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 30 graceful-red test block to tests/run.sh</name>
  <files>
    tests/run.sh
  </files>
  <read_first>
    1. tests/run.sh lines 5732–5747 — Phase 29 block header pattern: how P29_WS_LIB_OK / P29_WS_SH_OK guard variables are set, echo header pattern
    2. tests/run.sh lines 3470–3564 — Phase 24 SIGKILL block: exact background-launch + bounded-poll + kill-9 + relaunch-loop pattern to mirror at workspace scale
    3. tests/run.sh lines 2575–2633 — Phase 22 rollback + per-file sha256 loop
    4. 30-PATTERNS.md section "tests/run.sh — Phase 30 SIGKILL saga block" — the workspace-scale SIGKILL polling uses .phase field of .conjure-workspace-state.json (not .current_step)
    5. 30-VALIDATION.md Per-Task Verification Map — the 6 test IDs (30-00-01 through 30-04-02) and their correct behavior
    6. tail of tests/run.sh — where to append (before the final Summary block: "Clean up any gh-hiding stub dirs")
  </read_first>
  <action>
    Append the Phase 30 test block to tests/run.sh BEFORE the "Clean up any gh-hiding stub dirs" + Summary lines at the very end. The appended block must:

    Guard variables:
    - P30_WS_SH="$CONJURE_HOME/scripts/workspace.sh" (already exists from Phase 29)
    - P30_WS_TRIO="$CONJURE_HOME/tests/fixtures/_workspace-trio"
    - P30_WS_TRIO_OK=0; [ -d "$P30_WS_TRIO" ] && P30_WS_TRIO_OK=1
    - P30_WS_SH_OK=0; [ -f "$P30_WS_SH" ] && P30_WS_SH_OK=1 (reuse P29_WS_SH_OK value if already set, or re-derive)
    - P30_SHA helper function: p30_sha() — cross-platform sha256 identical to p22_sha but namespaced p30 (copy sha_of from adopt.sh lines 63–73 pattern)

    Define a p30_ws_adopt() helper that invokes scripts/workspace.sh adopt with CONJURE_HOME set, mirroring p22_adopt() from Phase 22.

    Section header: echo "▸ Phase 30 — Workspace Orchestration — Mutating + Rollback Saga (WS-05..07)"

    WS-05 update block: "▸ Phase 30 — workspace update (WS-05)"
    - Guard on P30_WS_SH_OK; fail with MISSING message if absent
    - Test: invoke `bash "$P30_WS_SH" update "$P30_WS_TRIO/.conjure-workspace.json"` from a mktemp copy of _workspace-trio; expect exit code 2 (not implemented) OR a graceful error; the fail message must say "workspace update not implemented — Wave 2 must add ws_do_update (WS-05)"
    - This test turns green after Wave 2

    WS-06 saga block: "▸ Phase 30 — workspace adopt saga invariant + dry-run + du gate (WS-06)"
    - Guard on P30_WS_SH_OK and P30_WS_TRIO_OK; fail MISSING if absent
    - Sub-test WS-06-SAGA-INVARIANT: adopt on _workspace-trio copy → assert state file shows all repos reach "snapshotted" before first "applied" (jq query: `.repos | map(select(.status == "applied")) | length > 0` must be false until all snapshotted); graceful-red: fail "workspace adopt saga invariant not verifiable — Wave 3 must implement ws_do_adopt (WS-06)"
    - Sub-test WS-06-DRY-RUN: adopt --dry-run → zero files written (no state file, no snapshot dirs, no repo mutations), exits 0; graceful-red: fail "workspace adopt --dry-run not implemented (WS-06)"
    - Sub-test WS-06-DU-GATE: stub du via PATH shim that echoes "2200000\t/path" to simulate >2 GiB; adopt without --allow-large-snapshots → exit 2; with --allow-large-snapshots → proceeds; graceful-red: fail "workspace adopt du gate not implemented (WS-06)"

    WS-07 rollback block: "▸ Phase 30 — workspace adopt --rollback (WS-07)"
    - Guard on P30_WS_SH_OK and P30_WS_TRIO_OK; fail MISSING if absent
    - Sub-test WS-07-NO-STATE: adopt --rollback with no .conjure-workspace-state.json → exit 2 (nothing to roll back); graceful-red
    - Sub-test WS-07-IDEMPOTENT: after rollback, second --rollback → exit 0 no-op; graceful-red
    - Sub-test WS-07-ARCHIVED: after rollback, .conjure-workspace-state.json is gone (archived with timestamp); graceful-red

    SAGA SIGKILL block: "▸ Phase 30 — SAGA SIGKILL zero-diff (WS-07 / criterion 4)"
    - This is the phase-defining test. Mirror Phase 24 SIGKILL pattern exactly but at workspace scale.
    - Setup: mktemp copy of _workspace-trio (P30_SK_WS_ROOT); record per-repo sha256 pre-run hash files (one per repo, outside the tree); PRE_COPY mktemp dir.
    - Relaunch loop (up to 3 attempts): background-launch `bash "$P30_WS_SH" adopt "$P30_SK_WS_ROOT/.conjure-workspace.json"`; bounded poll (200 × 0.05s) reading jq -r '.phase // ""' from "$P30_SK_WS_ROOT/.conjure-workspace-state.json"; break when phase == "apply" (meaning snapshot phase is done, at least 1 repo snapshotted, no repo applied yet — this is the kill window per CONTEXT.md); kill -9; wait; check P30_SK_INWINDOW.
    - After kill: run `bash "$P30_WS_SH" adopt --rollback "$P30_SK_WS_ROOT/.conjure-workspace.json"` (exit must be 0 or note partial).
    - Per-repo sha256 zero-diff assertion: for each repo, compare recorded pre-run hashes to post-rollback state; P30_SK_MISMATCH count; pass if 0.
    - diff -r zero-diff per repo (excl. .conjure-adopt-backups, .conjure-archive-*, .conjure-adopt-state, RESTRUCTURE-LOG.md, adopt-manifest.json, .conjure-workspace-state*.json).
    - Guard on P30_WS_SH_OK and P30_WS_TRIO_OK; fail "SAGA SIGKILL not testable — Wave 4 must implement --rollback (WS-07/criterion 4)" if either absent.

    All test sections must be graceful-red: use the fail() function (not exit) so the test suite continues and the full set of MISSING failures is reported. Every fail() message must include the requirement ID (WS-05, WS-06, WS-07) and the wave that will fix it.

    POSIX 3.2+ constraint: use $(seq 1 200) not {1..200}; use IFS= read -r; no mapfile; no associative arrays.

    Do NOT add a new `trap ... EXIT` at the script level — use the existing pattern of per-section `trap 'rm -rf ...' EXIT` + `trap - EXIT` pairs as used throughout Phase 22/24/29.
  </action>
  <verify>
    <automated>
      bash tests/run.sh 2>&1 | grep -c "Phase 30" | grep -qE '^[1-9]' &&
      bash tests/run.sh 2>&1 | grep -E "WS-05|WS-06|WS-07|SAGA" | grep -c "✗" | grep -qE '^[1-9]' &&
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh
    </automated>
  </verify>
  <done>
    Phase 30 block appended to tests/run.sh. All WS-05, WS-06, WS-07, and SAGA test sections present. Before saga code exists: bash tests/run.sh shows multiple "✗" failures for the Phase 30 block with MISSING/not-implemented messages. shellcheck passes. bash tests/run.sh exits non-zero.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture content → test harness | Fixture CLAUDE.md/SKILL.md content is static; no user input crosses this boundary |
| test runner → shell | tests/run.sh invokes scripts as subprocesses; PATH shim for du stub must be scoped to the sub-test, not leak into other tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-00-01 | Tampering | du PATH shim (WS-06 du gate test) | mitigate | Shim is scoped to a sub-shell or localised PATH= prefix; cleaned up with trap; never affects the real du in other tests |
| T-30-00-02 | Denial of Service | SIGKILL poll loop (200 × 0.05s) | accept | 10s max wait per attempt × 3 = 30s ceiling; bounded by design; acceptable in CI |
| T-30-00-03 | Tampering | _workspace-trio fixture content | accept | Static fixture, checked into git; no runtime-generated secrets or credentials |
| T-30-00-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `find tests/fixtures/_workspace-trio -type f | sort` — lists all fixture files (expect 10–15 files across 3 repos)
2. `jq . tests/fixtures/_workspace-trio/.conjure-workspace.json` — valid JSON, 3 repos, tags fields present
3. `bash tests/run.sh 2>&1 | grep -E "Phase 30|WS-05|WS-06|WS-07|SAGA"` — Phase 30 block present with FAIL marks
4. `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` — exits 0
5. `bash tests/run.sh` exits non-zero (Phase 30 tests fail as expected in graceful-red state)
</verification>

<success_criteria>
- tests/fixtures/_workspace-trio/ exists with 3 repos, valid manifest, adoptable .claude/ content
- tests/run.sh Phase 30 block covers WS-05, WS-06, WS-07, and SAGA sections
- All Phase 30 test sections emit graceful-red fail() messages before feature code exists
- SIGKILL poll test is present, polls .phase field of .conjure-workspace-state.json
- shellcheck passes on tests/run.sh
- bash tests/run.sh exits non-zero (Phase 30 failures registered)
</success_criteria>

<output>
Create `.planning/phases/30-workspace-orchestration-mutating-rollback-saga/30-00-SUMMARY.md` when done.
</output>
