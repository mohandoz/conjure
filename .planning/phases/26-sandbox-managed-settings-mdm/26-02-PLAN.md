---
phase: 26-sandbox-managed-settings-mdm
plan: "02"
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - lib/policy-helpers.sh
  - scripts/emit-policy.sh
autonomous: true
requirements:
  - POL-03
  - POL-04

must_haves:
  truths:
    - "lib/policy-helpers.sh defines: build_managed_settings, validate_managed_settings_json, build_plist_xml, build_ps1_script functions"
    - "conjure emit-policy --regime hipaa emits conjure-policy/managed-settings.json with disableBypassPermissionsMode as STRING 'disable' (never boolean)"
    - "conjure emit-policy --regime hipaa emits conjure-policy/com.anthropic.claudecode.plist with <string>disable</string> (not <true/>) for disableBypassPermissionsMode"
    - "conjure emit-policy --regime hipaa emits conjure-policy/Set-ClaudeCodePolicy.ps1 using $env:ProgramFiles path (NOT C:\\ProgramData\\ClaudeCode\\)"
    - "The deprecated Windows path C:\\ProgramData\\ClaudeCode\\ is absent from all emitted artifacts (grep-v verified)"
    - "emit-policy with --managed-only emits only managed-settings.json (no plist, no ps1)"
    - "emit-policy with --mdm-only emits only plist and ps1 (no managed-settings.json)"
    - "All artifacts are written to --output DIR (default ./conjure-policy/); zero system paths used"
    - "bash tests/run.sh 2>&1 | grep POL-03 shows PASS"
    - "bash tests/run.sh 2>&1 | grep POL-03-type shows PASS"
    - "bash tests/run.sh 2>&1 | grep POL-04-macos shows PASS"
    - "bash tests/run.sh 2>&1 | grep POL-04-win shows PASS"
  artifacts:
    - path: "lib/policy-helpers.sh"
      provides: "Added: build_managed_settings, validate_managed_settings_json, build_plist_xml, build_ps1_script"
    - path: "scripts/emit-policy.sh"
      provides: "Wave 2 stub replaced: managed-settings.json + plist + ps1 emit paths fully implemented; --managed-only and --mdm-only subset flags active"
  key_links:
    - from: "scripts/emit-policy.sh"
      to: "lib/policy-helpers.sh build_managed_settings"
      via: "MANAGED_JSON=$(build_managed_settings ...) before mutate_write"
      pattern: "build_managed_settings"
    - from: "scripts/emit-policy.sh"
      to: "lib/policy-helpers.sh validate_managed_settings_json"
      via: "validate_managed_settings_json $MANAGED_JSON || exit 2 — gate before write"
      pattern: "validate_managed_settings_json"
    - from: "scripts/emit-policy.sh"
      to: "lib/policy-helpers.sh build_plist_xml"
      via: "PLIST_XML=$(build_plist_xml ...) before mutate_write plist file"
      pattern: "build_plist_xml"
    - from: "scripts/emit-policy.sh"
      to: "lib/policy-helpers.sh build_ps1_script"
      via: "PS1_CONTENT=$(build_ps1_script ...) before mutate_write ps1 file"
      pattern: "build_ps1_script"
---

<objective>
Wave 2: managed-settings.json emission (POL-03) and MDM artifact generation — macOS plist + Windows ps1 (POL-04).

Purpose: Replace the Wave 1 stubs in scripts/emit-policy.sh with full implementations. Add four new functions to lib/policy-helpers.sh: build_managed_settings, validate_managed_settings_json, build_plist_xml, build_ps1_script. The validate_managed_settings_json function is the critical type-safety gate — it must confirm disableBypassPermissionsMode is the STRING "disable" and not boolean, blocking the most common production misconfig. The plist and ps1 renderers are fixed-structure templates (not recursive serializers) because the managed-settings structure is static; only array contents vary by regime.

The three hard correctness rules from CONTEXT.md and RESEARCH.md baked into acceptance criteria:
1. disableBypassPermissionsMode MUST be STRING "disable" in all three artifact types — jq type check in validate_managed_settings_json, plutil -lint gate (macOS) on plist, grep check on ps1 JSON heredoc.
2. C:\ProgramData\ClaudeCode\ MUST NEVER be emitted — the ps1 must use $env:ProgramFiles exclusively; a grep-v check in the acceptance criteria and in the test suite verifies absence.
3. All artifacts written to --output DIR only — zero system path logic in emit-policy.sh.

Output: lib/policy-helpers.sh with 4 new functions, scripts/emit-policy.sh Wave 2 stubs replaced with real implementations. Tests POL-03, POL-03-type, POL-04-macos, POL-04-win go green.
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-CONTEXT.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-RESEARCH.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-PATTERNS.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-VALIDATION.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add managed-settings, plist, and ps1 builders to lib/policy-helpers.sh</name>
  <files>
    lib/policy-helpers.sh
  </files>
  <read_first>
    - lib/policy-helpers.sh (full file as produced in Plan 01 — understand the existing function style and error-accumulation pattern to follow exactly)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-RESEARCH.md (Pattern 3: managed-settings.json exact schema with key types; Pattern 4: plist XML structure with encoding rules; Pattern 5: ps1 structure with $env:ProgramFiles and UTF8Encoding($false); Code Examples §validate_managed_settings_json)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-PATTERNS.md (§lib/policy-helpers.sh: core jq-validate pattern; §Shared Patterns: jq validate-before-write, POSIX 3.2+ safety)
    - tests/fixtures/_emit-policy/expected-plist.xml (exact XML structure to mirror)
    - tests/fixtures/_emit-policy/expected-policy.ps1 (exact ps1 structure to mirror)
  </read_first>
  <behavior>
    - validate_managed_settings_json('{"permissions":{"disableBypassPermissionsMode":"disable"},"allowManagedPermissionRulesOnly":true,"forceLoginOrgUUID":"REPLACE_WITH_ORG_UUID"}') returns 0 (valid)
    - validate_managed_settings_json('{"permissions":{"disableBypassPermissionsMode":true},"allowManagedPermissionRulesOnly":true,"forceLoginOrgUUID":"uuid"}') returns 1 (boolean disableBypassPermissionsMode → FAIL)
    - validate_managed_settings_json('{"permissions":{"disableBypassPermissionsMode":"enable"},...}') returns 1 (wrong string value → FAIL)
    - validate_managed_settings_json('{"permissions":{"disableBypassPermissionsMode":"disable"},"allowManagedPermissionRulesOnly":"true",...}') returns 1 (allowManagedPermissionRulesOnly wrong type → FAIL)
    - build_managed_settings returns JSON with disableBypassPermissionsMode as STRING "disable" (jq type check confirms)
    - build_managed_settings output includes _conjure_regime and _conjure_unreviewed:true markers
    - build_plist_xml returns XML containing <string>disable</string> for disableBypassPermissionsMode (never <true/>)
    - build_ps1_script returns PowerShell content containing $env:ProgramFiles (not ProgramData)
    - build_ps1_script content includes [System.IO.File]::WriteAllText pattern with UTF8Encoding($false) for BOM-free output
    - build_ps1_script content contains disableBypassPermissionsMode: "disable" in the JSON heredoc (string, not boolean)
  </behavior>
  <action>
Append four new functions to lib/policy-helpers.sh (after the existing 5 functions). Keep shellcheck clean throughout. All functions follow the same error-accumulation pattern as validate_sandbox_json.

FUNCTION 1 — validate_managed_settings_json(content):
Full implementation from RESEARCH.md Code Examples §validate_managed_settings_json. Use shell variables for intermediate jq results (not pipelines into jq-e) to keep shellcheck SC2155 compliance:
  - CRITICAL check: `dbpm_type="$(printf '%s' "$content" | jq -r '.permissions.disableBypassPermissionsMode | type' 2>/dev/null)"`. If dbpm_type == "boolean": echo "✗ managed-settings: disableBypassPermissionsMode is boolean — must be string \"disable\"" >&2; errors++. If dbpm_type == "string": also check the value equals "disable": `dbpm_val="$(printf '%s' "$content" | jq -r '.permissions.disableBypassPermissionsMode // empty' 2>/dev/null)"`. If dbpm_val != "disable": echo "✗ managed-settings: disableBypassPermissionsMode is \"$dbpm_val\" — must be \"disable\"" >&2; errors++.
  - Check allowManagedPermissionRulesOnly is boolean (if present): jq -e 'if .allowManagedPermissionRulesOnly != null then (.allowManagedPermissionRulesOnly | type) == "boolean" else true end'
  - Check forceLoginOrgUUID is string (if present): jq -e 'if .forceLoginOrgUUID != null then (.forceLoginOrgUUID | type) == "string" else true end'
  Return 1 if errors > 0, else return 0. Caller: validate_managed_settings_json "$MANAGED_JSON" || exit 2.

FUNCTION 2 — build_managed_settings(regime, deny_read_json, sandbox_json):
Arguments: regime string (hipaa|soc2|gdpr|pci), JSON array of permissions.deny Read() entries, JSON sandbox block.
Use jq -n with --arg/$argjson to construct the managed-settings JSON. The structure from RESEARCH.md Pattern 3:
  {
    "permissions": {
      "disableBypassPermissionsMode": "disable",   ← STRING — use jq string literal not boolean
      "deny": $deny_entries
    },
    "allowManagedPermissionRulesOnly": true,
    "forceLoginOrgUUID": "REPLACE_WITH_ORG_UUID",
    "sandbox": $sandbox,
    "_conjure_regime": $regime,
    "_conjure_unreviewed": true
  }
Note on _conjure_unreviewed and _conjure_regime: include them per RESEARCH.md Pattern 3 and CONTEXT.md (the open question about unknown keys was resolved: keep only REPLACE_WITH_ORG_UUID as the self-evident "unreviewed" sentinel; _conjure_unreviewed and _conjure_regime are metadata markers — include them, they are low risk since Claude Code ignores unknown keys per RESEARCH.md Assumption A3).
jq-empty-check before returning. Print result to stdout. Caller captures.

FUNCTION 3 — build_plist_xml(deny_read_json, deny_entries_json, sandbox_json):
Arguments: denyRead JSON array (for sandbox denyRead), deny_entries JSON array (for permissions.deny), sandbox JSON block.
Render using a shell printf/heredoc with jq-derived values. The plist structure is fixed (from RESEARCH.md Pattern 4) — only the array contents vary. Use jq to extract each value:
  - disableBypassPermissionsMode: hardcoded as `<string>disable</string>` (never <true/> — this is the most critical correctness constraint in the entire phase)
  - allowManagedPermissionRulesOnly: hardcoded as `<true/>`
  - forceLoginOrgUUID: hardcoded as `<string>REPLACE_WITH_ORG_UUID</string>`
  - denyRead array: iterate deny_read_json values with jq -r '.[]', wrap each in `<string>...</string>`
  - sandbox.denyRead / denyWrite / allowedDomains: iterate the arrays similarly
Build the plist by concatenating sections using printf. This is a template renderer, not a recursive serializer. Validate paths for XML metacharacters (reject paths containing &, <, >, with exit 2) as defense-in-depth before embedding.
After building, if `command -v plutil >/dev/null 2>&1`: write to a tempfile and run `plutil -lint "$tmpfile"` to validate; remove tempfile. If plutil absent (Linux CI): skip validation, note in output.
Print the final plist XML to stdout. Caller captures.

FUNCTION 4 — build_ps1_script(regime, managed_settings_json):
Arguments: regime string, managed-settings JSON content.
Render the ps1 template from RESEARCH.md Pattern 5. Key correctness rules:
  - The $dir line MUST use: `$dir = Join-Path $env:ProgramFiles 'ClaudeCode'` — NOT ProgramData, NOT a hardcoded path.
  - The JSON content embedded in the ps1 heredoc (@'...'@) MUST contain disableBypassPermissionsMode: "disable" (string). Extract from managed_settings_json with jq to ensure the correct value is embedded.
  - Use [System.IO.File]::WriteAllText($path, $json, (New-Object System.Text.UTF8Encoding($false))) for BOM-free UTF-8 output (from RESEARCH.md Pattern 5, CRITICAL note).
  - Include the header comment block: generation note with regime name, forceLoginOrgUUID replacement instruction, and the compliance disclaimer.
  - The ps1 is rendered as a bash string (not executed by bash). Use printf with careful quoting to avoid bash interpreting PowerShell syntax. The JSON content inside @'...'@ must be serialized via jq -c or jq with appropriate formatting.
  - Single quotes in the ps1 PowerShell heredoc (@'...'@) are delimiters — do NOT embed single quotes inside the @' block. The JSON body should use double quotes throughout (which it does as standard JSON).
Print the final ps1 content to stdout. Caller captures and writes via mutate_write.
  </action>
  <verify>
    <automated>
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 lib/policy-helpers.sh &amp;&amp;
      grep -q "validate_managed_settings_json" lib/policy-helpers.sh &amp;&amp;
      grep -q "build_managed_settings" lib/policy-helpers.sh &amp;&amp;
      grep -q "build_plist_xml" lib/policy-helpers.sh &amp;&amp;
      grep -q "build_ps1_script" lib/policy-helpers.sh &amp;&amp;
      grep -q "ProgramFiles" lib/policy-helpers.sh &amp;&amp;
      grep -qv "ProgramData" lib/policy-helpers.sh &amp;&amp;
      grep -q "disableBypassPermissionsMode.*disable" lib/policy-helpers.sh &amp;&amp;
      echo "policy-helpers.sh shellcheck clean with all 4 new functions; no ProgramData reference"
    </automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 lib/policy-helpers.sh` exits 0 with all 9 functions present
    - `grep -q 'validate_managed_settings_json' lib/policy-helpers.sh` exits 0
    - `grep -q 'build_managed_settings' lib/policy-helpers.sh` exits 0
    - `grep -q 'build_plist_xml' lib/policy-helpers.sh` exits 0
    - `grep -q 'build_ps1_script' lib/policy-helpers.sh` exits 0
    - `grep -q 'ProgramFiles' lib/policy-helpers.sh` exits 0 (correct Windows path present)
    - `grep -v '^#' lib/policy-helpers.sh | grep -c 'ProgramData'` returns 0 (forbidden Windows path absent from non-comment code lines)
    - `grep -v '^#' lib/policy-helpers.sh | grep -c '<true/>'` returns 0 for any disableBypassPermissionsMode context (the plist must use `<string>disable</string>` — assert the string literal is in the plist template and `<true/>` is only used for boolean true fields like allowManagedPermissionRulesOnly)
    - validate_managed_settings_json called with a boolean disableBypassPermissionsMode returns 1 (manually sourced function test in bash)
    - The dbpm_type check in validate_managed_settings_json uses `jq -r '... | type'` (not jq -e) to avoid SC2155 — confirm pattern with grep
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace Wave 1 stubs in scripts/emit-policy.sh with full managed-settings and MDM emit</name>
  <files>
    scripts/emit-policy.sh
  </files>
  <read_first>
    - scripts/emit-policy.sh (full file as produced in Plan 01 — locate the Wave 1 stub comments to replace; understand DENY_READ_JSON, SANDBOX_JSON variable names already set)
    - lib/policy-helpers.sh (full file — all 9 functions now available; note function signatures for build_managed_settings, build_plist_xml, build_ps1_script)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-RESEARCH.md (Pattern 3: managed-settings.json; Pattern 4: plist; Pattern 5: ps1; §Anti-Patterns: no managed-settings.d/ drop-in, no system paths)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-PATTERNS.md (§scripts/emit-policy.sh: validate-before-write discipline, success report pattern, mutate_mkdir + mutate_write sequence)
  </read_first>
  <behavior>
    - emit-policy.sh --regime hipaa --output /tmp/testpol: exits 0, writes /tmp/testpol/managed-settings.json, /tmp/testpol/com.anthropic.claudecode.plist, /tmp/testpol/Set-ClaudeCodePolicy.ps1, /tmp/testpol/VERIFY.txt
    - managed-settings.json has disableBypassPermissionsMode as STRING "disable" (jq type check confirms)
    - managed-settings.json has allowManagedPermissionRulesOnly:true and forceLoginOrgUUID:"REPLACE_WITH_ORG_UUID"
    - plist file contains <string>disable</string> (not <true/>) for disableBypassPermissionsMode
    - ps1 file contains $env:ProgramFiles (not C:\ProgramData\ClaudeCode\)
    - emit-policy.sh --regime hipaa --managed-only: writes only managed-settings.json (not plist, not ps1)
    - emit-policy.sh --regime hipaa --mdm-only: writes only plist and ps1 (not managed-settings.json)
    - emit-policy.sh with invalid managed-settings content (validate_managed_settings_json fails): exits 2 and writes no output files
    - DRY_RUN=1: exits 0, no files written, dry-run output mentions managed-settings.json and MDM artifacts
    - Secret in assembled managed-settings content: exits 2 before any write (secret_scan gate)
  </behavior>
  <action>
REPLACE the Wave 1 stub section in scripts/emit-policy.sh (the block after the sandbox merge section that currently contains the `# managed-settings.json — implemented in Phase 26 Plan 02 (Wave 2)` placeholder):

MANAGED-SETTINGS.JSON EMIT (when MANAGED_ONLY or when neither MANAGED_ONLY nor MDM_ONLY):
  1. Build deny entries JSON array from the denyRead paths (using build_deny_read_entries output converted to JSON array).
  2. Call: MANAGED_JSON="$(build_managed_settings "$REGIME" "$DENY_ENTRIES_JSON" "$SANDBOX_JSON")"
  3. secret_scan "$MANAGED_JSON" "managed-settings.json" || exit 2
  4. validate_managed_settings_json "$MANAGED_JSON" || exit 2
  5. mutate_mkdir "$OUTPUT_DIR"
  6. mutate_write "$OUTPUT_DIR/managed-settings.json" "$MANAGED_JSON"

MDM ARTIFACTS EMIT — macOS plist (when MDM_ONLY or when neither flag set):
  1. Call: PLIST_XML="$(build_plist_xml "$DENY_READ_JSON" "$DENY_ENTRIES_JSON" "$SANDBOX_JSON")"
  2. jq-empty-check equivalent for XML: verify PLIST_XML is non-empty: [ -z "$PLIST_XML" ] && { echo "✗ build_plist_xml produced empty output" >&2; exit 2; }
  3. mutate_mkdir "$OUTPUT_DIR"
  4. mutate_write "$OUTPUT_DIR/com.anthropic.claudecode.plist" "$PLIST_XML"

MDM ARTIFACTS EMIT — Windows ps1 (when MDM_ONLY or when neither flag set):
  1. Call: PS1_CONTENT="$(build_ps1_script "$REGIME" "$MANAGED_JSON")"
     Note: MANAGED_JSON already built above. If MDM_ONLY=1, build_managed_settings must still be called to build the JSON content for the ps1 heredoc, even though the managed-settings.json file itself is not written.
  2. [ -z "$PS1_CONTENT" ] && { echo "✗ build_ps1_script produced empty output" >&2; exit 2; }
  3. mutate_mkdir "$OUTPUT_DIR"
  4. mutate_write "$OUTPUT_DIR/Set-ClaudeCodePolicy.ps1" "$PS1_CONTENT"

EMIT VERIFY.TXT: call emit_verification_assertions "$REGIME" "$OUTPUT_DIR" (or inline the function from RESEARCH.md Code Examples if not already in policy-helpers.sh). VERIFY.txt content includes all 5 verification steps from RESEARCH.md + the compliance disclaimer.

UPDATE SUCCESS REPORT to list all three emitted artifact filenames:
  echo "  ✓ .claude/settings.json (sandbox block merged)"
  echo "  ✓ $OUTPUT_DIR/managed-settings.json"
  echo "  ✓ $OUTPUT_DIR/com.anthropic.claudecode.plist"
  echo "  ✓ $OUTPUT_DIR/Set-ClaudeCodePolicy.ps1"
  echo "  ✓ $OUTPUT_DIR/VERIFY.txt"
  (Adjust echoes based on MANAGED_ONLY/MDM_ONLY flags — only list files actually written.)

SUBSET FLAG LOGIC:
  - MDM_ONLY=1: skip sandbox block merge into settings.json AND skip managed-settings.json write; emit only plist + ps1 + VERIFY.txt.
  - MANAGED_ONLY=1: skip plist and ps1 write; emit only managed-settings.json + VERIFY.txt. Sandbox block merge into settings.json is still performed (it's independent of the --managed-only flag since it writes to the target, not the output dir).
  - Neither flag: emit everything (sandbox block + managed-settings.json + plist + ps1 + VERIFY.txt).

POSIX 3.2+: no associative arrays, no mapfile, no local -n. Shellcheck clean.
No managed-settings.d/ directory creation (deferred POL-F1 — do NOT create this).
No write to system paths — the only filesystem writes are to $TARGET/.claude/settings.json (via merge_sandbox_block) and $OUTPUT_DIR/* (via mutate_write).
  </action>
  <verify>
    <automated>
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/emit-policy.sh &amp;&amp;
      grep -q "build_managed_settings" scripts/emit-policy.sh &amp;&amp;
      grep -q "build_plist_xml" scripts/emit-policy.sh &amp;&amp;
      grep -q "build_ps1_script" scripts/emit-policy.sh &amp;&amp;
      grep -qv "ProgramData" scripts/emit-policy.sh &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep "POL-03\b" | grep -c PASS | grep -qE "^[1-9]" &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep "POL-03-type" | grep -c PASS | grep -qE "^[1-9]" &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep "POL-04-macos" | grep -c PASS | grep -qE "^[1-9]" &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep "POL-04-win" | grep -c PASS | grep -qE "^[1-9]" &amp;&amp;
      echo "emit-policy.sh shellcheck clean; all Wave 2 POL-03/04 tests pass"
    </automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 scripts/emit-policy.sh` exits 0
    - `grep -q 'build_managed_settings' scripts/emit-policy.sh` exits 0 (managed-settings builder called)
    - `grep -q 'validate_managed_settings_json' scripts/emit-policy.sh` exits 0 (type-safety gate present)
    - `grep -q 'build_plist_xml' scripts/emit-policy.sh` exits 0
    - `grep -q 'build_ps1_script' scripts/emit-policy.sh` exits 0
    - `grep -v '^#' scripts/emit-policy.sh | grep -c 'ProgramData'` returns 0 (deprecated path never emitted)
    - `grep -v '^#' scripts/emit-policy.sh | grep -c 'managed-settings.d'` returns 0 (POL-F1 deferred — drop-in dir must not be created)
    - `bash tests/run.sh 2>&1 | grep "POL-03\b"` shows PASS (managed-settings.json correct keys and types)
    - `bash tests/run.sh 2>&1 | grep "POL-03-type"` shows PASS (disableBypassPermissionsMode is STRING)
    - `bash tests/run.sh 2>&1 | grep "POL-04-macos"` shows PASS (plist contains <string>disable</string>)
    - `bash tests/run.sh 2>&1 | grep "POL-04-win"` shows PASS (ps1 uses ProgramFiles, no ProgramData)
    - `bash tests/run.sh 2>&1 | grep "POL-01\b"` still shows PASS (no regression on Wave 1 tests)
    - `bash tests/run.sh 2>&1 | grep "POL-02"` still shows PASS (no regression)
    - `bash tests/run.sh 2>&1 | grep "POL-dryrun"` still shows PASS (DRY_RUN still works for all 3 artifact types)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| managed-settings.json content → mutate_write | validate_managed_settings_json type-checks disableBypassPermissionsMode as STRING "disable" before any write; exit 2 on type error (prevents false-sense-of-security from boolean form) |
| denyRead paths → plist XML embed | Paths are validated for XML metacharacters (&, <, >) before embedding in plist heredoc; plutil -lint gate on macOS catches malformed plist post-render |
| ps1 Windows path → emit | Hard-coded $env:ProgramFiles; no user-controlled path component; ProgramData forbidden by acceptance criteria grep-v check |
| REPLACE_WITH_ORG_UUID → production deploy | forceLoginOrgUUID placeholder is self-evidently fake; Claude Code rejects it at login time; VERIFY.txt step 5 instructs operators to verify they have replaced it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering | disableBypassPermissionsMode type coercion in managed-settings.json | mitigate | validate_managed_settings_json(): `jq -r '.permissions.disableBypassPermissionsMode | type'` check before any write; exits 2 if boolean; this is the Pitfall 1 mitigation from RESEARCH.md; also verified by POL-03-type test in Wave 0 fixture |
| T-26-08 | Tampering | disableBypassPermissionsMode type coercion in plist (XML <true/> instead of <string>disable</string>) | mitigate | build_plist_xml() hardcodes `<string>disable</string>` for this field — the value is never dynamically derived from JSON booleans; plutil -lint catches malformed plist on macOS; POL-04-macos test grep-checks `<string>disable</string>` is present |
| T-26-09 | Tampering | deprecated Windows path C:\ProgramData\ClaudeCode\ emitted | mitigate | build_ps1_script() uses `$env:ProgramFiles` exclusively; forbidden path is tested by acceptance criteria `grep -v '^#' scripts/emit-policy.sh | grep -c 'ProgramData' == 0` and POL-04-win test |
| T-26-10 | Tampering | XML injection via denyRead path in plist (metacharacter in path) | mitigate | validate denyRead paths for XML metacharacters (&, <, >) in build_plist_xml before embedding; reject with exit 2; plutil -lint post-render catches any escaping failures |
| T-26-11 | Information Disclosure | secret in assembled managed-settings.json | mitigate | secret_scan() runs on MANAGED_JSON before any mutate_write call (mirrors Wave 1 pattern for sandbox JSON) |
| T-26-12 | Tampering | REPLACE_WITH_ORG_UUID deployed to production without replacement | mitigate | UUID placeholder is self-evidently fake (Claude login fails); VERIFY.txt step 5 checks `grep -c REPLACE_WITH_ORG_UUID managed-settings.json # must return 0`; POL-05-advisory audit check in Wave 3 notes its presence |
| T-26-13 | Tampering | managed-settings.d/ drop-in directory accidentally created (deferred scope) | mitigate | No managed-settings.d/ creation in any code path; acceptance criteria `grep -c 'managed-settings.d'` returns 0 in emit-policy.sh |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs — bash + jq + plutil (system binary) only |
</threat_model>

<verification>
After Wave 2 completes:
- `bash tests/run.sh 2>&1 | grep POL | grep PASS` shows 8 PASS: POL-01, POL-01-idem, POL-02, POL-03, POL-03-type, POL-04-macos, POL-04-win, POL-dryrun
- `bash tests/run.sh 2>&1 | grep POL | grep FAIL` shows 4 remaining FAILs: POL-05a, POL-05b, POL-05c, POL-05-advisory (Wave 3 — audit checks)
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 lib/policy-helpers.sh scripts/emit-policy.sh` both exit 0
- `grep -v '^#' scripts/emit-policy.sh | grep -c 'ProgramData'` returns 0
- `grep -v '^#' lib/policy-helpers.sh | grep -c 'ProgramData'` returns 0
- All pre-Wave 26 tests and Phase 25 PLUG-* tests still PASS (no regression)
</verification>

<success_criteria>
- lib/policy-helpers.sh has 9 functions total: the 5 from Plan 01 + validate_managed_settings_json, build_managed_settings, build_plist_xml, build_ps1_script
- validate_managed_settings_json blocks boolean disableBypassPermissionsMode with exit 2 (the most critical correctness gate in the phase)
- scripts/emit-policy.sh emits all three artifact types; --managed-only and --mdm-only subset flags work correctly
- No system paths (Library, ProgramData, etc.) appear in any emitted artifact or code path
- All four POL-03/POL-04 tests green; all POL-01/POL-02/POL-dryrun tests remain green
</success_criteria>

<output>
Create .planning/phases/26-sandbox-managed-settings-mdm/26-02-SUMMARY.md when done.
</output>
