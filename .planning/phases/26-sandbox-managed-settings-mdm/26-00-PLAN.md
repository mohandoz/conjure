---
phase: 26-sandbox-managed-settings-mdm
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/_emit-policy/harness/CLAUDE.md
  - tests/fixtures/_emit-policy/harness/.claude/settings.json
  - tests/fixtures/_emit-policy/harness/.conjure-version
  - tests/fixtures/_emit-policy/expected-sandbox.json
  - tests/fixtures/_emit-policy/expected-managed-settings.json
  - tests/fixtures/_emit-policy/expected-plist.xml
  - tests/fixtures/_emit-policy/expected-policy.ps1
  - tests/fixtures/_emit-policy-broken/harness/.claude/settings.json
  - tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json
  - tests/run.sh
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-03
  - POL-04
  - POL-05

must_haves:
  truths:
    - "tests/run.sh contains a Phase 26 block (▸ Phase 26 — Sandbox + Managed-Settings / MDM)"
    - "Running bash tests/run.sh exits non-zero with FAIL count when scripts/emit-policy.sh is absent (graceful-red)"
    - "tests/fixtures/_emit-policy/harness/ contains CLAUDE.md with <!-- compliance:hipaa --> marker and minimal .claude/settings.json with hooks block"
    - "tests/fixtures/_emit-policy/expected-sandbox.json is valid JSON with enabled:true, filesystem.denyRead as an array, network.allowedDomains as array"
    - "tests/fixtures/_emit-policy/expected-managed-settings.json has disableBypassPermissionsMode as STRING 'disable' (jq type check passes)"
    - "tests/fixtures/_emit-policy-broken/harness/.claude/settings.json has disableBypassPermissionsMode set to boolean true (negative fixture for POL-05c)"
    - "tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json contains REPLACE_WITH_ORG_UUID (advisory fixture)"
    - "All 12 POL-* grep tags are present in tests/run.sh: POL-01, POL-01-idem, POL-02, POL-03, POL-03-type, POL-04-macos, POL-04-win, POL-05a, POL-05b, POL-05c, POL-05-advisory, POL-dryrun"
  artifacts:
    - path: "tests/fixtures/_emit-policy/harness/CLAUDE.md"
      provides: "Harness with compliance marker for hipaa overlay detection"
      contains: "compliance:hipaa"
    - path: "tests/fixtures/_emit-policy/harness/.claude/settings.json"
      provides: "Minimal settings.json (no sandbox block) for emit-policy to merge into"
    - path: "tests/fixtures/_emit-policy/expected-sandbox.json"
      provides: "Golden sandbox block shape: enabled, failIfUnavailable, allowUnsandboxedCommands, filesystem.denyRead/denyWrite arrays, network.allowedDomains"
    - path: "tests/fixtures/_emit-policy/expected-managed-settings.json"
      provides: "Golden managed-settings: disableBypassPermissionsMode STRING, allowManagedPermissionRulesOnly, forceLoginOrgUUID placeholder"
    - path: "tests/fixtures/_emit-policy/expected-plist.xml"
      provides: "Golden plist XML with disableBypassPermissionsMode as <string>disable</string>"
    - path: "tests/fixtures/_emit-policy/expected-policy.ps1"
      provides: "Golden ps1 using $env:ProgramFiles path (no ProgramData)"
    - path: "tests/fixtures/_emit-policy-broken/harness/.claude/settings.json"
      provides: "Negative fixture: disableBypassPermissionsMode:true (boolean) for POL-05c test"
    - path: "tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json"
      provides: "Advisory fixture: REPLACE_WITH_ORG_UUID still present for POL-05-advisory test"
    - path: "tests/run.sh"
      provides: "Phase 26 graceful-red test block"
      contains: "Phase 26 — Sandbox + Managed-Settings / MDM"
  key_links:
    - from: "tests/run.sh Phase 26 block"
      to: "scripts/emit-policy.sh"
      via: "P26_EMIT_OK presence guard"
      pattern: "P26_EMIT_OK=0.*\\[ -f.*emit-policy.sh"
    - from: "tests/run.sh"
      to: "tests/fixtures/_emit-policy/harness/"
      via: "cp -r into mktemp sandbox"
      pattern: "cp -r.*_emit-policy/harness"
---

<objective>
Wave 0: Build the test infrastructure that all feature waves depend on.

Purpose: Nyquist Wave 0 invariant — the Phase 26 graceful-red test block and all golden fixtures must exist BEFORE any feature code lands, so that implementations in Waves 1–3 can be verified as they are built. When scripts/emit-policy.sh is absent (as it will be after this plan), the test suite must exit with FAIL (graceful-red) so CI shows failing tests, not silence. Mirrors the Phase 25 Wave 0 pattern exactly.

Output: tests/fixtures/_emit-policy/ (minimal harness + golden expected outputs), tests/fixtures/_emit-policy-broken/ (wrong-type disableBypassPermissionsMode negative fixture), tests/fixtures/_emit-policy-unreviewed/ (REPLACE_WITH_ORG_UUID advisory fixture), and the Phase 26 test block appended to tests/run.sh.
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-CONTEXT.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-RESEARCH.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-PATTERNS.md
@.planning/phases/26-sandbox-managed-settings-mdm/26-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden fixture harness and expected output files</name>
  <files>
    tests/fixtures/_emit-policy/harness/CLAUDE.md
    tests/fixtures/_emit-policy/harness/.claude/settings.json
    tests/fixtures/_emit-policy/harness/.conjure-version
    tests/fixtures/_emit-policy/expected-sandbox.json
    tests/fixtures/_emit-policy/expected-managed-settings.json
    tests/fixtures/_emit-policy/expected-plist.xml
    tests/fixtures/_emit-policy/expected-policy.ps1
    tests/fixtures/_emit-policy-broken/harness/.claude/settings.json
    tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json
  </files>
  <read_first>
    - .planning/phases/26-sandbox-managed-settings-mdm/26-RESEARCH.md (Pattern 3: managed-settings.json exact schema; Pattern 4: plist XML structure; Pattern 5: ps1 structure; Pattern 1: sandbox block shape)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-PATTERNS.md (§Fixture file structure: exact content for all three fixture directories)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-VALIDATION.md (§Wave 0 Requirements: full checklist)
    - tests/fixtures/_emit-plugin/harness/CLAUDE.md (header style to mirror)
    - tests/fixtures/_emit-plugin/harness/.claude/settings.json (minimal settings shape to mirror)
  </read_first>
  <action>
Create three fixture trees under tests/fixtures/. All directories are _-prefixed so the generic audit/golden loops exclude them per CLAUDE.md conventions.

TREE 1: tests/fixtures/_emit-policy/harness/ — minimal harness for the happy-path hipaa test:
  - harness/CLAUDE.md: two-line stub ending with the compliance marker on its own line:
      ## Project
      Test harness for conjure emit-policy fixture.
      <!-- compliance:hipaa -->
    The marker must be exactly <!-- compliance:hipaa --> — this is what audit-setup.sh grep-oE reads.
  - harness/.claude/settings.json: minimal valid JSON with a hooks block but NO sandbox block (emit-policy must create the sandbox block on this file). Shape:
      {"hooks": {"PostToolUse": [{"matcher": "Write|Edit", "hooks": [{"type": "command", "command": ".claude/hooks/format.mjs"}]}]}}
    No sandbox key, no permissions key — emit-policy.sh merges these in.
  - harness/.conjure-version: single line "0.7.0" (for version display in VERIFY.txt output)

TREE 1 golden outputs (in tests/fixtures/_emit-policy/, NOT in harness/):
  - expected-sandbox.json: the sandbox block that emit-policy.sh should produce for hipaa. Shape from RESEARCH.md Pattern 1 + Code Examples:
      {
        "enabled": true,
        "failIfUnavailable": true,
        "allowUnsandboxedCommands": false,
        "filesystem": {
          "denyRead": ["~/.aws", "~/.aws/credentials", "~/.ssh", "~/.gnupg", "~/.config/gcloud", ".env", "**/.env", "**/.env.*", "**/secrets", "**/credentials", "**/*.pem", "**/*.key", "**/*.p12", "**/*.pfx", "**/phi", "**/phi/**", "**/patient*", "**/medical*", "**/health-records/**", "**/ehr/**", "**/mrn*", "**/ssn*", "**/dob*"],
          "denyWrite": ["~/.ssh", "~/.gnupg", "~/.aws", "**/phi/**", "**/patient*"]
        },
        "network": {
          "allowedDomains": []
        }
      }
    This is the hipaa regime: baseline + hipaa delta paths. The exact path list comes from RESEARCH.md §Per-Regime Deny-Path Conventions. Executor uses this as a shape reference, not a strict byte-for-byte golden — the test checks structural assertions (enabled:true, denyRead is array, network.allowedDomains is array), not exact path sets, since per-regime paths are LOW-confidence advisory defaults.

  - expected-managed-settings.json: the managed-settings.json content from RESEARCH.md Pattern 3. Shape exactly:
      {
        "permissions": {
          "disableBypassPermissionsMode": "disable",
          "deny": ["Read(~/.aws/credentials)", "Read(~/.ssh/**)"]
        },
        "allowManagedPermissionRulesOnly": true,
        "forceLoginOrgUUID": "REPLACE_WITH_ORG_UUID",
        "sandbox": {
          "enabled": true,
          "failIfUnavailable": true,
          "allowUnsandboxedCommands": false,
          "filesystem": {"denyRead": ["~/.aws", "~/.aws/credentials", "~/.ssh"], "denyWrite": []},
          "network": {"allowedDomains": []}
        },
        "_conjure_regime": "hipaa",
        "_conjure_unreviewed": true
      }
    The deny array is a subset (the sample denyRead paths from the golden; the exact list will come from the real emit). The key invariant is the type of disableBypassPermissionsMode — must be STRING "disable". The executor does NOT need this to be a byte-for-byte match against real emit output; the test uses jq type checks.

  - expected-plist.xml: plist XML from RESEARCH.md Pattern 4. Use the exact XML structure from the research, with disableBypassPermissionsMode as <string>disable</string> (NOT <true/>). Include the sandbox block with denyRead arrays from the hipaa baseline. The XML must be well-formed and pass `plutil -lint` on macOS. Use the exact structure from RESEARCH.md Pattern 4 as the template — replace only the denyRead/deny arrays with a representative subset for a golden.

  - expected-policy.ps1: ps1 script from RESEARCH.md Pattern 5. The $dir line MUST use $env:ProgramFiles (NOT ProgramData — grep the golden after writing to assert no "ProgramData" string appears). The JSON heredoc inside the ps1 must contain disableBypassPermissionsMode:"disable" (string). Include the header comment block from Pattern 5 including the "conjure emit-policy --regime hipaa" note and the compliance disclaimer. Use [System.IO.File]::WriteAllText with UTF8Encoding($false) for BOM-free output.

TREE 2: tests/fixtures/_emit-policy-broken/harness/ — negative fixture for POL-05c:
  - harness/.claude/settings.json: settings.json with disableBypassPermissionsMode set to boolean true (wrong type). Shape from RESEARCH.md §Pitfall 1 and PATTERNS.md §Fixture file structure:
      {
        "permissions": {
          "disableBypassPermissionsMode": true
        }
      }
    This is the exact wrong-type boolean. POL-05c audit check must detect this and emit exit 2.

TREE 3: tests/fixtures/_emit-policy-unreviewed/conjure-policy/ — advisory fixture for POL-05-advisory:
  - conjure-policy/managed-settings.json: managed-settings.json where the forceLoginOrgUUID is still the REPLACE_WITH_ORG_UUID placeholder and _conjure_unreviewed is true. Shape from PATTERNS.md §Fixture file structure:
      {
        "permissions": {
          "disableBypassPermissionsMode": "disable"
        },
        "forceLoginOrgUUID": "REPLACE_WITH_ORG_UUID",
        "_conjure_unreviewed": true
      }
    Note: disableBypassPermissionsMode IS the correct string type here — the advisory is only about the unreviewed UUID placeholder, not a type error.

All _-prefixed fixture dirs are excluded from generic loops per CLAUDE.md.
  </action>
  <verify>
    <automated>
      test -f tests/fixtures/_emit-policy/harness/CLAUDE.md &amp;&amp;
      grep -q "compliance:hipaa" tests/fixtures/_emit-policy/harness/CLAUDE.md &amp;&amp;
      test -f tests/fixtures/_emit-policy/harness/.claude/settings.json &amp;&amp;
      jq empty tests/fixtures/_emit-policy/harness/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_emit-policy/expected-sandbox.json &amp;&amp;
      jq -e '(.enabled | type) == "boolean" and .enabled == true and (.filesystem.denyRead | type) == "array" and (.network.allowedDomains | type) == "array"' tests/fixtures/_emit-policy/expected-sandbox.json &amp;&amp;
      test -f tests/fixtures/_emit-policy/expected-managed-settings.json &amp;&amp;
      jq -e '(.permissions.disableBypassPermissionsMode | type) == "string" and .permissions.disableBypassPermissionsMode == "disable"' tests/fixtures/_emit-policy/expected-managed-settings.json &amp;&amp;
      jq -e '(.allowManagedPermissionRulesOnly | type) == "boolean" and .forceLoginOrgUUID == "REPLACE_WITH_ORG_UUID"' tests/fixtures/_emit-policy/expected-managed-settings.json &amp;&amp;
      test -f tests/fixtures/_emit-policy/expected-plist.xml &amp;&amp;
      grep -q "string.disable./string" tests/fixtures/_emit-policy/expected-plist.xml &amp;&amp;
      test -f tests/fixtures/_emit-policy/expected-policy.ps1 &amp;&amp;
      grep -q "ProgramFiles" tests/fixtures/_emit-policy/expected-policy.ps1 &amp;&amp;
      grep -qv "ProgramData" tests/fixtures/_emit-policy/expected-policy.ps1 &amp;&amp;
      test -f tests/fixtures/_emit-policy-broken/harness/.claude/settings.json &amp;&amp;
      jq -e '(.permissions.disableBypassPermissionsMode | type) == "boolean"' tests/fixtures/_emit-policy-broken/harness/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json &amp;&amp;
      grep -q "REPLACE_WITH_ORG_UUID" tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json &amp;&amp;
      echo "All fixture files exist with correct shapes"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/_emit-policy/harness/CLAUDE.md contains exactly the line: <!-- compliance:hipaa -->
    - tests/fixtures/_emit-policy/harness/.claude/settings.json passes `jq -e '.hooks != null'` and `jq -e '.sandbox == null'` (no sandbox yet — emit-policy creates it)
    - tests/fixtures/_emit-policy/expected-sandbox.json passes `jq -e '.enabled == true and (.filesystem.denyRead | type) == "array" and (.network.allowedDomains | type) == "array" and (.network.allowedDomains | length) == 0'`
    - tests/fixtures/_emit-policy/expected-managed-settings.json passes `jq -e '(.permissions.disableBypassPermissionsMode | type) == "string" and .permissions.disableBypassPermissionsMode == "disable" and .allowManagedPermissionRulesOnly == true and (.forceLoginOrgUUID | type) == "string"'`
    - tests/fixtures/_emit-policy/expected-plist.xml contains `<string>disable</string>` (not `<true/>`) for the disableBypassPermissionsMode key
    - tests/fixtures/_emit-policy/expected-policy.ps1 contains `$env:ProgramFiles` and does NOT contain `ProgramData` (grep -v asserts absence)
    - tests/fixtures/_emit-policy-broken/harness/.claude/settings.json passes `jq -e '(.permissions.disableBypassPermissionsMode | type) == "boolean"'` (the negative fixture)
    - tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json passes `jq -e '._conjure_unreviewed == true'` AND contains the string `REPLACE_WITH_ORG_UUID`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Phase 26 graceful-red test block to tests/run.sh</name>
  <files>
    tests/run.sh
  </files>
  <read_first>
    - tests/run.sh (read last 60 lines to see insertion point: before the GH_HIDE_STUBS cleanup loop and summary block — insert Phase 26 block immediately before the "# ────...# Clean up any gh-hiding stub dirs" comment)
    - tests/run.sh (read lines 3984-4030 for Phase 25 block header and P25_EMIT_OK presence-guard pattern — mirror exactly)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-PATTERNS.md (§tests/run.sh block: header pattern, mktemp sandbox pattern, advisory audit test pattern, fixture path pattern)
    - .planning/phases/26-sandbox-managed-settings-mdm/26-VALIDATION.md (§Per-Task Verification Map: 12 POL-* grep tags)
  </read_first>
  <action>
Append a Phase 26 test block to tests/run.sh IMMEDIATELY BEFORE the "for _s in $GH_HIDE_STUBS" cleanup loop near the end of the file (after the last Phase 25 test and before the GH_HIDE_STUBS loop — do NOT alter the summary block or the final `[ "$FAIL" -eq 0 ]` gate).

SECTION HEADER (matching Phase 25 width):
  # ──────────────────────────────────────────────────────────────────────────────
  # Phase 26 — Sandbox + Managed-Settings / MDM (POL-01..POL-05)
  # ──────────────────────────────────────────────────────────────────────────────

PRESENCE GUARD (per PATTERNS.md §Phase block header pattern):
  P26_EMIT_SH="$CONJURE_HOME/scripts/emit-policy.sh"
  P26_AUDIT_SH="$CONJURE_HOME/scripts/audit-setup.sh"
  P26_EMIT_OK=0
  [ -f "$P26_EMIT_SH" ] && P26_EMIT_OK=1

Echo header:
  echo
  echo "▸ Phase 26 — Sandbox + Managed-Settings / MDM (POL-01..POL-05)"

The 12 test cases to implement (each in its own mktemp sandbox with its own EXIT trap per PATTERNS.md §Per-test mktemp sandbox pattern):

1. POL-01: git-init sandbox from _emit-policy/harness; run `CONJURE_HOME="$CONJURE_HOME" bash "$P26_EMIT_SH" --regime hipaa --output "$P26_DIR/conjure-policy" 2>/dev/null`; check that .claude/settings.json now has `.sandbox.enabled == true` via jq; pass/fail "emit-policy merges sandbox block into settings.json (POL-01)"

2. POL-01-idem: git-init sandbox from _emit-policy/harness; run emit twice; capture .claude/settings.json after each run; assert `[ "$FIRST" = "$SECOND" ]` (idempotency — re-run produces identical sandbox block); pass/fail "emit-policy is idempotent: re-run produces identical settings.json (POL-01-idem)"

3. POL-02: same sandbox as POL-01 (or fresh); after emit, use jq to read all .sandbox.filesystem.denyRead paths; for each path check there is a matching `Read(...)` entry in .permissions.deny; at minimum assert that `.permissions.deny | length > 0` and that `.sandbox.filesystem.denyRead | length > 0`; pass/fail "emit-policy mirrors denyRead paths into permissions.deny (POL-02)"

4. POL-03: git-init sandbox; run emit; assert that `conjure-policy/managed-settings.json` exists; jq-check: disableBypassPermissionsMode is string and equals "disable", allowManagedPermissionRulesOnly is boolean true, forceLoginOrgUUID is string "REPLACE_WITH_ORG_UUID"; pass/fail "emit-policy produces managed-settings.json with correct keys and types (POL-03)"

5. POL-03-type: git-init sandbox; run emit; jq-assert `(.permissions.disableBypassPermissionsMode | type) == "string"` on the emitted managed-settings.json; if audit detects it as string: pass; if the type were boolean: fail; pass/fail "disableBypassPermissionsMode is STRING 'disable' not boolean (POL-03-type)"

6. POL-04-macos: git-init sandbox; run emit; assert `conjure-policy/com.anthropic.claudecode.plist` exists; grep-check `<string>disable</string>` is present and `<true/>` is absent for the disableBypassPermissionsMode key (use grep -c pattern); pass/fail "emit-policy produces macOS plist with correct XML (POL-04-macos)"

7. POL-04-win: git-init sandbox; run emit; assert `conjure-policy/Set-ClaudeCodePolicy.ps1` exists; grep-check `$env:ProgramFiles` is present; grep-v check `ProgramData` is absent; pass/fail "emit-policy produces Windows ps1 with correct path (no deprecated ProgramData) (POL-04-win)"

8. POL-05a: mktemp sandbox (no git needed); create .claude/settings.json without sandbox.enabled (or with sandbox.enabled:false); create CLAUDE.md with <!-- compliance:hipaa --> marker; run `CONJURE_HOME="$CONJURE_HOME" bash "$P26_AUDIT_SH" "$P26_DIR" 2>&1`; capture exit code; assert exit code == 2 AND output contains "sandbox.enabled"; pass/fail "audit-setup fails when overlay active but sandbox.enabled missing (POL-05a)"

9. POL-05b: mktemp sandbox; create .claude/settings.json with a denyRead path but no matching Read() in permissions.deny; create CLAUDE.md with compliance marker; run audit; assert exit code 2 AND output contains "denyRead" or "permissions.deny"; pass/fail "audit-setup fails when denyRead path has no mirrored permissions.deny Read() entry (POL-05b)"

10. POL-05c: mktemp sandbox (no git needed); copy tests/fixtures/_emit-policy-broken/harness/.claude/settings.json into sandbox; create CLAUDE.md with <!-- compliance:hipaa --> marker; run audit; assert exit code 2 AND output contains "disableBypassPermissionsMode"; pass/fail "audit-setup fails when disableBypassPermissionsMode is boolean (POL-05c)"

11. POL-05-advisory: mktemp sandbox; copy tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json into sandbox/conjure-policy/; create minimal .claude/settings.json with correct sandbox (so no hard-fail fires); run audit; assert exit code 0 AND output contains "REPLACE_WITH_ORG_UUID"; pass/fail "audit-setup issues note (exit 0) for unreviewed template with REPLACE_WITH_ORG_UUID (POL-05-advisory)"

12. POL-dryrun: git-init sandbox from _emit-policy/harness; run `CONJURE_HOME="$CONJURE_HOME" DRY_RUN=1 bash "$P26_EMIT_SH" --regime hipaa --output "$P26_DIR/conjure-policy" 2>/dev/null`; assert exit code 0; assert .claude/settings.json does NOT have sandbox.enabled (no file was written); assert conjure-policy/ does NOT exist; pass/fail "emit-policy --dry-run prints mutations but writes no files (POL-dryrun)"

All test cases:
  - Guard emit invocations behind `if [ "$P26_EMIT_OK" -eq 1 ]` ... else `fail "emit-policy.sh not found — Wave 1 must create scripts/emit-policy.sh (POL-XX)"` ... fi
  - Use `mktemp -d` for each test directory with `trap 'rm -rf "$P26_xxx_DIR"' EXIT` and `trap - EXIT` after cleanup
  - Use `pass`/`fail` helpers already defined in tests/run.sh (do NOT redefine them)
  - POSIX 3.2+: no associative arrays, no mapfile, no local -n
  - Use `printf '%s\n'` not `echo` for content output in test assertions
  - Use `grep -v '^#'` before `grep -c` where counting non-comment lines to avoid shellcheck-flagged grep-gate hygiene issues

Insert the entire block IMMEDIATELY BEFORE the `# ──────...# Clean up any gh-hiding stub dirs` comment. Do not alter anything below the insertion point.
  </action>
  <verify>
    <automated>
      grep -c "Phase 26" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "P26_EMIT_OK" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "POL-01-idem" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "POL-05-advisory" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "POL-dryrun" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep POL | grep -c FAIL | grep -qE "^[1-9]" &amp;&amp;
      echo "Phase 26 block present, shellcheck clean, and graceful-red"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/run.sh contains a "Phase 26 — Sandbox + Managed-Settings / MDM" section header
    - All 12 POL-* grep tags are present: POL-01, POL-01-idem, POL-02, POL-03, POL-03-type, POL-04-macos, POL-04-win, POL-05a, POL-05b, POL-05c, POL-05-advisory, POL-dryrun
    - `bash tests/run.sh 2>&1 | grep POL | grep FAIL` exits 0 with non-empty output (graceful-red — all POL-* FAIL because emit-policy.sh absent)
    - `bash tests/run.sh` exits non-zero (FAIL count > 0)
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` passes (no new errors introduced)
    - The existing summary block (PASS: N  FAIL: N) and final `[ "$FAIL" -eq 0 ]` remain unchanged at end of file
    - The Phase 25 PLUG-* test block and all pre-existing blocks are unmodified
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture file content → tests/run.sh | Fixture files are static, team-authored — not user-supplied input at test execution time |
| negative fixture (.claude/settings.json with boolean disableBypassPermissionsMode) → audit test | The wrong-type boolean is deliberately introduced in the fixture for testing; it must not appear in any "happy path" golden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-W0-01 | Information Disclosure | tests/fixtures/_emit-policy-unreviewed/conjure-policy/managed-settings.json | mitigate | REPLACE_WITH_ORG_UUID is a clearly fake placeholder string that fails Claude login validation; no real org UUID is used in any fixture |
| T-26-W0-02 | Tampering | expected-plist.xml containing disableBypassPermissionsMode | mitigate | The expected-plist.xml must contain `<string>disable</string>` (not `<true/>`); the Task 1 verify step asserts this with grep; any mutation to boolean would be caught in Task 2's POL-05c test |
| T-26-W0-03 | Tampering | expected-policy.ps1 containing Windows path | mitigate | expected-policy.ps1 must use `$env:ProgramFiles` (not `ProgramData`); Task 1 verify step asserts absence of ProgramData via grep-v |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — bash + jq only |
</threat_model>

<verification>
After both tasks complete:
- `bash tests/run.sh 2>&1 | grep POL | grep FAIL` shows 12 FAIL lines (graceful-red — all POL-* cases fail because emit-policy.sh is absent)
- `test -d tests/fixtures/_emit-policy && test -d tests/fixtures/_emit-policy-broken && test -d tests/fixtures/_emit-policy-unreviewed` exits 0
- `jq -e '(.permissions.disableBypassPermissionsMode | type) == "string"' tests/fixtures/_emit-policy/expected-managed-settings.json` exits 0
- `jq -e '(.permissions.disableBypassPermissionsMode | type) == "boolean"' tests/fixtures/_emit-policy-broken/harness/.claude/settings.json` exits 0
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` exits 0
</verification>

<success_criteria>
- Wave 0 is complete: all golden fixtures and graceful-red test block exist
- `bash tests/run.sh` exits non-zero because scripts/emit-policy.sh is absent (graceful-red is the correct state after Wave 0)
- Plan 01 (Wave 1) can now implement lib/policy-helpers.sh, compliance/*/policy.sh, scripts/emit-policy.sh, and cli/conjure dispatch to turn POL-01/POL-02 tests green
</success_criteria>

<output>
Create .planning/phases/26-sandbox-managed-settings-mdm/26-00-SUMMARY.md when done.
</output>
