---
phase: 06-cost-estimator
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - tests/run.sh
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-03

must_haves:
  truths:
    - "bash tests/run.sh exits 0 — all pre-existing tests pass without modification"
    - "The cost estimator test section in tests/run.sh asserts: cost section header present, label format correct (±20%, prices: YYYY-MM, model name), and no network calls in default path"
    - "The --exact fallback test asserts: advisory message present and exit code ≤ 2 when ANTHROPIC_API_KEY is absent"
    - "lib/prices.json is covered by the JSON validity loop in tests/run.sh"
  artifacts:
    - path: "tests/run.sh"
      provides: "Cost estimator test section (COST-01, COST-02, COST-03)"
      contains: "Cost estimator tests (COST-01, COST-02, COST-03)"
  key_links:
    - from: "tests/run.sh cost section"
      to: "scripts/audit-setup.sh"
      via: "CONJURE_COST=1 bash invocation against a sandbox fixture"
      pattern: "CONJURE_COST=1.*audit-setup\\.sh"
    - from: "tests/run.sh JSON validity loop"
      to: "lib/prices.json"
      via: "find scope extended to include lib/ directory"
      pattern: "lib/prices\\.json"
---

<objective>
Extend tests/run.sh with the cost estimator test section and extend the JSON validity loop
to cover lib/prices.json. These tests encode the COST-01, COST-02, COST-03 requirements
as automated, grep-based assertions that run in under 30 seconds.

Purpose: Phase gate — no phase is complete until the regression suite is green. This plan
closes the Nyquist gap (all requirements have automated verification) and ensures that future
changes to audit-setup.sh, prices.json, or exact-count.mjs are caught immediately.

Output:
- tests/run.sh: new "Cost estimator tests" section appended before the final summary block
- tests/run.sh: JSON validity find scope extended to include lib/ (so lib/prices.json is validated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-cost-estimator/06-CONTEXT.md
@.planning/phases/06-cost-estimator/06-VALIDATION.md
@.planning/phases/06-cost-estimator/06-PATTERNS.md
@.planning/phases/06-cost-estimator/06-01-SUMMARY.md
@.planning/phases/06-cost-estimator/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend JSON validity loop to include lib/ and add cost estimator test section (COST-01, COST-02, COST-03)</name>
  <read_first>
    tests/run.sh
    scripts/audit-setup.sh
    lib/prices.json
    .planning/phases/06-cost-estimator/06-PATTERNS.md (tests/run.sh section — section header pattern, grep-based assertion pattern, fixture+sandbox pattern)
    .planning/phases/06-cost-estimator/06-VALIDATION.md (per-task verification map)
  </read_first>
  <files>tests/run.sh</files>
  <behavior>
    - `bash tests/run.sh` exits 0 with all pre-existing tests still passing
    - The JSON validity loop at lines 37-43 now finds lib/prices.json and emits `pass "json valid: lib/prices.json"` (find scope must include lib/)
    - The cost test section header is `▸ Cost estimator tests (COST-01, COST-02, COST-03)`
    - COST-01 test: run `CONJURE_COST=1 bash "$CONJURE_HOME/scripts/audit-setup.sh" "$SANDBOX_DIR"` against a sandbox fixture, grep output for `── Cost Estimate ──`; pass/fail accordingly
    - COST-02 test: grep the same output for a pattern matching `Estimate: \$[0-9]+\.[0-9]{2} ±20%` and for the string `prices:`; pass/fail accordingly
    - COST-02 model name test: grep the same output for `claude-sonnet-4-6`; pass/fail accordingly
    - COST-03 no-network test: grep `scripts/audit-setup.sh` for curl, fetch, http in non-comment lines (use `grep -v '^#' | grep -cE "curl|fetch|http"` pattern); pass if count == 0
    - COST-03 fallback test: run `CONJURE_COST=1 CONJURE_EXACT=1 ANTHROPIC_API_KEY="" bash "$CONJURE_HOME/scripts/audit-setup.sh" "$SANDBOX_DIR"`; assert advisory message containing "ANTHROPIC_API_KEY not set" is present in output; assert exit code ≤ 2
    - The cost test section uses an existing green sandbox fixture (any profile fixture from tests/fixtures/[^_]*/) via sandbox_setup
    - The section uses the pass()/fail() helpers already defined in tests/run.sh (do not redefine them)
  </behavior>
  <action>
    Two edits to tests/run.sh:

    EDIT 1 — JSON validity loop (lines 37-43): Change the find command scope from
    `find templates .claude-plugin -name '*.json' 2>/dev/null` to
    `find templates .claude-plugin lib -name '*.json' 2>/dev/null`
    This adds lib/ to the scope so lib/prices.json is validated.

    EDIT 2 — Insert the cost test section before the final summary block (the block starting
    with `echo "═══════..."`). The section must contain exactly these test assertions in order:

    Section header:
      `echo ""`
      `echo "▸ Cost estimator tests (COST-01, COST-02, COST-03)"`

    Setup: pick the first green fixture using:
      `COST_FX="$CONJURE_HOME/tests/fixtures/python-fastapi"`
      (python-fastapi is a known green fixture; use sandbox_setup "$COST_FX" to isolate)
      `sandbox_setup "$COST_FX"`
      `trap 'rm -rf "$SANDBOX_DIR"' EXIT`

    Capture output:
      `COST_OUT="$(CONJURE_COST=1 bash "$CONJURE_HOME/scripts/audit-setup.sh" "$SANDBOX_DIR" 2>&1)"`
      `COST_RC=$?`

    Test COST-01 (header present):
      If `printf '%s' "$COST_OUT" | grep -q "── Cost Estimate ──"`:
        pass "cost section header present (COST-01)"
      else fail "cost section header missing (COST-01)"

    Test COST-02 (label format — ±20% band):
      If `printf '%s' "$COST_OUT" | grep -qE "Estimate: \\\$[0-9]+\.[0-9]{2} ±20%"`:
        pass "cost label has ±20% band (COST-02)"
      else fail "cost label format wrong — expected '±20%' (COST-02)"

    Test COST-02 (label format — prices date):
      If `printf '%s' "$COST_OUT" | grep -q "prices:"`:
        pass "cost label contains pricing date (COST-02)"
      else fail "cost label missing pricing date (COST-02)"

    Test COST-02 (model name in output):
      If `printf '%s' "$COST_OUT" | grep -q "claude-sonnet-4-6"`:
        pass "cost output names the model (COST-02)"
      else fail "cost output missing model name (COST-02)"

    Test COST-01 exit code (cost section does not crash):
      If `[ "$COST_RC" -le 2 ]`:
        pass "cost section exit code ≤ 2 (COST-01)"
      else fail "cost section crashed (rc=$COST_RC) (COST-01)"

    Test COST-03 (no network calls in default path):
      `NO_NET_COUNT=$(grep -v '^#' "$CONJURE_HOME/scripts/audit-setup.sh" | grep -cE "curl|fetch|http[s]?:" || true)`
      If `[ "$NO_NET_COUNT" -eq 0 ]`:
        pass "audit-setup.sh has no network calls in default path (COST-03)"
      else fail "audit-setup.sh has $NO_NET_COUNT network call(s) in default path (COST-03)"

    Test COST-03 (--exact fallback with no API key):
      `EXACT_OUT="$(CONJURE_COST=1 CONJURE_EXACT=1 ANTHROPIC_API_KEY="" bash "$CONJURE_HOME/scripts/audit-setup.sh" "$SANDBOX_DIR" 2>&1)"`
      `EXACT_RC=$?`
      If `printf '%s' "$EXACT_OUT" | grep -q "ANTHROPIC_API_KEY not set"`:
        pass "--exact fallback advisory present when API key absent (COST-03)"
      else fail "--exact fallback advisory missing (COST-03)"
      If `[ "$EXACT_RC" -le 2 ]`:
        pass "--exact fallback exits cleanly (rc=$EXACT_RC) (COST-03)"
      else fail "--exact fallback crashed (rc=$EXACT_RC) (COST-03)"

    Cleanup:
      `rm -rf "$SANDBOX_DIR"`
      `trap - EXIT`

    The cost section must be placed immediately before the final summary block
    (the `echo "═══..."` lines at the end of tests/run.sh). Do not move or modify
    the final summary block.

    Do NOT use `bash tests/run.sh --filter cost` — the existing test runner does not support
    --filter. All tests run via `bash tests/run.sh`.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | tee /tmp/conjure-test-out.txt | tail -5; grep -c "Cost estimator tests" /tmp/conjure-test-out.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run.sh` exits 0
    - `bash tests/run.sh 2>&1 | grep -c "▸ Cost estimator tests"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✓ cost section header present"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✓ cost label has ±20% band"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✓ cost label contains pricing date"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✓ cost output names the model"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✓ audit-setup.sh has no network calls"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✓ --exact fallback advisory present"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✓ json valid: lib/prices.json"` == 1
    - `bash tests/run.sh 2>&1 | grep -c "✗"` == 0 (zero failures)
  </acceptance_criteria>
  <done>tests/run.sh contains the cost estimator test section with all COST-01/02/03 assertions passing, lib/prices.json is covered by the JSON validity loop, and the full suite exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/run.sh → sandbox fixture | Test uses an isolated sandbox copy; sandbox_setup enforces isolation |
| tests/run.sh → scripts/audit-setup.sh | Bash subprocess invocation with CONJURE_COST env var; OUTPUT captured to variable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-31 | Tampering | tests/run.sh — ANTHROPIC_API_KEY="" in test | mitigate | API key explicitly set to empty string for the fallback test; real key in test env is not used and not logged |
| T-06-32 | Information Disclosure | tests/run.sh — COST_OUT captured to variable | accept | Output is from the local audit script on a local fixture; no credentials or PII in the output |
| T-06-33 | Denial of Service | tests/run.sh — sandbox fixture create/destroy in cost test | accept | sandbox_setup uses mktemp for isolation; cleanup guarded by trap EXIT; test is fast (no network) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; test assertions only |
</threat_model>

<verification>
After this plan completes:

1. `bash tests/run.sh` exits 0 — all assertions pass, zero failures
2. `bash tests/run.sh 2>&1 | grep -c "✓"` >= (prior passing count + 8 new assertions)
3. `bash tests/run.sh 2>&1 | grep "✗"` produces no output
4. `bash tests/run.sh 2>&1 | grep "json valid: lib/prices.json"` exits 0
5. `bash tests/run.sh 2>&1 | grep "Cost estimator tests"` exits 0
</verification>

<success_criteria>
- bash tests/run.sh exits 0 with all cost estimator assertions passing (COST-01, COST-02, COST-03)
- lib/prices.json is included in the JSON validity loop and passes (COST-01 per D-03)
- The full regression suite (all pre-existing assertions from phases 1-5 plus new cost assertions) exits 0
- The VALIDATION.md Nyquist requirement is satisfied: every COST requirement has at least one automated assertion in tests/run.sh
</success_criteria>

<output>
Create `.planning/phases/06-cost-estimator/06-03-SUMMARY.md` when done.
</output>
