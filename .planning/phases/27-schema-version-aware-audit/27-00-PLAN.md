---
phase: 27-schema-version-aware-audit
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/cc-schema.json
  - tests/fixtures/_schema-audit/valid/harness/CLAUDE.md
  - tests/fixtures/_schema-audit/valid/harness/.claude/settings.json
  - tests/fixtures/_schema-audit/valid/harness/.claude/skills/ok-skill/SKILL.md
  - tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md
  - tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json
  - tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json
  - tests/fixtures/_schema-audit-stale/cc-schema-stale.json
  - tests/run.sh
autonomous: true
requirements:
  - SCHM-01
  - SCHM-02
  - SCHM-03
  - SCHM-04
  - SCHM-05

must_haves:
  truths:
    - "lib/cc-schema.json exists, is valid JSON (jq empty passes), contains exactly 30 hook_events entries, 16 skill_frontmatter entries, and a renamed_events map with SessionStop→SessionEnd"
    - "tests/run.sh contains a Phase 27 block (▸ Phase 27 — Schema-Version-Aware Audit (SCHM-01..05))"
    - "Running bash tests/run.sh exits non-zero with FAIL count on all SCHM-* tests (graceful-red — audit-setup.sh SCHM extension and check.sh SCHM extension are absent)"
    - "tests/fixtures/_schema-audit/valid/harness/ contains a SKILL.md with only known fields and a settings.json with only valid hook events — passes all schema checks"
    - "tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md has disallowed-tools as YAML mapping {Bash: true} — negative fixture for SCHM-01"
    - "tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json has disableBypassPermissionsMode: true (boolean) at BOTH top-level and under permissions — negative fixture for SCHM-02"
    - "tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json has hooks.SessionStop — renamed event negative fixture for SCHM-03"
    - "tests/fixtures/_schema-audit-stale/cc-schema-stale.json has generated date >90 days in the past — staleness advisory fixture"
  artifacts:
    - path: "lib/cc-schema.json"
      provides: "Authoritative bundled Claude Code schema snapshot (30 events, 16 skill fields, settings_keys version map)"
      contains: "schema_version"
    - path: "tests/fixtures/_schema-audit/valid/harness/.claude/skills/ok-skill/SKILL.md"
      provides: "Valid SKILL.md fixture (all known fields, correct types)"
    - path: "tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md"
      provides: "Negative fixture: disallowed-tools as YAML block mapping {Bash: true}"
    - path: "tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json"
      provides: "Negative fixture: boolean disableBypassPermissionsMode (both paths)"
    - path: "tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json"
      provides: "Negative fixture: renamed hook event SessionStop"
    - path: "tests/fixtures/_schema-audit-stale/cc-schema-stale.json"
      provides: "Staleness fixture: generated date >90 days ago"
    - path: "tests/run.sh"
      provides: "Phase 27 graceful-red SCHM test block"
      contains: "Phase 27 — Schema-Version-Aware Audit"
  key_links:
    - from: "tests/run.sh Phase 27 block"
      to: "scripts/audit-setup.sh"
      via: "P27_AUDIT_OK presence guard"
      pattern: "P27_AUDIT_OK=.*\\[ -f.*audit-setup.sh"
    - from: "tests/run.sh Phase 27 block"
      to: "lib/cc-schema.json"
      via: "SCHEMA_FILE variable"
      pattern: "SCHEMA_FILE.*lib/cc-schema.json"
---

<objective>
Wave 0: Build the bundled schema file and test infrastructure for Phase 27.

Purpose: Nyquist Wave 0 invariant — lib/cc-schema.json (the authoritative bundled schema) and all fixture harnesses must exist BEFORE any SCHM check logic lands, so that Waves 1–3 implementations can be verified as they ship. When audit-setup.sh SCHM section and check.sh SCHM section are absent (as they will be after this plan), all SCHM-* test cases must FAIL (graceful-red). Mirrors the Phase 25/26 Wave 0 pattern exactly.

Output: lib/cc-schema.json (verbatim from RESEARCH authoritative content — 30 events, 16 fields), four negative fixture directories (badfield / disablebypass / hookevent / stale), one valid fixture directory, and the Phase 27 graceful-red SCHM test block appended to tests/run.sh.

Success-criteria reconciliation: ROADMAP Phase 27 SC numbers "34 events" / "14 fields" are superseded by the authoritative live-doc counts (30 / 16). The bundled lib/cc-schema.json is correct-by-construction from 27-RESEARCH.md. Verification accepts 30 events and 16 skill_frontmatter entries as correct.
</objective>

<execution_context>
@/Users/mohandoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohandoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-schema-version-aware-audit/27-CONTEXT.md
@.planning/phases/27-schema-version-aware-audit/27-RESEARCH.md
@.planning/phases/27-schema-version-aware-audit/27-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write lib/cc-schema.json verbatim from RESEARCH authoritative content</name>
  <files>lib/cc-schema.json</files>
  <read_first>
    - .planning/phases/27-schema-version-aware-audit/27-RESEARCH.md (section "Authoritative cc-schema.json Content" — the exact JSON block to write verbatim; 30 hook events, 16 skill_frontmatter fields with types, settings_keys introduced_version map, renamed_events, schema_version, generated, cc_version, _comment fields)
    - lib/prices.json (shape reference: bundled JSON lib pattern, _comment field convention)
  </read_first>
  <action>
Write lib/cc-schema.json with the EXACT JSON content from the "Authoritative cc-schema.json Content" section of 27-RESEARCH.md. Do not add, remove, or reorder any fields — the content is authoritative and must be written verbatim.

Key shape invariants to verify after writing:
- Top-level keys: _comment, schema_version ("1"), generated ("2026-06-03"), cc_version ("2.1.161"), hook_events (array), renamed_events (object), settings_keys (object), skill_frontmatter (object).
- hook_events array has exactly 30 string entries in the order listed in RESEARCH.md.
- renamed_events has exactly one entry: "SessionStop": "SessionEnd".
- skill_frontmatter has exactly 16 entries with types: "string", "array-or-space-string", "boolean", "object".
- settings_keys has 28 entries: 20 with value "all" plus 8 with explicit version strings (skillOverrides: "2.1.129", maxSkillDescriptionChars: "2.1.105", skillListingBudgetFraction: "2.1.105", workflowKeywordTriggerEnabled: "2.1.157", worktree.bgIsolation: "2.1.143", policyHelper: "2.1.136", parentSettingsBehavior: "2.1.133", disableRemoteControl: "2.1.128").

After writing: run `jq empty lib/cc-schema.json` to validate well-formed JSON. This is the only schema file — zero runtime fetch, zero external dependencies.
  </action>
  <verify>
    <automated>
      jq empty lib/cc-schema.json &amp;&amp;
      jq -e '.schema_version == "1"' lib/cc-schema.json &amp;&amp;
      jq -e '(.hook_events | length) == 30' lib/cc-schema.json &amp;&amp;
      jq -e '(.skill_frontmatter | keys | length) == 16' lib/cc-schema.json &amp;&amp;
      jq -e '.renamed_events.SessionStop == "SessionEnd"' lib/cc-schema.json &amp;&amp;
      jq -e '.settings_keys.skillOverrides == "2.1.129"' lib/cc-schema.json &amp;&amp;
      jq -e '(.hook_events | map(select(. == "PreToolUse")) | length) == 1' lib/cc-schema.json &amp;&amp;
      jq -e '(.hook_events | map(select(. == "SessionEnd")) | length) == 1' lib/cc-schema.json &amp;&amp;
      jq -e '(.skill_frontmatter["disallowed-tools"]) == "array-or-space-string"' lib/cc-schema.json &amp;&amp;
      jq -e '(.skill_frontmatter["hooks"]) == "object"' lib/cc-schema.json &amp;&amp;
      echo "lib/cc-schema.json valid — 30 events, 16 fields, renamed_events OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - lib/cc-schema.json passes `jq empty` (well-formed JSON)
    - `jq -e '(.hook_events | length) == 30' lib/cc-schema.json` exits 0
    - `jq -e '(.skill_frontmatter | keys | length) == 16' lib/cc-schema.json` exits 0
    - `jq -e '.renamed_events.SessionStop == "SessionEnd"' lib/cc-schema.json` exits 0
    - `jq -e '.settings_keys.skillOverrides == "2.1.129"' lib/cc-schema.json` exits 0 (spot-check version-tagged key)
    - `jq -e '.skill_frontmatter["disallowed-tools"] == "array-or-space-string"' lib/cc-schema.json` exits 0
    - `jq -e '.skill_frontmatter.hooks == "object"' lib/cc-schema.json` exits 0
    - File does NOT contain any fetch/curl/wget call (grep check) — zero-egress invariant
    - The `generated` field matches "2026-06-03" (from RESEARCH date)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create fixture harnesses for SCHM-01..05 tests</name>
  <files>
    tests/fixtures/_schema-audit/valid/harness/CLAUDE.md
    tests/fixtures/_schema-audit/valid/harness/.claude/settings.json
    tests/fixtures/_schema-audit/valid/harness/.claude/skills/ok-skill/SKILL.md
    tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md
    tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json
    tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json
    tests/fixtures/_schema-audit-stale/cc-schema-stale.json
  </files>
  <read_first>
    - .planning/phases/27-schema-version-aware-audit/27-RESEARCH.md (Pattern 1: YAML frontmatter type detection; Pattern 6: SCHM-02 both paths; §Wave 0 Gaps; §SKILL.md Frontmatter Schema 16 fields table)
    - .planning/phases/27-schema-version-aware-audit/27-VALIDATION.md (§Wave 0 Requirements: full checklist of required fixture dirs)
    - tests/fixtures/_emit-policy-broken/harness/.claude/settings.json (negative fixture pattern to mirror)
    - tests/fixtures/go-gin/CLAUDE.md (valid CLAUDE.md header style to mirror)
  </read_first>
  <action>
Create five fixture trees under tests/fixtures/. All are _-prefixed so generic audit/golden loops exclude them.

TREE 1 — tests/fixtures/_schema-audit/valid/harness/ (happy-path, passes all SCHM checks):
  - valid/harness/CLAUDE.md: two-line stub with ## Project header and "Test harness for conjure schema-audit fixture." — no @imports.
  - valid/harness/.claude/settings.json: minimal JSON with hooks using only KNOWN event names (use PreToolUse and PostToolUse) and NO boolean disableBypassPermissionsMode. Shape: {"hooks": {"PreToolUse": [{"matcher": "*", "hooks": [{"type": "command", "command": ".claude/hooks/pre.mjs"}]}]}}
  - valid/harness/.claude/skills/ok-skill/SKILL.md: SKILL.md with all-string-or-array-typed fields (no object-typed fields). Frontmatter must use only known 16-field names. Example: name, description, allowed-tools (as YAML array ["Read", "Bash"]), user-invocable (false). NO disallowed-tools as object.

TREE 2 — tests/fixtures/_schema-audit-badfield/harness/ (negative: SCHM-01 invalid type):
  - harness/.claude/skills/bad-skill/SKILL.md: SKILL.md where disallowed-tools is a YAML block mapping (object), NOT a list or string. Must use block-style YAML (NOT inline {}) to exercise the awk two-line lookahead pattern. Shape of frontmatter:
      ---
      name: bad-skill
      description: Test skill with wrong-typed frontmatter for SCHM-01 negative test
      disallowed-tools:
        Bash: true
        Write: true
      ---
    The indented "Bash: true" line under an empty disallowed-tools value IS the object-typing signal. This tests the awk lookahead (not just inline {Bash: true}).

TREE 3 — tests/fixtures/_schema-audit-disablebypass/harness/ (negative: SCHM-02 both paths):
  - harness/.claude/settings.json: settings.json with boolean disableBypassPermissionsMode set to true at BOTH the top-level AND under permissions. Shape:
      {
        "disableBypassPermissionsMode": true,
        "permissions": {
          "disableBypassPermissionsMode": true
        }
      }
    Both paths checked by SCHM-02 (Pattern 6 from RESEARCH). Both booleans must be present so the test covers both jq path checks.

TREE 4 — tests/fixtures/_schema-audit-hookevent/harness/ (negative: SCHM-03 renamed/unknown event):
  - harness/.claude/settings.json: settings.json with a renamed event (SessionStop) AND an unknown event (UnknownEvent42) in the hooks object. Shape:
      {
        "hooks": {
          "SessionStop": [{"matcher": "*", "hooks": [{"type": "command", "command": "echo renamed"}]}],
          "UnknownEvent42": [{"matcher": "*", "hooks": [{"type": "command", "command": "echo unknown"}]}]
        }
      }
    SessionStop → should be flagged as renamed (use SessionEnd). UnknownEvent42 → unknown event not in schema.

TREE 5 — tests/fixtures/_schema-audit-stale/ (staleness advisory: SCHM-STALE):
  - cc-schema-stale.json: a copy of lib/cc-schema.json content shape but with `generated` set to "2025-01-01" (>90 days before 2026-06-03). The staleness check reads the `generated` field to compute age. This file is NOT placed at lib/cc-schema.json — it is a separate fixture that the test copies into place temporarily to exercise the staleness path without touching the real schema.

All _-prefixed directories are excluded from generic audit loops per CLAUDE.md conventions.
  </action>
  <verify>
    <automated>
      test -f tests/fixtures/_schema-audit/valid/harness/CLAUDE.md &amp;&amp;
      test -f tests/fixtures/_schema-audit/valid/harness/.claude/settings.json &amp;&amp;
      jq empty tests/fixtures/_schema-audit/valid/harness/.claude/settings.json &amp;&amp;
      jq -e '(.hooks.PreToolUse | type) == "array"' tests/fixtures/_schema-audit/valid/harness/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_schema-audit/valid/harness/.claude/skills/ok-skill/SKILL.md &amp;&amp;
      grep -q "^name:" tests/fixtures/_schema-audit/valid/harness/.claude/skills/ok-skill/SKILL.md &amp;&amp;
      test -f tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md &amp;&amp;
      grep -q "^disallowed-tools:" tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md &amp;&amp;
      grep -qE "^  Bash:" tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md &amp;&amp;
      test -f tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json &amp;&amp;
      jq -e '(.disableBypassPermissionsMode | type) == "boolean"' tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json &amp;&amp;
      jq -e '(.permissions.disableBypassPermissionsMode | type) == "boolean"' tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json &amp;&amp;
      jq -e '.hooks.SessionStop != null' tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json &amp;&amp;
      jq -e '.hooks.UnknownEvent42 != null' tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json &amp;&amp;
      test -f tests/fixtures/_schema-audit-stale/cc-schema-stale.json &amp;&amp;
      jq -e '.generated == "2025-01-01"' tests/fixtures/_schema-audit-stale/cc-schema-stale.json &amp;&amp;
      echo "All SCHM fixture files present with correct shapes"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/_schema-audit/valid/harness/.claude/settings.json passes `jq -e '(.hooks.PreToolUse | type) == "array"'` and has NO disableBypassPermissionsMode key of boolean type
    - tests/fixtures/_schema-audit/valid/harness/.claude/skills/ok-skill/SKILL.md frontmatter uses only known field names (name, description, allowed-tools, user-invocable) and NO object-typed fields
    - tests/fixtures/_schema-audit-badfield/harness/.claude/skills/bad-skill/SKILL.md has `disallowed-tools:` on a key line with an empty value AND indented `  Bash: true` on the next line (block-style YAML mapping, NOT inline `{Bash: true}`)
    - tests/fixtures/_schema-audit-disablebypass/harness/.claude/settings.json passes `jq -e '(.disableBypassPermissionsMode | type) == "boolean"'` AND `jq -e '(.permissions.disableBypassPermissionsMode | type) == "boolean"'`
    - tests/fixtures/_schema-audit-hookevent/harness/.claude/settings.json passes `jq -e '.hooks | has("SessionStop")'` AND `jq -e '.hooks | has("UnknownEvent42")'`
    - tests/fixtures/_schema-audit-stale/cc-schema-stale.json passes `jq -e '.generated == "2025-01-01"'` and `jq -e '(.hook_events | length) == 30'` (same shape as real schema, just with old date)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Phase 27 graceful-red SCHM test block to tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh (read the last 60 lines to find the insertion point: immediately BEFORE the "# Clean up any gh-hiding stub dirs" comment near the end — insert Phase 27 block there, exactly as Phase 26 was inserted before the same marker)
    - tests/run.sh (read ~20 lines around the Phase 26 block header to mirror the exact header width and presence-guard pattern: P26_EMIT_OK=0 / [ -f "$P26_EMIT_SH" ] && P26_EMIT_OK=1)
    - .planning/phases/27-schema-version-aware-audit/27-VALIDATION.md (§Per-Task Verification Map: full list of SCHM-* test case IDs)
    - .planning/phases/27-schema-version-aware-audit/27-RESEARCH.md (§Architecture Patterns Pattern 2: SCHM-03 jq hook event validation; Pattern 3: --json accumulation; §Pitfall 2: stdout/stderr contamination in --json mode; §Pitfall 4: jq .hooks // {} null coalescing)
  </read_first>
  <action>
Append a Phase 27 test block to tests/run.sh IMMEDIATELY BEFORE the "for _s in $GH_HIDE_STUBS" cleanup loop (after the last Phase 26 test and before the GH_HIDE_STUBS loop). Do NOT alter the summary block or the final `[ "$FAIL" -eq 0 ]` gate.

SECTION HEADER (matching Phase 26 width exactly):
  # ──────────────────────────────────────────────────────────────────────────────
  # Phase 27 — Schema-Version-Aware Audit (SCHM-01..05)
  # ──────────────────────────────────────────────────────────────────────────────

PRESENCE GUARDS:
  P27_AUDIT_SH="$CONJURE_HOME/scripts/audit-setup.sh"
  P27_CHECK_SH="$CONJURE_HOME/scripts/check.sh"
  P27_SCHEMA_FILE="$CONJURE_HOME/lib/cc-schema.json"
  P27_AUDIT_OK=0
  P27_CHECK_OK=0
  P27_SCHEMA_OK=0
  [ -f "$P27_AUDIT_SH" ] && P27_AUDIT_OK=1
  [ -f "$P27_CHECK_SH" ] && P27_CHECK_OK=1
  [ -f "$P27_SCHEMA_FILE" ] && jq -e '(.hook_events | length) == 30' "$P27_SCHEMA_FILE" >/dev/null 2>&1 && P27_SCHEMA_OK=1

Echo header:
  echo
  echo "▸ Phase 27 — Schema-Version-Aware Audit (SCHM-01..05)"

Implement these test cases (each using its own mktemp sandbox with EXIT trap, per Phase 25/26 pattern):

SCHM-SCHEMA: verify lib/cc-schema.json exists with correct shape (independent of audit/check code):
  - Assert P27_SCHEMA_OK=1; assert 30 hook events; assert 16 skill_frontmatter entries; assert renamed_events.SessionStop=SessionEnd; assert skill_frontmatter["disallowed-tools"]="array-or-space-string"
  - pass/fail "lib/cc-schema.json has 30 hook events, 16 skill fields, renamed_events map (SCHM-SCHEMA)"

SCHM-01-badtype: SKILL.md with block-style object-typed disallowed-tools → audit must fail exit 2:
  - Build mktemp sandbox from _schema-audit-badfield fixture; init git; add minimal CLAUDE.md; run audit-setup.sh on it; assert exit code 2 AND output contains "disallowed-tools"; guard with P27_AUDIT_OK
  - pass/fail "audit fails when SKILL.md disallowed-tools is YAML object mapping (SCHM-01-badtype)"

SCHM-01-valid: valid SKILL.md with array-typed disallowed-tools → audit must NOT produce a SCHM-01 fail:
  - Build mktemp sandbox from _schema-audit/valid fixture; init git; add CLAUDE.md; run audit; assert output does NOT contain "SCHM-01" fail message for valid fields; guard with P27_AUDIT_OK
  - pass/fail "audit accepts valid SKILL.md frontmatter field types (SCHM-01-valid)"

SCHM-01-unknown: SKILL.md with an unknown frontmatter field → audit must WARN (exit 1 or emit warning text), NOT exit 2:
  - Build mktemp sandbox; write SKILL.md with frontmatter field "totally_unknown_field: value"; run audit; assert exit code != 2 (warn not fail); guard with P27_AUDIT_OK
  - pass/fail "audit warns (not fails) on unknown SKILL.md frontmatter field (SCHM-01-unknown)"

SCHM-02-permissions: boolean disableBypassPermissionsMode at permissions.* path → audit must fail exit 2:
  - Build mktemp sandbox from _schema-audit-disablebypass fixture (which has BOTH paths set to boolean true); run audit; assert exit code 2 AND output contains "disableBypassPermissionsMode"; guard with P27_AUDIT_OK
  - pass/fail "audit fails when permissions.disableBypassPermissionsMode is boolean (SCHM-02-permissions)"

SCHM-02-toplevel: boolean disableBypassPermissionsMode at top-level path → audit must fail exit 2:
  - Build mktemp sandbox with ONLY top-level boolean disableBypassPermissionsMode (no permissions. path); run audit; assert exit code 2; guard with P27_AUDIT_OK
  - pass/fail "audit fails when top-level disableBypassPermissionsMode is boolean (SCHM-02-toplevel)"

SCHM-03-renamed: settings.json with SessionStop hook → check must fail exit 2:
  - Build mktemp sandbox from _schema-audit-hookevent fixture; init git; run check.sh on it; assert exit code 2 AND output contains "SessionStop" or "renamed"; guard with P27_CHECK_OK
  - pass/fail "check fails when settings.json uses renamed hook event SessionStop (SCHM-03-renamed)"

SCHM-03-unknown: settings.json with unknown hook event → check must fail exit 2:
  - Reuse the same _schema-audit-hookevent fixture (has UnknownEvent42); assert exit code 2 AND output contains "UnknownEvent42" or "unknown"; guard with P27_CHECK_OK
  - pass/fail "check fails when settings.json uses unknown hook event (SCHM-03-unknown)"

SCHM-04-schema: check --schema on a valid harness → outputs per-key version info, exit 0 or 1 (info only, never exit 2):
  - Build mktemp sandbox with .claude/settings.json containing "skillOverrides" key; run check.sh with CONJURE_SCHEMA=1; assert exit code != 2; guard with P27_CHECK_OK
  - pass/fail "check --schema emits per-key version info and does not fail (SCHM-04-schema)"

SCHM-STALE: audit with a >90-day generated date → audit warns but does NOT exit 2:
  - Build mktemp sandbox from _schema-audit/valid fixture; temporarily swap lib/cc-schema.json for the stale fixture (cp + restore in trap); run audit; assert output contains "days old" or "stale"; assert exit code != 2; restore original; guard with P27_AUDIT_OK
  - Alternative if swap is unsafe: write a custom test that calls the staleness check function directly using the stale fixture date
  - pass/fail "audit warns (not fails) when cc-schema.json is >90 days old (SCHM-STALE)"

SCHM-05-json: audit --json emits parseable JSON to stdout, human text to stderr:
  - Build mktemp sandbox from _schema-audit/valid fixture; run audit-setup.sh with CONJURE_JSON=1; capture stdout and stderr separately; assert stdout passes `jq -e '.status'`; assert stdout does NOT contain human-readable audit header; assert exit code follows fail/warn/pass semantics; guard with P27_AUDIT_OK
  - pass/fail "audit --json emits JSON-only stdout parseable by jq (SCHM-05-json)"

SCHM-05-exit2: audit --json on a bad fixture fails exit 2 with status:"fail" in JSON:
  - Build mktemp sandbox from _schema-audit-disablebypass fixture; run audit-setup.sh with CONJURE_JSON=1; assert exit code 2; capture stdout; assert `jq -e '.status == "fail"'` on stdout; guard with P27_AUDIT_OK
  - pass/fail "audit --json exit 2 on fail with JSON status:fail (SCHM-05-exit2)"

All test cases:
  - Guard audit/check invocations behind P27_AUDIT_OK / P27_CHECK_OK (else fail "... not implemented — Wave N must create ...")
  - Use `mktemp -d` per test with `trap 'rm -rf "$P27_xxx_DIR"' EXIT` and `trap - EXIT` after cleanup
  - Use `pass`/`fail` helpers already in tests/run.sh (do NOT redefine)
  - POSIX 3.2+: no associative arrays, no mapfile, no local -n
  - Use `printf '%s\n'` not bare echo for content assertions; `>&2` redirects in JSON mode
  - Use `grep -v '^#' | grep -c` pattern when counting non-comment lines for shellcheck grep-gate hygiene
  - For stdout/stderr capture: use `STDOUT=$(...); STDERR=$(... 2>&1 1>/dev/null)` or redirect to tmpfiles
  - Insert IMMEDIATELY BEFORE the "for _s in $GH_HIDE_STUBS" loop — do not alter anything below that point
  </action>
  <verify>
    <automated>
      grep -c "Phase 27" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "P27_AUDIT_OK" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "SCHM-01-badtype" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "SCHM-05-exit2" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      grep -c "SCHM-STALE" tests/run.sh | grep -qE "^[1-9]" &amp;&amp;
      shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh &amp;&amp;
      bash tests/run.sh 2>&amp;1 | grep -E "FAIL.*(SCHM)" | grep -c "FAIL" | grep -qE "^[1-9]" &amp;&amp;
      echo "Phase 27 block present, shellcheck clean, and graceful-red on SCHM-* tests"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/run.sh contains a "Phase 27 — Schema-Version-Aware Audit (SCHM-01..05)" section header
    - All 12 SCHM-* grep tags are present: SCHM-SCHEMA, SCHM-01-badtype, SCHM-01-valid, SCHM-01-unknown, SCHM-02-permissions, SCHM-02-toplevel, SCHM-03-renamed, SCHM-03-unknown, SCHM-04-schema, SCHM-STALE, SCHM-05-json, SCHM-05-exit2
    - `bash tests/run.sh 2>&1 | grep -E "FAIL.*(SCHM-0[1-5]|SCHM-[A-Z])" | grep -c FAIL` exits 0 with count >= 10 (graceful-red — all SCHM-01..05 tests fail because audit-setup.sh and check.sh SCHM sections are absent)
    - SCHM-SCHEMA passes (it tests lib/cc-schema.json which exists after Task 1)
    - `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` passes (no new errors)
    - The existing summary block and `[ "$FAIL" -eq 0 ]` gate are unmodified
    - All Phase 26 POL-* tests are unmodified (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/cc-schema.json → audit-setup.sh / check.sh | Schema file is team-authored; read via jq at runtime; no user-supplied input at read time |
| fixture file content → tests/run.sh | Fixtures are static, team-authored; not user-supplied at test execution time |
| negative fixture (boolean disableBypassPermissionsMode) → audit test | Deliberately wrong-type boolean in fixture; must NOT appear in any happy-path golden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-W0-01 | Tampering | lib/cc-schema.json content (hook_events array) | mitigate | Task 1 verify step asserts `(.hook_events | length) == 30` via jq; any deletion or addition is caught before Wave 1 proceeds |
| T-27-W0-02 | Tampering | fixture _schema-audit-disablebypass (boolean DBPM must not become string) | mitigate | Task 2 verify step asserts `(.disableBypassPermissionsMode | type) == "boolean"` — any accidental type coercion would fail the verify |
| T-27-W0-03 | Information Disclosure | cc-schema-stale.json in tests/fixtures/ | accept | Contains only a dated copy of public CC schema data; no credentials or PII |
| T-27-W0-04 | Denial of Service | A false-negative in graceful-red (SCHM-* tests silently pass when implementation absent) | mitigate | Each test is gated on P27_AUDIT_OK / P27_CHECK_OK; when the implementation is absent, the test must call `fail "... not implemented"` — never silently pass |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — bash + jq only |
</threat_model>

<verification>
After all three tasks complete:
- `jq -e '(.hook_events | length) == 30' lib/cc-schema.json` exits 0
- `jq -e '(.skill_frontmatter | keys | length) == 16' lib/cc-schema.json` exits 0
- `bash tests/run.sh 2>&1 | grep -E "FAIL.*(SCHM-0[1-5]|SCHM-STALE|SCHM-SCHEMA)" | grep -c FAIL` shows >= 10 FAIL (all except SCHM-SCHEMA which should PASS)
- `shellcheck -S error -e SC2164,SC2044,SC2034,SC2155 tests/run.sh` exits 0
- All five fixture directories exist under tests/fixtures/
</verification>

<success_criteria>
- Wave 0 complete: lib/cc-schema.json (authoritative 30 events / 16 fields) + all fixtures + graceful-red SCHM test block exist
- `bash tests/run.sh` exits non-zero (FAIL count > 0) because audit-setup.sh and check.sh SCHM sections are absent
- SCHM-SCHEMA test PASSES (lib/cc-schema.json is present and structurally correct)
- Plan 01 (Wave 1) can now implement SCHM-01 and SCHM-02 in scripts/audit-setup.sh to turn those tests green
</success_criteria>

<output>
Create .planning/phases/27-schema-version-aware-audit/27-00-SUMMARY.md when done.
</output>
