---
phase: 31
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/MANUAL-UAT.md
autonomous: true
requirements:
  - UAT-03
must_haves:
  truths:
    - "tests/MANUAL-UAT.md exists with checklists for MDM hardware (macOS plist + Windows PS1) and managed-settings deploy scenarios"
    - "Each scenario section has: prerequisites, numbered steps, expected result, checkbox items, and a date/version field"
    - "The file addresses the deferred Phase 25 HUMAN-UAT (live claude --validate) and Phase 26 HUMAN-UAT (MDM hardware, managed-settings deploy)"
  artifacts:
    - path: "tests/MANUAL-UAT.md"
      provides: "MDM + managed-settings + claude-validate UAT checklists"
      contains: "- [ ]"
  key_links: []
---

<objective>
Create tests/MANUAL-UAT.md with three manual UAT checklist sections: (1) live claude --validate / plugin validate smoke, (2) MDM hardware deploy (macOS plist + Windows PS1), and (3) managed-settings deploy. Each section follows the D-10 format: prerequisites, numbered steps, expected result, checkbox items, and a date/version field.

Purpose: Closes UAT-03 and addresses the two deferred HUMAN-UAT items from Phase 25 (claude plugin validate) and Phase 26 (MDM hardware and managed-settings deploy). These steps require real hardware and cannot be automated.

Output: tests/MANUAL-UAT.md with three complete checklist sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-CONTEXT.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-RESEARCH.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/MANUAL-UAT.md with three UAT checklists</name>
  <files>tests/MANUAL-UAT.md</files>
  <read_first>
    - FAILURE-MODES.md (lines 168-178) — doc style reference: symptom/cause/fix structure with fenced code blocks, same markdown family
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-CONTEXT.md (D-10, D-08 sections) — exact format mandate and scenario list
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md (tests/MANUAL-UAT.md section at end) — style guidance
    - .planning/STATE.md (Deferred Items table) — confirms exactly which v0.7.0 deferred items this file closes
  </read_first>
  <action>
    Create tests/MANUAL-UAT.md as a new file. Header: `# Conjure — Manual UAT Checklists` with a brief paragraph stating these tests require real hardware or live binary access and cannot be automated, and that they address the deferred HUMAN-UAT items from v0.7.0 Phases 25 and 26.

    Include three H2 sections in this order:

    **Section 1: `## UAT-25: Live claude plugin validate Smoke Test`**
    Address the Phase 25 HUMAN-UAT deferred item (live claude --validate / claude plugin validate).
    Prerequisites: claude CLI installed (`command -v claude` succeeds), conjure installed.
    Steps: (1) Create a temp dir, (2) Run `conjure emit-plugin --output <tmpdir>` to scaffold a .claude-plugin/, (3) Run `claude plugin validate <tmpdir>`, (4) Confirm exit code 0 and no error output.
    Expected result: `claude plugin validate` exits 0 with no errors on a conjure-emitted plugin.
    Checkboxes: `- [ ] conjure emit-plugin emits valid .claude-plugin/ structure`, `- [ ] claude plugin validate exits 0`, `- [ ] No error messages in output`.
    Date/version field: `**Verified:** <!-- date --> · conjure <!-- version --> · claude <!-- version -->`.

    **Section 2: `## UAT-26a: MDM Hardware Deploy — macOS Managed Plist`**
    Address the Phase 26 HUMAN-UAT MDM hardware (macOS) deferred item.
    Prerequisites: macOS device enrolled in MDM, conjure installed, access to MDM console to push a profile.
    Steps: (1) Run `conjure emit-policy --overlay mdm --output <dir>` to generate the macOS plist, (2) Review the emitted `com.anthropic.claude-code.plist` for required keys (`PayloadType`, `disableBypassPermissionsMode`, sandbox block), (3) Import plist into MDM console and push to a test device, (4) On the enrolled device, verify the plist is in `/Library/Managed Preferences/com.anthropic.claude-code.plist`, (5) Open Claude Code on the enrolled device and confirm policy-restricted behaviour is enforced.
    Expected result: Claude Code on the enrolled device respects the managed policy (e.g. disableBypassPermissionsMode blocks bypass).
    Checkboxes: `- [ ] emit-policy generates plist without error`, `- [ ] Plist contains PayloadType and disableBypassPermissionsMode keys`, `- [ ] MDM push succeeds (check MDM console)`, `- [ ] Managed plist present at /Library/Managed Preferences/ on enrolled device`, `- [ ] Claude Code enforces policy on enrolled device`.
    Date/version field: `**Verified:** <!-- date --> · conjure <!-- version --> · macOS <!-- version --> · MDM <!-- product -->`.

    **Section 3: `## UAT-26b: MDM Hardware Deploy — Windows Managed Policy (PS1)`**
    Address the Phase 26 HUMAN-UAT MDM hardware (Windows) deferred item.
    Prerequisites: Windows device (or VM), PowerShell 5+, registry access, conjure installed.
    Steps: (1) Run `conjure emit-policy --overlay mdm --output <dir>` to generate the Windows PS1, (2) Review emitted `apply-claude-policy.ps1` for expected registry keys, (3) Run the PS1 as Administrator on the test device (`Set-ExecutionPolicy RemoteSigned` if needed), (4) Verify registry key `HKLM:\SOFTWARE\Anthropic\ClaudeCode` contains `DisableBypassPermissionsMode=1` (or equivalent), (5) Open Claude Code and confirm policy-restricted behaviour is enforced.
    Expected result: Claude Code on the Windows device respects the managed policy after PS1 execution.
    Checkboxes: `- [ ] emit-policy generates PS1 without error`, `- [ ] PS1 contains expected registry key writes`, `- [ ] PS1 executes without errors as Administrator`, `- [ ] Registry key present after PS1 execution`, `- [ ] Claude Code enforces policy on Windows device`.
    Date/version field: `**Verified:** <!-- date --> · conjure <!-- version --> · Windows <!-- version -->`.

    **Section 4: `## UAT-26c: Managed-Settings Deploy (managed-settings.json)`**
    Address the Phase 26 HUMAN-UAT managed-settings deploy deferred item.
    Prerequisites: conjure installed, target repo or test directory.
    Steps: (1) Run `conjure emit-policy --overlay managed --output <dir>` to generate `managed-settings.json`, (2) Review the file for required keys (`permissions`, `sandbox`), (3) Place `managed-settings.json` in the `~/.claude/` directory of the target user, (4) Open Claude Code and verify the settings are applied (check Claude Code's settings UI or behaviour), (5) Run `conjure audit <target-repo>` and confirm no policy-related warnings.
    Expected result: Claude Code loads managed-settings.json and applies permissions/sandbox configuration.
    Checkboxes: `- [ ] emit-policy generates managed-settings.json without error`, `- [ ] File contains permissions and sandbox keys`, `- [ ] Claude Code loads the file on startup (no "invalid settings" error)`, `- [ ] conjure audit passes on target repo`.
    Date/version field: `**Verified:** <!-- date --> · conjure <!-- version --> · claude <!-- version -->`.

    End the document with a `## Notes` section: "Run the automated live-test gates first (`CONJURE_LIVE_TEST=1 bash tests/run.sh`) before performing manual UAT. The automated gates cover UAT-01 (claude plugin validate in a sandbox) and UAT-02 (live promptfoo eval) and are prerequisites for the manual steps above."
  </action>
  <verify>
    <automated>test -f tests/MANUAL-UAT.md && grep -c '\- \[ \]' tests/MANUAL-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/MANUAL-UAT.md` exits 0
    - `grep -c '## UAT-' tests/MANUAL-UAT.md` returns 4 (four UAT sections: UAT-25, UAT-26a, UAT-26b, UAT-26c)
    - `grep -c '\- \[ \]' tests/MANUAL-UAT.md` returns 18 or more (at least 18 checkbox items total across all sections)
    - `grep -c 'Verified:' tests/MANUAL-UAT.md` returns 4 (one date/version field per section)
    - `grep -q 'Prerequisites' tests/MANUAL-UAT.md` exits 0 (prerequisites block present)
    - `grep -q 'Expected result' tests/MANUAL-UAT.md` exits 0
    - `grep -q 'UAT-25' tests/MANUAL-UAT.md` exits 0 (Phase 25 deferred item addressed)
    - `grep -q 'MDM' tests/MANUAL-UAT.md` exits 0 (MDM scenario present)
    - `grep -q 'managed-settings' tests/MANUAL-UAT.md` exits 0 (managed-settings scenario present)
  </acceptance_criteria>
  <done>tests/MANUAL-UAT.md exists with four scenario sections (UAT-25, UAT-26a, UAT-26b, UAT-26c), each with prerequisites, numbered steps, expected result, checkboxes, and date/version field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → human operator | MANUAL-UAT.md instructs operators to run PowerShell as Administrator and push MDM profiles; instructions must be safe and not suggest unsafe commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | tests/MANUAL-UAT.md PowerShell steps | accept | Instructions are documentation only; operator reviews before executing; Set-ExecutionPolicy note included |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this plan |
</threat_model>

<verification>
```bash
test -f tests/MANUAL-UAT.md && echo "EXISTS" || echo "MISSING"
grep -c '## UAT-' tests/MANUAL-UAT.md
grep -c '\- \[ \]' tests/MANUAL-UAT.md
```
Expected: EXISTS, 4 sections, ≥18 checkboxes.
</verification>

<success_criteria>
- tests/MANUAL-UAT.md exists with four scenario sections addressing Phase 25 and Phase 26 deferred HUMAN-UAT items
- Each section has prerequisites, numbered steps, expected result, checkboxes, and date/version field
- Document ends with a note pointing operators to run automated gates first
</success_criteria>

<output>
Create `.planning/phases/31-deferred-debt-test-harness-hardening/31-02-SUMMARY.md` when done
</output>
