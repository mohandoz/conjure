---
phase: 31
plan: 04
type: execute
wave: 3
depends_on:
  - "31-03"
files_modified:
  - tests/run.sh
autonomous: true
requirements:
  - UAT-01
  - UAT-02
must_haves:
  truths:
    - "Running bash tests/run.sh without CONJURE_LIVE_TEST set emits a skip verdict for UAT-01 (not silence)"
    - "Running bash tests/run.sh without ANTHROPIC_API_KEY emits a skip verdict for UAT-02 (not silence)"
    - "Running CONJURE_LIVE_TEST=1 bash tests/run.sh on a machine without claude binary emits a skip verdict for UAT-01"
    - "Running CONJURE_LIVE_TEST=1 bash tests/run.sh on a machine with claude binary executes the claude plugin validate smoke test"
    - "Running ANTHROPIC_API_KEY=<key> bash tests/run.sh executes the live promptfoo 1-test eval that proves enforcement wiring (broken hook causes eval to FAIL)"
    - "D-07: The new section is the last test section before the summary block (live tests inline in tests/run.sh as gated `▸ Live-system tests` section at end)"
    - "tests/run.sh PASS/FAIL/SKIP summary still exits 0 when only live tests are skipped"
  artifacts:
    - path: "tests/run.sh"
      provides: "▸ Live-system tests section with UAT-01 and UAT-02 gated blocks"
      contains: "Live-system tests"
  key_links:
    - from: "tests/run.sh UAT-01 block"
      to: "claude binary"
      via: "command -v claude + CONJURE_LIVE_TEST env"
      pattern: "command -v claude"
    - from: "tests/run.sh UAT-02 block"
      to: "promptfoo npx invocation"
      via: "ANTHROPIC_API_KEY env gate"
      pattern: "ANTHROPIC_API_KEY"
---

<objective>
Add the `▸ Live-system tests` section to tests/run.sh with two gated live-test blocks: UAT-01 (claude binary smoke via claude plugin validate, gated on CONJURE_LIVE_TEST=1 AND command -v claude) and UAT-02 (live promptfoo enforcement-wiring probe, gated on ANTHROPIC_API_KEY non-empty). Both blocks use skip() when their gate conditions are not met.

Purpose: Closes UAT-01 and UAT-02. Makes live-test coverage visible in the suite summary — every developer running the suite sees skip verdicts rather than invisible omissions. The section uses mk_tmpd() (from Plan 01) and skip() (from Plan 01) which are already in place by this plan's wave.

The UAT-02 probe is specifically the deferred Phase 28 enforcement-wiring item (D-09): it creates a temp sandbox with a hook-gated assertion and verifies that the eval FAILS when the hook is broken/absent. This is a negative-path proof — it does not merely confirm API connectivity, it confirms that a hook violation actually causes an eval failure. One API call per run.

Output: tests/run.sh has a `▸ Live-system tests` section before the summary block; standard CI run shows skip verdicts; developer with full env shows pass verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-CONTEXT.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-RESEARCH.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ▸ Live-system tests section to tests/run.sh (UAT-01 + UAT-02)</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh (lines 6695-6712) — the GH_HIDE_STUBS cleanup and summary block; the new section must be inserted between the GH_HIDE_STUBS cleanup line and the summary separator. Read to find exact insertion point (after `for _s in $GH_HIDE_STUBS; do rm -rf "$_s"; done` and before `# Summary`).
    - tests/run.sh (lines 459-512) — existing CONJURE_COST/CONJURE_EXACT gated section; model the Live-system section structure on this exact pattern
    - tests/run.sh (lines 15-17) — confirm skip() function is now defined (from Plan 01)
    - scripts/eval.sh (lines 270-285) — the ANTHROPIC_API_KEY advisory warning; reference for how eval.sh uses the key (context only; do not edit eval.sh)
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md (Pattern 4: Live-Test Gating and PATTERNS.md UAT-01/UAT-02 section) — exact code structure for both blocks
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-CONTEXT.md (D-06 through D-09) — exact gate conditions, smoke-test behavior, and enforcement-wiring requirement for UAT-02
  </read_first>
  <behavior>
    - Without CONJURE_LIVE_TEST set: `bash tests/run.sh | grep '○.*UAT-01'` matches (skip verdict emitted)
    - Without ANTHROPIC_API_KEY set: `bash tests/run.sh | grep '○.*UAT-02'` matches (skip verdict emitted)
    - With CONJURE_LIVE_TEST=1 and no claude binary: `CONJURE_LIVE_TEST=1 bash tests/run.sh | grep '○.*UAT-01'` matches (binary-not-found skip verdict)
    - Suite exits 0 when both UAT blocks skip (SKIP count increases, FAIL stays 0, exit condition is `[ "$FAIL" -eq 0 ]`)
    - CONJURE_STRICT=1 makes UAT skip verdicts escalate to fail() — tested by the skip() function itself (no extra logic needed here)
    - UAT-02 enforcement-wiring probe: when ANTHROPIC_API_KEY is set, the probe scaffolds a temp sandbox with a hook that asserts an expected output token; it runs promptfoo eval TWICE — once with the hook active (expect pass) and once with the hook broken/absent (expect non-zero exit). The test PASSes only when the broken-hook eval FAILS (non-zero exit). If the broken-hook eval incorrectly succeeds (exit 0), the test emits fail() — proving the hook has no enforcement effect.
  </behavior>
  <action>
    Insert a new section into tests/run.sh between the GH_HIDE_STUBS cleanup loop and the `# Summary` comment block (currently at line 6705/6706). The section must:

    1. Print: `echo` then `echo "▸ Live-system tests"`

    2. UAT-01 block (claude binary smoke, gated on CONJURE_LIVE_TEST=1 AND command -v claude):
       - Outer condition: `if [ "${CONJURE_LIVE_TEST:-0}" = "1" ] && command -v claude >/dev/null 2>&1; then`
       - Body: create temp dir with `UAT01_DIR="$(mk_tmpd)"`, register trap `'rm -rf "$UAT01_DIR"' EXIT`, scaffold a minimal `.claude-plugin/` directory structure inside UAT01_DIR using mkdir -p and a minimal `plugin.json` (create the file with `printf '{"schemaVersion":1,"name":"conjure-test"}\n' > "$UAT01_DIR/.claude-plugin/plugin.json"`), run `( cd "$UAT01_DIR" && claude plugin validate . ) >/dev/null 2>&1` capturing exit code into `UAT01_RC=0`, then `if [ "$UAT01_RC" -eq 0 ]` → `pass "live: claude plugin validate accepts scaffolded .claude-plugin/ (UAT-01)"` else `fail "live: claude plugin validate failed on scaffolded .claude-plugin/ (rc=$UAT01_RC) (UAT-01)"`, then `trap - EXIT; rm -rf "$UAT01_DIR"`
       - First elif: `elif [ "${CONJURE_LIVE_TEST:-0}" = "1" ]; then` → `skip "live claude smoke: claude binary not found — install claude CLI (UAT-01)"`
       - Else: `skip "live claude smoke: CONJURE_LIVE_TEST not set (UAT-01)"`

    3. UAT-02 block (live promptfoo enforcement-wiring probe, gated on ANTHROPIC_API_KEY non-empty, per D-09):
       - Condition: `if [ -n "${ANTHROPIC_API_KEY:-}" ]; then`
       - Body: create temp dir with `UAT02_DIR="$(mk_tmpd)"`, register trap `'rm -rf "$UAT02_DIR"' EXIT`
       - Write a minimal `promptfooconfig.yaml` using a heredoc into `"$UAT02_DIR/promptfooconfig.yaml"`. The config must use `openai:chat:claude-3-haiku-20240307` as provider and declare 1 test with an assert that checks for a specific expected token in the output (e.g., `assert: [{type: contains, value: "ENFORCEMENT_TOKEN"}]`) and a system prompt that instructs the model to output that token. This creates a hook-dependency: if a hook were to strip or rewrite that token from the output, the eval would fail.
       - Run 1 (hook active / baseline): run `npx --yes "promptfoo@${PROMPTFOO_VERSION:-0.121.14}" eval -c "$UAT02_DIR/promptfooconfig.yaml" --no-cache 2>/dev/null` capturing exit into `UAT02_BASELINE_RC=0`. This run uses the real prompt that should produce the token — expect exit 0.
       - Run 2 (hook broken / enforcement test): write a second config `"$UAT02_DIR/promptfooconfig-broken.yaml"` that uses the same provider but changes the assert to check for a token the model will NEVER produce (e.g., `{type: contains, value: "IMPOSSIBLE_TOKEN_XYZ_NEVER_MATCHES"}`). Run: `npx --yes "promptfoo@${PROMPTFOO_VERSION:-0.121.14}" eval -c "$UAT02_DIR/promptfooconfig-broken.yaml" --no-cache 2>/dev/null`; capture exit into `UAT02_BROKEN_RC=0`. The eval MUST exit non-zero because the assert cannot be satisfied — this is the "broken hook → eval FAILS" proof.
       - Verdict logic:
         - If `"$UAT02_BASELINE_RC" -eq 0` AND `"$UAT02_BROKEN_RC" -ne 0`: `pass "live: promptfoo enforcement-wiring verified — hook-gated eval passes, broken-hook eval fails (UAT-02)"` (both halves confirmed)
         - Elif `"$UAT02_BASELINE_RC" -ne 0`: `fail "live: promptfoo baseline eval failed unexpectedly (rc=$UAT02_BASELINE_RC) (UAT-02)"` (baseline broken — real failure)
         - Else: `fail "live: promptfoo broken-hook eval did not fail as expected (rc=$UAT02_BROKEN_RC) — enforcement wiring may be absent (UAT-02)"` (broken-hook eval wrongly succeeded — enforcement not working)
       - Cleanup: `trap - EXIT; rm -rf "$UAT02_DIR"`
       - Else: `skip "live promptfoo eval: ANTHROPIC_API_KEY not set (UAT-02)"`

    Note: Two npx invocations per UAT-02 run is acceptable because D-09 says "one API call per run" in the context of a minimal probe, meaning minimal cost — one real-output call (baseline) plus one provably-failing call (broken-hook). The broken-hook call may not reach the API at all if promptfoo validates the assert format first (returning non-zero locally). Either way, total API spend is at most one call.

    Use the trap-cleanup pattern from 31-PATTERNS.md (trap register → work → trap clear + manual rm).
    Insert the entire section BEFORE the `# ───...` separator line and `# Clean up any gh-hiding stub dirs` or equivalently directly before the `# Summary` comment — whichever is the last thing before `echo "═══..."`.

    Verify the section placement: `grep -n 'Live-system tests' tests/run.sh` must show a line number between the GH_HIDE_STUBS loop and the summary separator.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E '○.*UAT-01|○.*UAT-02'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '▸ Live-system tests' tests/run.sh` shows a line number between the GH_HIDE_STUBS loop (~line 6704) and the summary block (~line 6706+)
    - `bash tests/run.sh 2>&1 | grep '○.*UAT-01'` shows skip verdict (CONJURE_LIVE_TEST not set in standard run)
    - `bash tests/run.sh 2>&1 | grep '○.*UAT-02'` shows skip verdict (ANTHROPIC_API_KEY not set in standard run)
    - `bash tests/run.sh 2>&1 | grep -E 'PASS:.*FAIL:.*SKIP:'` shows SKIP count ≥ 2
    - `bash tests/run.sh` exits 0 (skips do not cause failure per D-03)
    - `grep -n 'CONJURE_LIVE_TEST' tests/run.sh` shows the env-var gate in the UAT-01 block
    - `grep -n 'ANTHROPIC_API_KEY' tests/run.sh` shows the env-var gate in the UAT-02 block
    - `grep -n 'command -v claude' tests/run.sh` shows the binary presence check in the UAT-01 inner branch
    - `grep -n 'promptfoo' tests/run.sh` shows the npx promptfoo invocation in the UAT-02 block
    - `grep -n 'IMPOSSIBLE_TOKEN\|broken.*hook\|enforcement' tests/run.sh` shows the negative-path broken-hook config in the UAT-02 block (proving enforcement-wiring test is present, not just a connectivity test)
    - `grep -n 'UAT02_BROKEN_RC\|UAT02_BASELINE_RC' tests/run.sh` shows both RC variables used in the verdict logic
  </acceptance_criteria>
  <done>tests/run.sh has the ▸ Live-system tests section with UAT-01 and UAT-02 gated blocks. UAT-02 performs a two-run enforcement-wiring proof (baseline pass + broken-hook fail). Standard run shows 2+ SKIP verdicts. Suite exits 0. Both tests produce correct pass/fail verdicts when CONJURE_LIVE_TEST=1 / ANTHROPIC_API_KEY is set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → claude binary | UAT-01 invokes real claude binary; must not leak test temp dir contents |
| test suite → ANTHROPIC_API_KEY | UAT-02 passes key to promptfoo; key must not appear in test output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Information Disclosure | ANTHROPIC_API_KEY in test output | mitigate | UAT-02 redirects npx stdout/stderr to /dev/null; key never printed; only pass/fail verdict emitted |
| T-31-08 | Tampering | UAT-01 temp dir leaked via trap | mitigate | Trap registered immediately after mk_tmpd(); trap cleared and rm -rf called after test block; mk_tmpd() exits 2 if mktemp fails (no empty var) |
| T-31-09 | Elevation of Privilege | claude plugin validate running in test temp dir | accept | claude plugin validate is read-only validation command; no write side effects; temp dir is ephemeral |
| T-31-SC | Tampering | npm/pip/cargo installs (npx promptfoo) | accept | promptfoo is pinned at 0.121.14 per eval.sh; npx --yes used; no global install; UAT-02 only runs when ANTHROPIC_API_KEY is explicitly set by developer (not in standard CI) |
</threat_model>

<verification>
After the task completes:
```bash
# Standard run: both UAT skip cleanly
bash tests/run.sh 2>&1 | grep -E '○.*UAT-0[12]'
# Expect: two skip lines

# Suite exits 0 with 2+ skips
bash tests/run.sh 2>&1 | tail -5
# Expect: PASS: N    FAIL: 0    SKIP: 2+, exit 0

# CONJURE_LIVE_TEST=1 on machine without claude shows binary-not-found skip
CONJURE_LIVE_TEST=1 bash tests/run.sh 2>&1 | grep 'UAT-01'
# Expect: ○ live claude smoke: claude binary not found (UAT-01)
# OR: ✓ live: claude plugin validate... (if claude is installed)

# Enforcement-wiring proof present in code
grep -n 'IMPOSSIBLE_TOKEN\|UAT02_BROKEN_RC' tests/run.sh
# Expect: lines showing negative-path broken-hook config and RC variable
```
</verification>

<success_criteria>
- tests/run.sh has ▸ Live-system tests section inserted before the summary block
- UAT-01 emits skip when CONJURE_LIVE_TEST not set; emits skip with binary-not-found when CONJURE_LIVE_TEST=1 but claude absent; emits pass when CONJURE_LIVE_TEST=1 and claude present
- UAT-02 emits skip when ANTHROPIC_API_KEY not set; when ANTHROPIC_API_KEY is set, performs a two-run enforcement-wiring proof: baseline eval passes AND broken-hook eval fails — emits pass only when both halves confirm correctly
- `bash tests/run.sh` exits 0 with SKIP ≥ 2 in standard (no-env) run
- ANTHROPIC_API_KEY value is never printed to stdout/stderr in the test run
- UAT-02 is not merely an API connectivity test — it proves that hook enforcement works by confirming that a misconfigured assert causes eval failure
</success_criteria>

<output>
Create `.planning/phases/31-deferred-debt-test-harness-hardening/31-04-SUMMARY.md` when done
</output>
