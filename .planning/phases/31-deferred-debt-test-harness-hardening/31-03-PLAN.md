---
phase: 31
plan: 03
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - tests/run.sh
  - scripts/audit-setup.sh
  - FAILURE-MODES.md
autonomous: true
requirements:
  - DEBT-03
  - DEBT-06
must_haves:
  truths:
    - "D-12: All raw $(mktemp -d) calls in tests/run.sh have been replaced with $(mk_tmpd) (sweep scope: ALL test files; production scripts/ untouched)"
    - "D-13: A regression gate in tests/run.sh fails if any new raw $(mktemp -d) appears outside sandbox.sh (greps tests/ for raw $(mktemp -d) outside sandbox.sh)"
    - "D-14: The SCHM-STALE test at run.sh:5107-5139 no longer swaps lib/cc-schema.json; it uses CONJURE_SCHEMA_FILE env override (eliminate cp-swap)"
    - "scripts/audit-setup.sh resolves its schema path via CONJURE_SCHEMA_FILE env override with production fallback"
    - "D-15: FAILURE-MODES.md documents the atomic-write mandate for any future cc-schema.json write path (documentation-only atomic-write clause)"
    - "A D-16 audit grep of tests/run.sh for cp/mv targeting $CONJURE_HOME paths has been run and results recorded; any other production-file swaps found are remediated with the same no-swap/env-override treatment"
    - "tests/run.sh passes after all changes"
  artifacts:
    - path: "tests/run.sh"
      provides: "mk_tmpd sweep, regression gate, SCHM-STALE env-override rework"
      contains: "mk_tmpd"
    - path: "scripts/audit-setup.sh"
      provides: "CONJURE_SCHEMA_FILE env override at schema lookup"
      contains: "CONJURE_SCHEMA_FILE"
    - path: "FAILURE-MODES.md"
      provides: "atomic-write mandate entry"
      contains: "cc-schema.json"
  key_links:
    - from: "tests/run.sh (SCHM-STALE block)"
      to: "scripts/audit-setup.sh"
      via: "CONJURE_SCHEMA_FILE env var override"
      pattern: "CONJURE_SCHEMA_FILE"
    - from: "scripts/audit-setup.sh line 117"
      to: "lib/cc-schema.json"
      via: "CONJURE_SCHEMA_FILE env override with fallback"
      pattern: "CONJURE_SCHEMA_FILE:-"
---

<objective>
Apply the DEBT-03 mktemp sweep across all 180 raw `mktemp -d` call sites in tests/run.sh (replacing each with mk_tmpd), add the regression gate convention self-test, and apply the DEBT-06 SCHM-STALE kill-safety fix (replace the cp-swap at lines 5118-5126 with CONJURE_SCHEMA_FILE env override, add the env override hook to scripts/audit-setup.sh:117, and document the atomic-write mandate in FAILURE-MODES.md). Also perform the D-16 audit: grep tests/run.sh for all cp/mv operations targeting $CONJURE_HOME paths; record the audit result and remediate any other production-file swaps found.

Purpose: Closes two safety-critical debt items. DEBT-03 eliminates the sandbox-escape vector (git -C on empty var) across the entire test suite. DEBT-06 eliminates the RETROSPECTIVE.md:159 incident pattern (SIGKILL-interrupted test corrupting lib/cc-schema.json). D-16 ensures no other cp/mv swap of production kit files was missed.

Output: tests/run.sh, scripts/audit-setup.sh, FAILURE-MODES.md updated; full test suite green with SCHM-STALE test passing via env override; D-16 audit evidence recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-CONTEXT.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-RESEARCH.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep mktemp -d in tests/run.sh + add regression gate (DEBT-03)</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh (lines 1-50) — verify sandbox.sh is sourced at line 8 (mk_tmpd available before any mktemp call); note the IS_WINDOWS/PERF_CEILING setup; confirm TESTS() array is still POSIX-safe
    - tests/run.sh (lines 120-135) — existing convention gate shape (hook exit codes gate at lines 125-130) to model the new regression gate after
    - tests/run.sh (lines 225-230) — first representative raw mktemp -d call in the main body (TMPDIR_TARGET)
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md (Pattern 2 and Pattern 5) — exact mk_tmpd sweep pattern and regression gate grep command
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-01-SUMMARY.md — confirm mk_tmpd() is now in sandbox.sh (output of Plan 01)
  </read_first>
  <action>
    Step 1 — Grep sweep: In tests/run.sh, replace every occurrence of `VAR="$(mktemp -d)"` and `VAR=$(mktemp -d)` (all 180 occurrences) with `VAR="$(mk_tmpd)"`. This is a pure string substitution: `$(mktemp -d)` → `$(mk_tmpd)`. Do NOT change `$(mktemp)` (file form) — only directory form `$(mktemp -d)`. Do NOT touch tests/lib/sandbox.sh (already done in Plan 01).

    The substitution pattern to apply across all occurrences:
    - `"$(mktemp -d)"` → `"$(mk_tmpd)"`
    - `$(mktemp -d)` (unquoted) → `"$(mk_tmpd)"` (add quotes for safety)

    Note: The special case at line 37 inside mk_path_without_gh is `stub="$(mktemp -d)"` — this is inside a function in run.sh (not sandbox.sh) and MUST be swept too. mk_tmpd is available because sandbox.sh is sourced at line 8 before any function is called.

    Step 2 — Regression gate: Add a new convention self-test block near the existing hook-exit-code gate (around lines 120-130). Place it immediately after the `▸ Hook exit codes` block (after the done loop at line 130). The gate must:
    - Print section header: `echo` then `echo "▸ Convention: no raw \$(mktemp -d) in test files (use mk_tmpd)"`
    - Run: `RAW_MKTEMP_HITS="$(grep -rn '\$(mktemp -d)' tests/ --include='*.sh' --exclude='sandbox.sh' 2>/dev/null || true)"`
    - If `[ -z "$RAW_MKTEMP_HITS" ]`: call `pass "convention: no raw \$(mktemp -d) outside sandbox.sh"`
    - Else: call `fail "convention: raw \$(mktemp -d) found outside sandbox.sh"` and print each hit line prefixed with 4 spaces using a while-read loop

    The grep uses `--exclude='sandbox.sh'` (not `--exclude-dir`) because sandbox.sh is the one permitted location. Match 31-PATTERNS.md Pattern 5 exactly.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E 'PASS:|FAIL:|SKIP:'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mktemp -d' tests/run.sh` returns 0 (all sweep complete — no raw mktemp -d remaining outside mk_tmpd body; note: mk_tmpd is in sandbox.sh not run.sh, so run.sh should have 0)
    - `grep -n 'mk_tmpd' tests/run.sh | wc -l` returns a count approximately equal to the number of former raw mktemp -d calls (expect 175-185)
    - `grep -n "Convention: no raw" tests/run.sh` shows the regression gate section header
    - `bash tests/run.sh 2>&1 | grep "convention: no raw"` shows `✓ convention: no raw $(mktemp -d) outside sandbox.sh` (pass verdict, not fail)
    - `bash tests/run.sh` exits 0 (no regressions introduced)
  </acceptance_criteria>
  <done>All raw $(mktemp -d) in tests/run.sh replaced with $(mk_tmpd). Regression gate added. Suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-06 — SCHM-STALE env override + audit-setup.sh hook + FAILURE-MODES.md entry + D-16 production-file swap audit</name>
  <files>tests/run.sh, scripts/audit-setup.sh, FAILURE-MODES.md</files>
  <read_first>
    - tests/run.sh (lines 5107-5139) — the exact SCHM-STALE cp-swap block to replace; read every line carefully before editing
    - scripts/audit-setup.sh (lines 112-130) — the schema lookup block; line 117 is `SCHEMA_FILE="${CONJURE_HOME}/lib/cc-schema.json"`; confirm exact surrounding context before editing
    - FAILURE-MODES.md (lines 168-178 and lines 280-291) — the existing entry style to match and the current tail of the file (append location)
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md (Pattern 3 for SCHM-STALE rework and audit-setup.sh override; FAILURE-MODES.md section) — exact replacement code
    - .planning/RETROSPECTIVE.md (line 159) — the SCHM-STALE incident description (for context; do not edit this file)
  </read_first>
  <action>
    Change 1 — tests/run.sh SCHM-STALE block (lines 5117-5126, inside the `if [ "$P27_AUDIT_OK" -eq 1 ]; then` branch):
    Replace the entire cp-swap sequence:
      `P27_STALE_SCHEMA_BAK="$(mktemp)"` through `rm -f "$P27_STALE_SCHEMA_BAK"`
    with the env-override invocation from 31-PATTERNS.md Pattern 3:
      `# DEBT-06: use CONJURE_SCHEMA_FILE override — production lib/cc-schema.json never touched`
      `P27_STALE_RC=0`
      `P27_STALE_OUT="$(CONJURE_HOME="$CONJURE_HOME" \`
      `  CONJURE_SCHEMA_FILE="$CONJURE_HOME/tests/fixtures/_schema-audit-stale/cc-schema-stale.json" \`
      `  bash "$P27_AUDIT_SH" "$P27_STALE_DIR" 2>&1)" || P27_STALE_RC=$?`
    The rest of the block (the pass/fail check on P27_STALE_RC and P27_STALE_OUT, and the rm/trap cleanup) stays unchanged.
    Also: the `P27_STALE_DIR="$(mktemp -d)"` at line 5108 was already swept to `"$(mk_tmpd)"` in Task 1 above — verify it is already correct before editing.

    Change 2 — scripts/audit-setup.sh line 117:
    Replace: `SCHEMA_FILE="${CONJURE_HOME}/lib/cc-schema.json"`
    With:
      `# SAFETY: Never write directly to cc-schema.json. Any future write path MUST use:`
      `#   jq ... > "$_tmp" && mv "$_tmp" "$SCHEMA_FILE"   (POSIX mv is atomic same-fs)`
      `# CONJURE_SCHEMA_FILE allows tests to point at a fixture without touching the`
      `# production file — kill-safe by construction (DEBT-06).`
      `SCHEMA_FILE="${CONJURE_SCHEMA_FILE:-${CONJURE_HOME}/lib/cc-schema.json}"`

    Change 3 — FAILURE-MODES.md: append a new entry after the final existing section (before the `## Where to get help` line). Match the symptom/cause/fix structure from 31-PATTERNS.md (FAILURE-MODES.md section). Title: `## Test interrupted mid-swap corrupts production file`. Include the symptom (lib/cc-schema.json has stale generated date), cause (SIGKILL defeats bash trap), fix (git checkout lib/cc-schema.json), and prevention mandate (use CONJURE_SCHEMA_FILE env override; if write path must exist, use atomic `jq ... > "$_tmp" && mv "$_tmp" "$target"`).

    Change 4 — D-16 production-file swap audit (per D-16): Before finalising this task, run:
      `grep -n 'cp\|mv' tests/run.sh | grep 'CONJURE_HOME'`
    to surface any remaining cp/mv operations targeting $CONJURE_HOME paths in tests/run.sh. Record the output in the SUMMARY.md audit evidence section. For each hit found:
      - If it is the already-remediated SCHM-STALE block: confirm the swap is gone (P27_STALE_SCHEMA_BAK absent) — expected after Change 1 above.
      - If any other production-file swap is found: apply the same env-override treatment (or trap-based restore if env override is not viable) before closing this task.
    The acceptance criterion is: audit grep shows either zero hits, or all hits have been remediated and the remediation evidence is recorded.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E 'SCHM-STALE|PASS:|FAIL:|SKIP:'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'CONJURE_SCHEMA_FILE' scripts/audit-setup.sh` shows the env override on the SCHEMA_FILE line (line ~117) with safety comment above it
    - `grep -n 'P27_STALE_SCHEMA_BAK' tests/run.sh` returns empty (the BAK variable no longer exists — the swap is gone)
    - `grep -n 'CONJURE_SCHEMA_FILE' tests/run.sh` shows the env override in the SCHM-STALE block at ~line 5118-5122
    - `grep -n 'cp.*P27_SCHEMA_FILE' tests/run.sh` returns empty (no more cp to/from the production schema in this block)
    - `grep -q 'Test interrupted mid-swap corrupts production file' FAILURE-MODES.md` exits 0
    - `grep -q 'POSIX mv is atomic' FAILURE-MODES.md` exits 0
    - D-16 audit: `grep -n 'cp\|mv' tests/run.sh | grep 'CONJURE_HOME'` output is recorded in SUMMARY.md; any hits outside the now-remediated SCHM-STALE block are also remediated with env-override treatment
    - `bash tests/run.sh 2>&1 | grep 'SCHM-STALE'` shows a pass verdict: `✓ audit warns (not fails) when cc-schema.json is >90 days old (SCHM-STALE)`
    - `bash tests/run.sh` exits 0
    - `lib/cc-schema.json` is unchanged after the test run (verify via `git diff lib/cc-schema.json` — must show no diff)
  </acceptance_criteria>
  <done>SCHM-STALE test uses CONJURE_SCHEMA_FILE env override (no production file swap). audit-setup.sh has the env override with safety comment. FAILURE-MODES.md documents the atomic-write mandate. D-16 audit grep run and results recorded in SUMMARY.md; no other unmitigated production-file swaps remain. Test suite passes including SCHM-STALE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env → production kit files | tests/run.sh must never write to lib/cc-schema.json or any other production file |
| CONJURE_SCHEMA_FILE env → audit-setup.sh | env override must only widen the lookup path, never bypass security checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | SCHM-STALE cp-swap (eliminated) | mitigate | Replace cp-swap with CONJURE_SCHEMA_FILE env override; production lib/cc-schema.json is never written by tests — kill-safe by construction |
| T-31-05 | Tampering | git -C with empty mktemp result (eliminated) | mitigate | All 180 raw mktemp -d calls swept to mk_tmpd(); regression gate prevents reintroduction |
| T-31-06 | Tampering | CONJURE_SCHEMA_FILE env override path traversal | accept | Override is set only in test invocations with known-good fixture paths; no user-controlled input reaches this var in production |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
```bash
# Full suite green
bash tests/run.sh 2>&1 | tail -5
# Expect: PASS: N    FAIL: 0    SKIP: N, exit 0

# SCHM-STALE uses env override (not cp-swap)
grep -n 'P27_STALE_SCHEMA_BAK' tests/run.sh
# Expect: empty (variable gone)

grep -n 'CONJURE_SCHEMA_FILE' tests/run.sh scripts/audit-setup.sh
# Expect: results in both files

# Production schema not touched by test run
git diff lib/cc-schema.json
# Expect: no diff

# Regression gate passes
bash tests/run.sh 2>&1 | grep 'convention: no raw'
# Expect: ✓ convention: no raw $(mktemp -d) outside sandbox.sh

# D-16 audit: no unmitigated production-file swaps
grep -n 'cp\|mv' tests/run.sh | grep 'CONJURE_HOME'
# Expect: empty (all CONJURE_HOME-targeting cp/mv removed); or if hits remain, each must be documented as remediated in SUMMARY.md
```
</verification>

<success_criteria>
- tests/run.sh: all 180 raw `$(mktemp -d)` replaced with `$(mk_tmpd)`; regression gate passes; SCHM-STALE uses CONJURE_SCHEMA_FILE override
- scripts/audit-setup.sh: line 117 uses `${CONJURE_SCHEMA_FILE:-...}` with safety comment block
- FAILURE-MODES.md: atomic-write mandate entry appended
- D-16 audit evidence recorded in SUMMARY.md: grep output showing no remaining unmitigated cp/mv to $CONJURE_HOME paths
- `bash tests/run.sh` exits 0 with no new FAIL count
- `git diff lib/cc-schema.json` shows no changes after a test run
</success_criteria>

<output>
Create `.planning/phases/31-deferred-debt-test-harness-hardening/31-03-SUMMARY.md` when done
</output>
