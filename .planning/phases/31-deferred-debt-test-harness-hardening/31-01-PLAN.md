---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/preflight.sh
  - tests/lib/sandbox.sh
  - tests/run.sh
autonomous: true
requirements:
  - DEBT-03
  - DEBT-04
  - DEBT-05
must_haves:
  truths:
    - "scripts/preflight.sh exits 2 (not 1) when a required dep is missing"
    - "D-11: tests/lib/sandbox.sh exports mk_tmpd() that validates mktemp result and exits 2 on failure"
    - "D-05: tests/run.sh initializes a SKIP counter and defines skip() with CONJURE_STRICT escalation (skip() applies to NEW gates only; existing soft gates not migrated)"
    - "D-02: tests/run.sh summary line reads PASS: N    FAIL: N    SKIP: N"
  artifacts:
    - path: "scripts/preflight.sh"
      provides: "exit 2 at line 109 (formerly exit 1)"
      contains: "exit 2"
    - path: "tests/lib/sandbox.sh"
      provides: "mk_tmpd() helper"
      exports: ["mk_tmpd"]
    - path: "tests/run.sh"
      provides: "SKIP counter, skip() function, CONJURE_STRICT escalation, updated summary line"
      contains: "SKIP=0"
  key_links:
    - from: "tests/run.sh"
      to: "tests/lib/sandbox.sh"
      via: "source at line 8"
      pattern: "source.*sandbox\\.sh"
    - from: "cli/conjure"
      to: "scripts/preflight.sh"
      via: "cmd_preflight calls"
      pattern: "cmd_preflight"
---

<objective>
Apply DEBT-04 (preflight.sh exit 1 → exit 2), add mk_tmpd() to tests/lib/sandbox.sh and migrate sandbox_setup()'s own raw mktemp call (DEBT-03 foundation), and add the skip() counter + strict-mode guard + updated summary line to tests/run.sh (DEBT-05).

Purpose: These three changes are prerequisites for all subsequent plans — UAT-01/02 depend on skip(), and the run.sh mktemp sweep (Plan 03) depends on mk_tmpd() being defined in sandbox.sh first. DEBT-04 is independent and closes a project-convention violation that has existed since Phase 27.

Output: preflight.sh exits 2 correctly; sandbox.sh exports mk_tmpd(); run.sh has skip()/SKIP/strict-mode/summary changes; tests/run.sh is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-CONTEXT.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-RESEARCH.md
@.planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix preflight.sh exit code + add mk_tmpd() to sandbox.sh</name>
  <files>scripts/preflight.sh, tests/lib/sandbox.sh</files>
  <read_first>
    - scripts/preflight.sh (lines 100-115) — the block containing the sole exit 1 at line 109; also skim lines 1-30 for shebang/set flags
    - tests/lib/sandbox.sh (full file, 63 lines) — see _sandbox_tool_dir() style to match for mk_tmpd(); see sandbox_setup() line 48 raw mktemp -d to migrate
    - CLAUDE.md (Constraints section) — exit 2, never exit 1; POSIX bash 3.2+; no associative arrays / local -n
  </read_first>
  <behavior>
    - scripts/preflight.sh: grep confirms no `exit 2` at line 109 before edit; after edit, `grep -n 'exit 2' scripts/preflight.sh` shows line 109
    - tests/lib/sandbox.sh: mk_tmpd() is added above sandbox_setup(); function body uses `local _d`, calls `mktemp -d`, checks `[ -z "$_d" ] || [ ! -d "$_d" ]`, prints FATAL to stderr and exits 2 on failure, otherwise prints the path via `printf '%s' "$_d"` — no echo (avoids trailing newline ambiguity)
    - sandbox_setup() line 48: `SANDBOX_DIR="$(mktemp -d)"` becomes `SANDBOX_DIR="$(mk_tmpd)"`
    - POSIX bash 3.2+ only: no `local -n`, no `mapfile`, no associative arrays
  </behavior>
  <action>
    In scripts/preflight.sh: change line 109 from `[ "$REQUIRED_FAILED" -eq 1 ] && exit 1` to `[ "$REQUIRED_FAILED" -eq 1 ] && exit 2`. No other changes. Record the caller audit evidence in a comment above line 109 or in the commit message: all 4 callers in cli/conjure (lines 84, 172, 174, 234) use `|| return 1` (generic non-zero) — confirmed by grep, safe to change (per D-04 Claude's Discretion and CONTEXT.md).

    In tests/lib/sandbox.sh: insert mk_tmpd() between the closing brace of _sandbox_tool_dir() (after line 42) and the sandbox_setup() comment block (before line 44). Follow the exact _sandbox_tool_dir() function style: leading comment block, then function body with `local _d`. Function exits 2 (not 1) on failure per project convention. Then change line 48 `SANDBOX_DIR="$(mktemp -d)"` to `SANDBOX_DIR="$(mk_tmpd)"`.
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'exit 2' scripts/preflight.sh` shows line 109 (and no `exit 1` remains in that file)
    - `grep -n 'exit 1' scripts/preflight.sh` returns empty (no false positives)
    - `grep -n 'mk_tmpd' tests/lib/sandbox.sh` shows: the function definition AND the sandbox_setup() call at line 48 (now migrated)
    - `grep -n 'mktemp -d' tests/lib/sandbox.sh` shows exactly one occurrence — inside mk_tmpd() itself; sandbox_setup() line shows mk_tmpd
    - `bash tests/run.sh 2>&1 | grep -E 'PASS:|FAIL:'` exits 0 and shows no regression (FAIL count unchanged from before)
  </acceptance_criteria>
  <done>preflight.sh line 109 exits 2; sandbox.sh exports mk_tmpd() with validation + exit 2; sandbox_setup() uses mk_tmpd(); test suite still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add skip() counter + strict-mode guard + update summary line in tests/run.sh</name>
  <files>tests/run.sh</files>
  <read_first>
    - tests/run.sh (lines 10-17) — exact current pass()/fail() pattern, PASS/FAIL/TESTS() declarations; this is the insertion point for SKIP=0 and skip()
    - tests/run.sh (lines 6706-6712) — exact current summary block; this is the target for the SKIP column addition
    - .planning/phases/31-deferred-debt-test-harness-hardening/31-PATTERNS.md (Pattern 1: skip() function, lines 26-73) — exact code shape to implement
  </read_first>
  <behavior>
    - SKIP=0 initialised on the same line block as PASS=0 and FAIL=0 (after FAIL=0, before or alongside TESTS=())
    - skip() function is added immediately after fail() at line 16; body: if CONJURE_STRICT=1 → call fail() with "$1 (SKIPPED in strict mode)"; else printf "  ○ %s\n" "$1" and SKIP=$((SKIP+1))
    - Summary line at tail becomes: `echo "PASS: $PASS    FAIL: $FAIL    SKIP: $SKIP"` — four spaces between each column, matching existing style
    - Exit condition `[ "$FAIL" -eq 0 ]` is NOT changed (per D-03)
    - CONJURE_STRICT default is 0 via `${CONJURE_STRICT:-0}` (not relying on set -u to catch unset)
  </behavior>
  <action>
    Edit tests/run.sh in two locations:

    Location 1 (lines 10-16 region): Add `SKIP=0` after `FAIL=0` on line 11. Add skip() function immediately after the `fail()` line (currently line 16). The skip() function body follows the exact pattern from 31-PATTERNS.md Pattern 1: three lines — `if [ "${CONJURE_STRICT:-0}" = "1" ]; then`, `  fail "$1 (SKIPPED in strict mode)"`, `else`, `  printf "  ○ %s\n" "$1"; SKIP=$((SKIP+1))`, `fi`. Use the `○` glyph (Unicode U+25CB white circle), matching D-01.

    Location 2 (line 6709 — the summary echo): change `echo "PASS: $PASS    FAIL: $FAIL"` to `echo "PASS: $PASS    FAIL: $FAIL    SKIP: $SKIP"`. Leave the `[ "$FAIL" -eq 0 ]` exit condition on the next line exactly as-is (per D-03).
  </action>
  <verify>
    <automated>bash tests/run.sh 2>&1 | grep -E 'PASS:.*FAIL:.*SKIP:'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'SKIP=0' tests/run.sh` shows the initialisation line in the 10-17 block
    - `grep -n 'skip()' tests/run.sh` shows the function definition after fail()
    - `grep -n 'CONJURE_STRICT' tests/run.sh` shows the strict-mode guard inside skip()
    - `bash tests/run.sh 2>&1 | grep -E 'PASS:[[:space:]]+[0-9]+[[:space:]]+FAIL:[[:space:]]*[0-9]+[[:space:]]+SKIP:[[:space:]]*[0-9]+'` exits 0
    - `bash tests/run.sh` exits 0 (no regressions)
    - `CONJURE_STRICT=1 bash tests/run.sh 2>&1 | grep -E 'PASS:|FAIL:'` exits 0 with no live tests to skip yet (strict mode has no skips to escalate at this point)
  </acceptance_criteria>
  <done>tests/run.sh has SKIP=0, skip() with strict-mode guard, and summary line shows PASS/FAIL/SKIP counts. Test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → production scripts | tests/run.sh invokes scripts/preflight.sh; must not corrupt production kit files |
| test temp dirs → repo CWD | mktemp -d failure can cause git -C to operate on repo CWD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | tests/lib/sandbox.sh mk_tmpd() | mitigate | Validate mktemp result non-empty AND directory exists before returning; exit 2 on failure so git -C never sees empty var |
| T-31-02 | Tampering | scripts/preflight.sh exit code | mitigate | Change exit 1 → exit 2 only; all callers use generic non-zero (|| return 1) — confirmed by grep of cli/conjure:84,172,174,234 |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this plan; no package manager invocations |
</threat_model>

<verification>
After both tasks complete, verify the full suite is green and the SKIP column is present in summary output:

```bash
bash tests/run.sh 2>&1 | tail -6
# Expect: PASS: N    FAIL: 0    SKIP: N (where N ≥ 0)
# Expect: exit code 0
```

Confirm the preflight exit-code fix:
```bash
grep -n 'exit' scripts/preflight.sh | grep -v '^#'
# Line 109 must show: exit 2
# No line 109 exit 1 should remain
```

Confirm mk_tmpd() in sandbox.sh:
```bash
grep -A 10 'mk_tmpd()' tests/lib/sandbox.sh
# Must show: local _d, mktemp -d, non-empty check, exit 2 on failure, printf '%s' "$_d"
```
</verification>

<success_criteria>
- scripts/preflight.sh line 109: `exit 2` (not `exit 1`)
- tests/lib/sandbox.sh: mk_tmpd() defined and sandbox_setup() uses it
- tests/run.sh: SKIP=0 initialised, skip() defined with CONJURE_STRICT guard, summary includes SKIP column
- `bash tests/run.sh` exits 0 with no new failures
</success_criteria>

<output>
Create `.planning/phases/31-deferred-debt-test-harness-hardening/31-01-SUMMARY.md` when done
</output>
