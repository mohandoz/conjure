---
phase: 08-nyquist-compliance-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md
  - .planning/phases/05-readme-demo/05-VALIDATION.md
autonomous: true
requirements:
  - TECH-02c
  - TECH-02d

must_haves:
  truths:
    - "A contributor can copy-paste any verify block from 04-VALIDATION.md and confirm regression suite behavior without reading source code"
    - "A contributor can copy-paste any verify block from 05-VALIDATION.md and confirm README demo artifacts exist without reading source code"
    - "04-VALIDATION.md covers: green fixtures audit exit 0, _broken fixture exits non-zero, dry-run leaves tree byte-identical, CLAUDE.md size cap violation detected"
    - "05-VALIDATION.md covers: record-demo.sh is executable, README.md contains demo reference, CLI version exits 0"
  artifacts:
    - path: ".planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md"
      provides: "Executable verify commands for Phase 04 regression suite and dry-run proof"
      contains: "<!-- Covers: TECH-02c"
    - path: ".planning/phases/05-readme-demo/05-VALIDATION.md"
      provides: "Executable verify commands for Phase 05 README demo"
      contains: "<!-- Covers: TECH-02d"
  key_links:
    - from: ".planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md"
      to: "scripts/audit-setup.sh"
      via: "verify block invocations"
      pattern: "bash scripts/audit-setup.sh"
    - from: ".planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md"
      to: "tests/fixtures/"
      via: "fixture copy commands"
      pattern: "tests/fixtures/"
    - from: ".planning/phases/05-readme-demo/05-VALIDATION.md"
      to: "scripts/record-demo.sh"
      via: "executable check"
      pattern: "record-demo.sh"
---

<objective>
Create VALIDATION.md files for Phase 04 (regression suite and dry-run proof) and Phase 05 (README demo). Each file contains standalone, copy-paste-runnable shell verify blocks that confirm the phase's shipped behavior.

Purpose: Close the Nyquist compliance gap for TECH-02c and TECH-02d (per D-01 through D-04 in 08-CONTEXT.md).
Output: Two VALIDATION.md files covering golden-file fixture audits, dry-run byte-identity, failure-mode detection, and README demo artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-nyquist-compliance-backfill/08-CONTEXT.md
@.planning/ROADMAP.md
@tests/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 04-VALIDATION.md for Phase 04 (Regression Suite + Dry-Run Proof)</name>
  <files>.planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md</files>
  <read_first>
    - tests/run.sh lines 252-375 (fixture audit loop, golden-file EXPECT loop, dry-run snapshot, FM-1 size cap — exact commands and expected output patterns)
    - tests/fixtures/ directory listing (to know which fixture profile names appear in expected output)
  </read_first>
  <action>
    Create .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md following D-03 and D-04.

    Header line (per D-04):
    <!-- Covers: TECH-02c | TEST-03, TEST-05, TEST-06, TEST-07 -->

    Title: # Phase 04 VALIDATION

    Write exactly 4 ## Verify sections. All blocks run from repo root with CONJURE_HOME=$(pwd).

    Section 1 — "Verify all green fixtures audit exit 0 (TEST-03, TEST-05)" (mirrors run.sh lines 254-268):
    This block iterates tests/fixtures/[^_]*/ in a for loop. For each fixture, copy the fixture tree into a fresh tmpdir, run bash scripts/audit-setup.sh against it, and print PASS/FAIL. Use the same copy-then-audit pattern (no sourcing sandbox.sh — keep standalone per D-02):
    ```
    CONJURE_HOME=$(pwd)
    for fx in tests/fixtures/[^_]*/; do
      prof=$(basename "$fx")
      TMPDIR=$(mktemp -d)
      cp -r "$fx/." "$TMPDIR/"
      AUDIT_OUT=$(bash scripts/audit-setup.sh "$TMPDIR" 2>&1)
      rc=$?
      rm -rf "$TMPDIR"
      [ "$rc" -eq 0 ] && echo "PASS: $prof" || echo "FAIL: $prof (rc=$rc)"
    done
    ```
    Expected: `PASS:` line for each profile; no `FAIL:` lines.

    Section 2 — "Verify _broken fixture audit exits non-zero (TEST-07)" (mirrors run.sh lines 273-291):
    Copy _broken fixture into a fresh tmpdir, run audit-setup.sh, capture exit code.
    ```
    CONJDIR=$(pwd)
    TMPDIR=$(mktemp -d); trap 'rm -rf "$TMPDIR"' EXIT
    cp -r tests/fixtures/_broken/. "$TMPDIR/"
    bash scripts/audit-setup.sh "$TMPDIR" 2>&1
    echo "exit: $?"
    ```
    Expected: `exit: [^0]` (non-zero exit code) and output containing text from tests/fixtures/_broken/EXPECT.

    Section 3 — "Verify dry-run leaves fixture tree byte-identical (TEST-05)" (mirrors run.sh lines 317-332):
    Use python-fastapi as the representative fixture (single fixture is sufficient for VALIDATION.md — full matrix runs in tests/run.sh):
    ```
    CONJURE_HOME=$(pwd)
    ORIG=$(mktemp -d); SNAP=$(mktemp -d)
    trap 'rm -rf "$ORIG" "$SNAP"' EXIT
    cp -r tests/fixtures/python-fastapi/. "$ORIG/"
    cp -r tests/fixtures/python-fastapi/. "$SNAP/"
    cli/conjure init --dry-run "$SNAP" >/dev/null 2>&1 || true
    diff -r "$SNAP" "$ORIG" && echo "PASS: byte-identical" || echo "FAIL: diff found"
    ```
    Expected: `PASS: byte-identical`

    Section 4 — "Verify audit detects CLAUDE.md size cap violation (TEST-07 FM-1)" (mirrors run.sh lines 337-348):
    ```
    TMPDIR=$(mktemp -d); trap 'rm -rf "$TMPDIR"' EXIT
    printf '# size-cap test\n' > "$TMPDIR/CLAUDE.md"
    for i in $(seq 1 205); do printf '# filler %s\n' "$i" >> "$TMPDIR/CLAUDE.md"; done
    bash scripts/audit-setup.sh "$TMPDIR" 2>&1 | grep -i "HARD CAP exceeded"
    ```
    Expected: line containing `HARD CAP exceeded`

    Do NOT include prose background, failure modes, or explanatory paragraphs.
  </action>
  <verify>
    <automated>
      grep -c '<!-- Covers: TECH-02c' .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md &&
      grep -c '## Verify' .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md | grep -E '^4$' &&
      grep -c 'Expected:' .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md | grep -E '^4$' &&
      grep -c 'HARD CAP' .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md | grep -E '^[1-9]'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md
    - First line matches: <!-- Covers: TECH-02c | TEST-03, TEST-05, TEST-06, TEST-07 -->
    - File contains exactly 4 ## Verify sections
    - File contains exactly 4 **Expected:** lines
    - grep -c 'PASS: byte-identical' == 1 (TEST-05 assertion present)
    - grep -c 'HARD CAP' >= 1 (FM-1 assertion present)
    - grep -c 'tests/fixtures' >= 3 (fixture paths referenced in verify blocks)
    - grep -c 'TMPDIR.*mktemp' >= 3 (each filesystem-touching block has its own tmpdir)
    - grep -c 'bash scripts/audit-setup.sh' >= 3 (audit-setup.sh invoked across sections)
  </acceptance_criteria>
  <done>04-VALIDATION.md exists with 4 standalone verify sections covering TEST-03, TEST-05, TEST-06, TEST-07 behaviors. Each section is independently copy-paste-runnable from repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Create 05-VALIDATION.md for Phase 05 (README Demo)</name>
  <files>.planning/phases/05-readme-demo/05-VALIDATION.md</files>
  <read_first>
    - tests/run.sh line 27 (CLI smoke test — conjure version)
    - scripts/record-demo.sh (confirm file exists and what it does at a high level)
    - README.md (confirm what demo reference pattern it contains — demo.gif, demo.cast, or asciinema)
  </read_first>
  <action>
    Create .planning/phases/05-readme-demo/05-VALIDATION.md following D-03 and D-04.

    Phase 05 is documentation-centric. The verifiable shipped artifacts are:
    1. scripts/record-demo.sh exists and is executable
    2. README.md contains a reference to the demo recording (demo.gif, demo.cast, or asciinema embed)
    3. CLI smoke test: cli/conjure version exits 0

    Header line (per D-04):
    <!-- Covers: TECH-02d | DOCS-01 -->

    Title: # Phase 05 VALIDATION

    Write exactly 3 ## Verify sections. These blocks are primarily static-file checks (no tmpdir needed), except the CLI smoke test.

    Section 1 — "Verify scripts/record-demo.sh exists and is executable (DOCS-01)":
    ```
    [ -x scripts/record-demo.sh ] && echo "PASS: executable" || echo "FAIL: missing or not executable"
    ```
    Expected: `PASS: executable`

    Section 2 — "Verify README.md contains demo recording reference (DOCS-01)":
    ```
    grep -iE 'demo\.gif|demo\.cast|asciinema' README.md
    ```
    Expected: line containing `demo.gif`, `demo.cast`, or `asciinema`

    Section 3 — "Verify CLI smoke: conjure version exits 0 (DOCS-01)" (mirrors run.sh line 27):
    ```
    CONJURE_HOME=$(pwd) cli/conjure version
    echo "exit: $?"
    ```
    Expected: version string output followed by `exit: 0`

    Do NOT include prose background, failure modes, or explanatory paragraphs.
    These are intentionally brief — Phase 05 delivered a demo recording artifact, not executable logic, so 3 verify sections is the correct scope per D-03 (derive from phase success criteria, not exhaustive edge cases).
  </action>
  <verify>
    <automated>
      grep -c '<!-- Covers: TECH-02d' .planning/phases/05-readme-demo/05-VALIDATION.md &&
      grep -c '## Verify' .planning/phases/05-readme-demo/05-VALIDATION.md | grep -E '^3$' &&
      grep -c 'Expected:' .planning/phases/05-readme-demo/05-VALIDATION.md | grep -E '^3$' &&
      grep -c 'DOCS-01' .planning/phases/05-readme-demo/05-VALIDATION.md | grep -E '^[1-9]'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/05-readme-demo/05-VALIDATION.md
    - First line matches: <!-- Covers: TECH-02d | DOCS-01 -->
    - File contains exactly 3 ## Verify sections
    - File contains exactly 3 **Expected:** lines
    - grep -c 'record-demo.sh' >= 1 (demo script check present)
    - grep -c 'PASS: executable' == 1 (script executability assertion present)
    - grep -c 'demo\.gif\|demo\.cast\|asciinema' >= 1 (README demo reference check present)
    - grep -c 'conjure version' >= 1 (CLI smoke test present)
    - grep -c 'exit: 0' >= 1 (exit code assertion present)
  </acceptance_criteria>
  <done>05-VALIDATION.md exists with 3 standalone verify sections covering DOCS-01 behavior. Each section is independently copy-paste-runnable from repo root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → documentation | VALIDATION.md commands are run by contributors in their local shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | 04-VALIDATION.md fixture loop commands | accept | mktemp -d + trap cleanup; copies fixture trees to isolated tmpdirs; no persistent side-effects |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm both VALIDATION.md files exist
ls -la .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md
ls -la .planning/phases/05-readme-demo/05-VALIDATION.md

# Confirm header coverage annotations
head -1 .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md
head -1 .planning/phases/05-readme-demo/05-VALIDATION.md

# Confirm section counts
grep -c '## Verify' .planning/phases/04-regression-suite-dry-run-proof/04-VALIDATION.md
grep -c '## Verify' .planning/phases/05-readme-demo/05-VALIDATION.md

# CI passes
bash tests/run.sh 2>&1 | tail -5
```
</verification>

<success_criteria>
- 04-VALIDATION.md and 05-VALIDATION.md both exist in their respective phase directories
- Both files start with the correct <!-- Covers: TECH-02x | ... --> header annotation
- 04-VALIDATION.md has 4 ## Verify sections; 05-VALIDATION.md has 3 ## Verify sections
- Every filesystem-touching section has its own inline tmpdir setup
- Every section has an **Expected:** line with a grep-friendly partial pattern
- CI (bash tests/run.sh) exits 0 with the new files present
</success_criteria>

<output>
Create .planning/phases/08-nyquist-compliance-backfill/08-02-SUMMARY.md when done.
</output>
