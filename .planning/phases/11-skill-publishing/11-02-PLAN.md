---
phase: 11-skill-publishing
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - cli/conjure
  - tests/run.sh
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04

must_haves:
  truths:
    - "conjure publish-skill test-skill runs without error (all gates pass in sandbox)"
    - "conjure publish-skill exits 1 when no skill name is given"
    - "conjure publish-skill --to org/repo passes TARGET_REPO=org/repo to publish-skill.sh"
    - "bash tests/run.sh exits 0 with all SKILL-01 through SKILL-04 tests passing"
    - "conjure publish-skill appears in conjure help output"
  artifacts:
    - path: "cli/conjure"
      provides: "cmd_publish_skill dispatch + usage update"
      contains: "cmd_publish_skill"
    - path: "tests/run.sh"
      provides: "SKILL-01 through SKILL-04 regression block"
      contains: "SKILL publish-skill tests"
  key_links:
    - from: "cli/conjure cmd_publish_skill"
      to: "scripts/publish-skill.sh"
      via: "bash $CONJURE_HOME/scripts/publish-skill.sh"
      pattern: "bash.*publish-skill.sh"
    - from: "tests/run.sh SKILL block"
      to: "scripts/publish-skill.sh"
      via: "sandbox copy + bash invocation"
      pattern: "publish-skill.sh.*test-skill"
---

<objective>
Wire cli/conjure and extend tests/run.sh to complete the conjure publish-skill command.

Two tasks in parallel scope:
1. Add cmd_publish_skill function + publish-skill dispatch case + usage() line to cli/conjure
2. Add SKILL-01..SKILL-04 regression test block to tests/run.sh using the MKTPL sandbox pattern

Purpose: Deliver the user-facing command surface (SKILL-01, SKILL-02, SKILL-03, SKILL-04) and regression coverage.
Output: Modified cli/conjure and tests/run.sh; bash tests/run.sh exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-skill-publishing/11-CONTEXT.md
@.planning/phases/11-skill-publishing/11-RESEARCH.md
@.planning/phases/11-skill-publishing/11-PATTERNS.md
@.planning/phases/11-skill-publishing/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmd_publish_skill and dispatch to cli/conjure</name>
  <files>cli/conjure</files>

  <read_first>
    - cli/conjure — read the full file before editing; specifically: cmd_publish function (lines ~264-278) as the exact template for cmd_publish_skill; dispatch table (lines ~288-301) where publish-skill) case must be inserted before the wildcard *) case; usage() function (lines ~27-49) where "conjure publish-skill" line must be added after "conjure publish"
    - .planning/phases/11-skill-publishing/11-PATTERNS.md — "cli/conjure — add cmd_publish_skill + dispatch case" section; complete code excerpts for cmd_publish_skill function and dispatch table insertion
    - .planning/phases/11-skill-publishing/11-RESEARCH.md — Pitfall 6 (missing positional arg check); Pitfall 5 (DRY_RUN env var name)
    - .planning/phases/11-skill-publishing/11-CONTEXT.md — D-04, D-05 (--to flag and fallback behavior); D-07/D-08 (skill name positional, not --skill=)
  </read_first>

  <action>
  Make three edits to cli/conjure. Read the full file first to identify exact insertion points.

  EDIT 1 — Add cmd_publish_skill function:
  Insert immediately after the closing brace of cmd_publish() (currently ends around line 278) and before cmd_help(). The function must exactly follow the shape of cmd_publish():
  - Local vars: skill_name="" target_repo="mohandoz/conjure" dryrun=0
  - while loop cases: --to) shift; target_repo="${1:-}" ;; | --to=*) target_repo="${1#--to=}" ;; | --dry-run) dryrun=1 ;; | --help|-h) echo "Usage: conjure publish-skill <name> [--to <org/repo>] [--dry-run]"; return 0 ;; | -*) echo "Unknown option: $1"; return 1 ;; | *) skill_name="$1" ;;
  - After loop: [ -z "$skill_name" ] && { echo "Usage: conjure publish-skill <name> [--to <org/repo>] [--dry-run]"; return 1; }
  - Dispatch line: CONJURE_HOME="$CONJURE_HOME" DRY_RUN="$dryrun" TARGET_REPO="$target_repo" bash "$CONJURE_HOME/scripts/publish-skill.sh" "$skill_name"
  Function name: cmd_publish_skill

  EDIT 2 — Add dispatch case:
  In the dispatch case block (currently around line 288-301), insert before the publish) line or after it — either position is acceptable as long as it appears before the wildcard *) case:
    publish-skill)    shift; cmd_publish_skill "$@"    ;;
  Do not remove or reorder any existing case entries.

  EDIT 3 — Update usage():
  In the usage() heredoc (cat <<EOF block), add after the "conjure publish [--submit] [--dry-run]" line:
    conjure publish-skill <name> [--to <org/repo>] [--dry-run]
  Use consistent indentation (two spaces) matching the surrounding lines.
  </action>

  <verify>
    <automated>bash cli/conjure help 2>&1 | grep -q 'publish-skill' && echo "usage ok" && shellcheck cli/conjure && echo "shellcheck clean"</automated>
  </verify>

  <acceptance_criteria>
  - cli/conjure contains "cmd_publish_skill" function definition (grep -c 'cmd_publish_skill()' cli/conjure >= 1)
  - cli/conjure dispatch table contains "publish-skill)" case (grep -c 'publish-skill)' cli/conjure >= 1)
  - cli/conjure usage output includes "publish-skill": bash cli/conjure help 2>&1 | grep -q 'publish-skill' exits 0
  - shellcheck cli/conjure exits 0 with no warnings
  - bash cli/conjure publish-skill exits 1 (no skill name given) and prints usage to stdout or stderr
  - The function passes DRY_RUN, TARGET_REPO, and CONJURE_HOME as env vars to scripts/publish-skill.sh (grep -c 'TARGET_REPO' cli/conjure >= 1)
  </acceptance_criteria>

  <done>cli/conjure contains cmd_publish_skill, the publish-skill) dispatch case, and the usage() line; shellcheck passes; invoking conjure publish-skill with no args exits 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add SKILL-01..SKILL-04 regression tests to tests/run.sh</name>
  <files>tests/run.sh</files>

  <read_first>
    - tests/run.sh — read the full file; specifically lines 755-897 (MKTPL block + summary) as the exact structural template for the SKILL block; the SKILL block must be inserted before the summary (echo PASS/FAIL lines) at the end of the file; note the rm -rf "$MKTPL_DIR" at line 888 and the summary starting at line 890
    - .planning/phases/11-skill-publishing/11-PATTERNS.md — "tests/run.sh — add SKILL-01..SKILL-04 test block" section; sandbox setup, assertion patterns, negative test pattern, cleanup
    - .planning/phases/11-skill-publishing/11-RESEARCH.md — Pitfall 4 (sandbox missing git tag), Phase Requirements → Test Map (full list of required test cases); Pitfall 3 (two git contexts: $TARGET for skill check, $CONJURE_HOME for tag check)
    - .planning/phases/11-skill-publishing/11-CONTEXT.md — D-07 exact failure messages for dirty-tree and untagged-head guards; D-03 (gh pr create must not be executed — test that the word "create" appears in output without the script actually firing)
  </read_first>

  <action>
  Insert a SKILL test block into tests/run.sh immediately before the existing summary section (the "═══" echo lines and PASS/FAIL output near the end of the file). The block must follow the MKTPL sandbox pattern exactly.

  The SKILL block must cover all required test cases from the RESEARCH.md Phase Requirements → Test Map. Structure:

  SECTION 1 — Block header and sandbox setup:
  Print section header: echo and echo "▸ SKILL publish-skill tests (SKILL-01 through SKILL-04)".
  Create sandbox: SKILL_DIR="$(mktemp -d)". Initialize a real git repo: git -C "$SKILL_DIR" init -q; git config user.email/user.name. Create directory: mkdir -p "$SKILL_DIR/.claude/skills/test-skill" "$SKILL_DIR/scripts" "$SKILL_DIR/lib". Write a clean SKILL.md with valid frontmatter (name: test-skill, description at least 40 chars with no egress content, a body section with no network patterns). Copy scripts/publish-skill.sh, lib/mutate.sh, and VERSION into the sandbox. Commit everything: git add -A && git commit -m "add test-skill". CRITICAL: tag the sandbox HEAD with git -C "$SKILL_DIR" tag "v$(cat "$CONJURE_HOME/VERSION")" so the conjure-tag guard passes on the happy path (Pitfall 4).

  SECTION 2 — SKILL-01 tests:
  Test: dry-run output — DRY_RUN=1 bash "$SKILL_DIR/scripts/publish-skill.sh" test-skill 2>&1; check output contains "dry-run". Pass/fail with label "publish-skill --dry-run prints dry-run accounting (SKILL-01)".
  Test: size cap — write a 201-line SKILL.md to the sandbox (201 newlines, any content), run the script, expect exit code 1. Restore original SKILL.md via git -C "$SKILL_DIR" checkout -- . before next test.
  Test: frontmatter missing name — write a SKILL.md with no name: field; expect exit code 1. Restore.
  Test: egress scan blocks curl — write a SKILL.md body containing "curl https://example.com"; expect exit code 1. Restore.
  Test: egress scan blocks $SECRET — write a SKILL.md body containing "$SECRET"; expect exit code 1. Restore.
  Test: clean skill passes all gates — run the script with no modifications; expect exit code 0. Label: "publish-skill exits 0 for valid clean skill (SKILL-01)".

  SECTION 3 — SKILL-02 tests:
  Test: gh present (stub) — run script with PATH modified to include a stub 'gh' that does nothing (create a temp bin dir with an executable 'gh' stub that just exits 0); check output contains "gh pr create". Label: "publish-skill prints gh pr create when gh is present (SKILL-02)".
  Test: gh absent — run script with PATH set to exclude gh (PATH=/usr/bin:/bin); check output contains "github.com/$TARGET_REPO" or "manually". Label: "publish-skill prints manual URL when gh is absent (SKILL-02)".

  SECTION 4 — SKILL-03 tests:
  Test: dirty skill → exit 1 — append "dirty" to the SKILL.md to make it uncommitted, capture exit code. Expect exit code 1. Also check output contains "uncommitted". Restore via git -C "$SKILL_DIR" checkout -- .
  Test: untagged conjure HEAD → exit 1 — create a separate copy of publish-skill.sh that points to a git repo with no tags (create a tmpdir, git init, copy VERSION, commit but do NOT tag). Expect exit code 1. Check output contains "tagged release". Clean up the untagged sandbox.

  SECTION 5 — SKILL-04 test:
  Test: --to flag — run bash "$SKILL_DIR/scripts/publish-skill.sh" test-skill --to myorg/myrepo 2>&1; check output contains "myorg/myrepo". Label: "--to flag substitutes target repo in PR instructions (SKILL-04)".

  SECTION 6 — Cleanup:
  rm -rf "$SKILL_DIR"

  IMPORTANT test infrastructure rules:
  - Use the same pass()/fail() functions already defined at the top of tests/run.sh; do not redefine them
  - Capture exit codes with: RC=0; bash ... || RC=$?; then check [ "$RC" -eq 1 ]
  - Restore sandbox after each mutating test (git checkout -- . or re-write the file)
  - All sandbox writes stay inside SKILL_DIR; never write to the real .claude/skills/
  - Test label strings must include the SKILL-NN identifier in parentheses at the end (matches MKTPL convention)
  - gh stub: create STUB_BIN="$(mktemp -d)" && printf '#!/bin/sh\nexit 0\n' > "$STUB_BIN/gh" && chmod +x "$STUB_BIN/gh" && PATH="$STUB_BIN:$PATH" for that one test invocation; restore PATH afterward
  </action>

  <verify>
    <automated>bash tests/run.sh 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - tests/run.sh contains "SKILL publish-skill tests" section header (grep -c 'SKILL publish-skill tests' tests/run.sh >= 1)
  - tests/run.sh contains SKILL-01 label (grep -c 'SKILL-01' tests/run.sh >= 3)
  - tests/run.sh contains SKILL-02 label (grep -c 'SKILL-02' tests/run.sh >= 2)
  - tests/run.sh contains SKILL-03 label (grep -c 'SKILL-03' tests/run.sh >= 2)
  - tests/run.sh contains SKILL-04 label (grep -c 'SKILL-04' tests/run.sh >= 1)
  - bash tests/run.sh exits 0 (all assertions pass including the new SKILL block)
  - bash tests/run.sh output contains "FAIL: 0"
  - shellcheck tests/run.sh exits 0
  </acceptance_criteria>

  <done>tests/run.sh contains the SKILL regression block with all required test cases; bash tests/run.sh exits 0 with FAIL: 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → cmd_publish_skill | skill_name and target_repo parsed from user input in cli/conjure |
| Test sandbox writes → temp dir | All test-injected content stays inside mktemp -d SKILL_DIR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | skill_name positional arg in cmd_publish_skill | mitigate | Empty-name guard: [ -z "$skill_name" ] && { echo usage; return 1; }; name is passed directly to publish-skill.sh which validates it against the schema pattern |
| T-11-07 | Tampering | target_repo from --to flag | mitigate | Validation in publish-skill.sh (Plan 01, T-11-02); cli/conjure passes it as-is via TARGET_REPO env var |
| T-11-08 | Tampering | Test sandbox contamination | mitigate | All test writes inside mktemp -d SKILL_DIR; cleanup via rm -rf at block end; no writes to real .claude/skills/ |
| T-11-09 | Denial of Service | Test gh stub leaking into PATH for subsequent tests | mitigate | Restore PATH after the gh-present test: use a subshell or reset PATH=$(echo "$PATH" | sed ...) immediately after the single invocation |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; editing bash files only |
</threat_model>

<verification>
bash tests/run.sh exits 0 with "FAIL: 0"
bash cli/conjure help | grep -q 'publish-skill'
shellcheck cli/conjure exits 0
shellcheck tests/run.sh exits 0
</verification>

<success_criteria>
- conjure publish-skill appears in help output
- conjure publish-skill (no args) exits 1 with usage
- bash tests/run.sh exits 0 — all SKILL-01 through SKILL-04 tests pass, existing tests unaffected
- shellcheck passes on both modified files
</success_criteria>

<output>
Create `.planning/phases/11-skill-publishing/11-02-SUMMARY.md` when done
</output>
